#!/bin/bash

# Остановить текущие контейнеры и удалить тома
echo "🔄 Останавливаем текущие контейнеры и удаляем тома..."
docker-compose down -v

# Пересобрать образ с исправлениями путей внутри
echo "🔨 Пересобираем Docker-образ (исправление путей происходит внутри Dockerfile)..."
docker-compose build --no-cache

# Запустить контейнеры заново
echo "🚀 Запускаем контейнеры..."
docker-compose up -d

# Проверить логи для диагностики
echo "📋 Просмотр логов приложения:"
docker-compose logs -f 