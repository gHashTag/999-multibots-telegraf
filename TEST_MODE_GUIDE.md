# 🧪 ТЕСТОВЫЙ РЕЖИМ ДЛЯ ШАБЛОНА 1

**Дата:** 2 ноября 2025
**Статус:** ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ
**Назначение:** Бесплатное тестирование монтажа lip-sync

---

## 🎯 НАЗНАЧЕНИЕ:

Позволяет тестировать логику монтажа lip-sync БЕСПЛАТНО без списания денег для проверки:
- ✅ Работы с видео
- ✅ Обрезки видео по длине аудио
- ✅ Генерации TTS
- ✅ Workflow целиком

---

## 🔧 КАК ВКЛЮЧИТЬ:

### Вариант 1: Переменная окружения
```bash
export LIPSYNC_TEST_MODE=true
npm start
```

### Вариант 2: В .env файле
```
LIPSYNC_TEST_MODE=true
```

### Вариант 3: Автоматически в тестах
```bash
npm test
# TEST_MODE = true автоматически
```

---

## 🎬 WORKFLOW В ТЕСТОВОМ РЕЖИМЕ:

```
1. 👤 Пользователь → "🎬 ИИ Рилс" → "1️⃣ Шаблон 1"
2. 📹 Загружает видео
3. ✍️ Вводит текст
4. 🧪 ПОКАЗЫВАЕТСЯ: "ТЕСТОВЫЙ РЕЖИМ - Бесплатно!"
5. 💰 НЕ списываются деньги
6. 🎤 Генерируется TTS (реально)
7. 🎬 Создается lip-sync (тестовые данные)
8. ✂️ Обрезается видео по длине аудио (если есть аудио)
9. 📤 Отправляется результат с пометкой "🧪 Тестовый режим"
```

---

## 📊 КОД ТЕСТОВОГО РЕЖИМА:

### ai-reels-wizard.ts
```typescript
// Режим тестирования - бесплатно для проверки монтажа
const TEST_MODE = process.env.NODE_ENV === 'test' || process.env.LIPSYNC_TEST_MODE === 'true'

// В Step 3 - проверка баланса
if (!TEST_MODE) {
  // Проверяем баланс и списываем деньги
} else {
  // Пропускаем проверку, показываем "ТЕСТОВЫЙ РЕЖИМ"
}

// В обработке ошибки
if (!TEST_MODE) {
  // Возвращаем деньги при ошибке
} else {
  // Просто сообщаем об ошибке
}
```

---

## ✂️ ОБРЕЗКА ВИДЕО:

### Автоматическая обрезка:
- Получает длительность аудио (липсинка)
- Обрезает исходное видео до этой длины
- Использует FFmpeg для быстрой обрезки

### Код:
```typescript
// Получаем длительность аудио
const audioDuration = await getMediaDuration(audioUrl)

// Скачиваем исходное видео
await downloadFile(videoUrl, originalVideoPath)

// Обрезаем видео до длительности аудио
await trimVideo(originalVideoPath, trimmedVideoPath, audioDuration)
```

### Функции:
- `getMediaDuration(url)` - получает длительность медиафайла
- `trimVideo(input, output, duration)` - обрезает видео до нужной длины

---

## 🧪 ТЕСТОВЫЕ ДАННЫЕ:

```typescript
const TEST_ASSETS = {
  USER_VIDEO: 'https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4',
  TEST_AUDIO: 'https://www2.cs.uic.edu/~i101/SoundFiles/StarWars3.wav',
  LIPSYNC_RESULT: 'https://storage.googleapis.com/falserverless/example_outputs/veed-fabric-lipsync.mp4',
}
```

---

## 📝 СООБЩЕНИЯ ПОЛЬЗОВАТЕЛЮ:

### При запуске:
```
🧪 ТЕСТОВЫЙ РЕЖИМ - Бесплатно!
🎬 Начинаю создание lip-sync видео...
⏳ Тестирование монтажа...
```

### При успехе:
```
✅ Lip-sync видео готово!
🎬 Ваш lip-sync готов!
🧪 Тестовый режим
✨ Приятного просмотра!
```

### При ошибке:
```
❌ Произошла ошибка при создании lip-sync.
🧪 Тестовый режим - деньги не списывались.
```

---

## 🔍 ЧТО ТЕСТИРУЕТСЯ:

### ✅ Работает в тестовом режиме:
1. **Загрузка видео** - обрабатывает реальное видео от пользователя
2. **Ввод текста** - принимает текст/голосовое
3. **Генерация TTS** - создает реальное аудио (ЕСЛИ ЕСТЬ ТЕКСТ)
4. **Обрезка видео** - использует FFmpeg для обрезки
5. **Получение длительности** - использует ffprobe
6. **Workflow** - полный процесс без денег

### ⚠️ НЕ тестируется (используются заглушки):
1. **Lip-sync генерация** - использует тестовые данные
2. **Реальные AI модели** - не вызываются
3. **Upload в хранилище** - не выполняется

---

## 🎯 ДЛЯ ЧЕГО НУЖЕН:

### Тестирование монтажа:
- ✅ Проверить что FFmpeg работает
- ✅ Проверить что обрезка видео работает
- ✅ Проверить что длительность определяется
- ✅ Проверить что workflow не падает

### Отладка:
- ✅ Посмотреть логи в консоли
- ✅ Проверить временные файлы
- ✅ Протестировать без расходов

### Разработка:
- ✅ Быстро проверить изменения
- ✅ Не тратить деньги на тесты
- ✅ Тестировать в CI/CD

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ:

### 1. Локальная разработка:
```bash
export LIPSYNC_TEST_MODE=true
npm run dev
```

### 2. Тестирование:
```bash
npm test
# Автоматически включает тестовый режим
```

### 3. Ручное тестирование:
```bash
# В .env файле:
LIPSYNC_TEST_MODE=true

# Или перед запуском:
export LIPSYNC_TEST_MODE=true
npm start
```

---

## 📊 ПРИМЕР ЛОГОВ:

### Успешный запуск:
```
🧪 [SIMPLE LIPSYNC] ТЕСТОВЫЙ РЕЖИМ
📏 [SIMPLE LIPSYNC] Длительность аудио: 5.2 сек
✂️ [SIMPLE LIPSYNC] Обрезка видео до 5.2 сек
✅ [SIMPLE LIPSYNC] Видео обрезано
🎉 [SIMPLE LIPSYNC] Workflow завершен
```

### С ошибкой:
```
⚠️ [SIMPLE LIPSYNC] Не удалось обрезать видео
❌ [SIMPLE LIPSYNC] Ошибка генерации
🧪 Тестовый режим - деньги не списывались
```

---

## 🎊 ЗАКЛЮЧЕНИЕ:

**ТЕСТОВЫЙ РЕЖИМ ГОТОВ!**

### ✅ Что работает:
1. **Бесплатное тестирование** - не списывает деньги
2. **Обрезка видео** - по длине липсинка
3. **Реальный TTS** - если есть текст
4. **Полный workflow** - для тестирования логики

### ✅ Когда использовать:
- Локальная разработка
- Тестирование изменений
- Отладка монтажа
- CI/CD тесты

### ✅ Когда НЕ использовать:
- Реальные пользователи (только production)
- Тестирование AI моделей
- Измерение времени генерации

---

**🧪 Включайте тестовый режим и тестируйте монтаж БЕСПЛАТНО!** ✨
