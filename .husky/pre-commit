#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Проверка TypeScript ошибок..."
npm run type-check
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ Найдены ошибки TypeScript! Исправьте их перед коммитом."
  exit 1
fi

echo "✅ TypeScript проверка пройдена успешно!"

# Запускаем тесты
npm run test 