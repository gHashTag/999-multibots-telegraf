#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🎨 Форматирование кода с помощью Prettier..."
npm run format

echo "🔍 Проверка TypeScript ошибок..."
npm run type-check
npm run lint

if [ $? -ne 0 ]; then
  echo "❌ Найдены ошибки! Исправьте их перед коммитом."
  exit 1
fi

echo "✅ Проверки пройдены успешно!"

# Запускаем тесты
npm run test 