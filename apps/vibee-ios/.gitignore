# Артефакты сборки: воссоздаются из исходников одной командой.
build/
build2/
DerivedData/
# Проект генерируется из project.yml через xcodegen — держать оба значит
# получать конфликты в XML при каждой правке списка файлов.
Vibee.xcodeproj/
