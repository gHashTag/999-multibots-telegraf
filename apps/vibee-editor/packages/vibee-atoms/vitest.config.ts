import { defineConfig } from 'vitest/config'

/**
 * Свой конфиг, а не наследование корневого.
 *
 * Без этого файла vitest поднимался до корня репозитория, находил там
 * `vitest.config.ts` и пытался загрузить его `setupFiles: ['./vitest.setup.ts']`
 * — путь, разрешаемый относительно ТЕКУЩЕГО каталога, а не каталога конфига.
 * Из пакета такого файла нет, и все три файла тестов падали на загрузке, не
 * выполнив ни одного assert: «Test Files 3 failed, Tests no tests».
 *
 * Пакету корневой setup и не нужен: он подставляет фиктивные ключи провайдеров
 * (FAL и прочие) для сервисов бота. Здесь нет ни сети, ни провайдеров — только
 * атомы jotai.
 */
export default defineConfig({
  test: {
    globals: true,
    // Атомы работают с хранилищем и DOM-событиями: в среде `node` их нет.
    environment: 'jsdom',
  },
})
