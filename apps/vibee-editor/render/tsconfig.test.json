{
  "//": "Тесты рендер-сервера НЕ входят в основную программу: include там тянет src/** и render-server.ts, а тестовые файлы не импортирует никто. Значит `tsc --noEmit` с его нулём ошибок про них не говорит ничего. Этот конфиг — единственное место, где они вообще проверяются.",
  "extends": "./tsconfig.json",
  "include": ["*.test.ts", "src/**/*.test.ts", "src/**/*", "render-server.ts"]
}
