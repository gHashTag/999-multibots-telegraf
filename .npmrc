# Modern npm configuration alternatives
# Replaced deprecated options with current standards

# Handle peer dependencies with legacy resolution for compatibility
legacy-peer-deps=true

# Suppress funding messages during install (optional)
fund=false

# Set audit level for security scanning
audit-level=moderate

# Enable package-lock.json auto-update
package-lock=true
