# Очередь работ

Живой список задач, поставленных владельцем 06.09.2026. Файл существует
потому, что список задач в сессии агента живёт ровно до конца сессии, а
работа — дольше. Здесь он переживает перезапуск.

Порядок — тот, в котором задачи брались. Сделанное не удаляется: по нему
видно, что уже закрыто и чем именно.

---

## Закрыто

### 1. Бот не отвечал — @t27ai_bot не получал ни одного сообщения

**PR #2067.** Четыре разумных решения сложились в глухоту: рендер каждые 10
минут ставил боту вебхук; ставил с `allowed_updates`, где `successful_payment`
— не тип апдейта, а поле внутри `message`, и Telegram молча его выбрасывал;
пока вебхук стоит, `getUpdates` отвечает 409; сервис бота, видя вебхук, опрос
для этого бота не запускал.

Итог: `message` не доходил никуда. Ни `/start`, ни меню, ни оплата клуба.
Продавать бот тоже не мог — маршрут вебхука закрыт общим гвардом, Telegram
приходил без ключа и получал 401, поэтому `pre_checkout` никто не
подтверждал. Ноль транзакций за всё время — следствие, а не везение.

Приём апдейтов теперь один на все одиннадцать ботов — опрос.

### 2. Четыре денежные дыры, открытые или разбуженные первой правкой

**PR #2068.** Найдены состязательным ревью моего же PR:

- **Печать токенов.** `/api/stars/credit` закрывался только общим гвардом, а
  тот пускает по подписи мини-аппа — она есть у каждого пользователя. Сумма и
  получатель брались из тела. Любой мог начислить себе сколько угодно.
- **Двойное зачисление** на первой же реальной продаже: `/api/tokens/verify`
  и путь бота запирались на разных ключах. Пакет на 10 токенов дал бы 20.
- **Пустой `chargeId`** отключал защиту от повтора.
- **Сцена съела бы оплату:** обработчики стояли после `stage.middleware()`, а
  под опросом оплата приезжает внутри `message`; шаг сцены, ждущий текста,
  вернул бы управление молча — деньги списаны, токенов нет, в журнале пусто.

### 3. Фото не грузилось агенту в чате

**PR #2069.** Две причины.

- Запрос уходил **без удостоверения вообще**: `authHeaders` знала два способа
  из трёх, а ключ агента дописывали снаружи десять компонентов. Загрузка не
  дописала.
- **Файл с русским именем не грузился никогда.** Значение HTTP-заголовка
  обязано быть Latin-1, `Headers.set` на «фото.jpg» бросает исключение до
  отправки — в журнале сервера такого отказа не видно в принципе.

Отказ теперь называет причину, а не «загрузка не удалась».

### 4. Первый экран продавал несуществующие тарифы

**PR #2070.** Системная подсказка ИИ-ответчика содержала «Free (3/день),
Basic (299₽), Pro (699₽), Studio (1999₽)». Модель это не выдумывала.
Настоящая модель: тарифов нет, токены перепродаются с наценкой ×2, заработок
— на рилсах и контент-плане.

Цены в подсказке не называются намеренно: они считаются из себестоимости и
наценки и меняются вместе с прайсами. Разбор прошлых оплат по планам не
тронут — убрано предложение, а не история.

---

## В работе и в очереди

### 5. Убрать нерабочие кнопки бота

Владелец: «убери кнопки из бота так как они не работают». На экране:
Фото, Видео, Аудио, Аватары, Маркетплейс, Пополнить, Профиль, Видеоредактор.

**Не удалять на глаз.** Сначала установить, какие действительно мертвы:
каждая кнопка ведёт в `mode`, каждый `mode` — в сцену, и надо проверить, что
сцена существует и зарегистрирована.

### 6. Агент умеет всё, что сцены, — без сцен

Владелец: «агент в чате должен уметь все функции бота без сцен, сохранять
скилы и помогать юзеру общаться в телеграм». Сопоставить реестр инструментов
агента с каталогом сцен, найти пробелы, закрывать по одному.

Связано с 5: если агент умеет всё, кнопки не нужны.

### 7. Чек на любое количество звёзд

Сейчас `POST /api/tokens/invoice` принимает фиксированный `pack`. Нужен счёт
на произвольное число токенов и инструмент агента, чтобы он выставлял его сам.
Наценка ×2 остаётся единственным источником цены, зачисление — на одном замке
(`star_payments.charge_id`).

### 8. Синхронизация чата бота и чата мини-аппа

Один разговор, две поверхности: написанное в боте видно в мини-аппе и
наоборот, с общей историей и тем же агентом. Загрузка файлов работает с обеих
сторон.

### 9. CRM для продвижения владельца

Настроить аккаунт, изучить контакты, предлагать горячие лиды на услуги,
сделать агента проактивным советчиком.

### 10. Доступ агента к переписке владельца через GramJS

**Установлено 06.09.2026:**

- пакет `telegram` (GramJS) уже в зависимостях **и бота, и рендера**;
- рабочий клиент со `StringSession` уже есть —
  `scripts/stars-withdrawal/gramjs-client.ts`;
- `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_SESSION_STRING` заданы на
  сервисе `999-multibots-telegraf`;
- **но сессия мертва**: `client.checkAuthorization()` вернул `false`.

Без владельца делается всё, кроме входа: клиент, инструменты агента
(контакты, диалоги, поиск по переписке), оценка лидов, хранение, доступ.

**Один шаг требует владельца лично:** разовый вход по телефону и коду (и
паролю 2FA, если включён), чтобы получить свежую строку сессии и положить её
в Railway. Коды авторизации агент не вводит — это не осторожность ради
осторожности, а граница вокруг учётных данных человека.

---

## Вход в приложение (луп 07.09.2026)

### 11. Дверь входа запиралась одним запросом — и чинилась в три захода

**PR #2095.** Промах кода в `pair/claim` начислял попытку ЖИВЫМ КОДАМ ВСЕХ
людей платформы (`UPDATE … WHERE consumed_at IS NULL`). Пять запросов в
секунду с любого адреса делали вход по коду невозможным для всех, навсегда,
ценой копеек. Начисление снято, вместо него тормоз; код удлинён с шести цифр
до восьми (и `верх` считается от `DIGITS`, иначе цифры были бы косметикой).

**PR #2096.** Тот же тормоз, применённый к остальным дверям, оказался хуже
болезни: ключом служил `socket.remoteAddress`, а Railway обрывает TLS у себя —
это адрес ПРОКСИ, один на всех. Десять попыток в минуту на такой ключ = запрет
входа всей платформе. Теперь ключей два: узкий лимит на клиента (левый
`X-Forwarded-For`, подделываемый) и широкий потолок на соединение
(неподделываемый, 1000/мин). Там же снято ложное обещание `dkt`: комментарий
уверял, что поле привязывает токен к устройству, а `verifyAppSession` его не
читает и ни один клиент не шлёт `device_pubkey`.

**PR #2097.** Откат «нет заголовка — считаем по соединению» возвращал ту же
аварию в запасной ветке. Неразличимый клиент теперь не получает узкого лимита
вовсе — только потолок.

**PR #2098.** Измерено снаружи: заголовок доходит, ключ клиента живой
(400 ×6, дальше 429). `tri throttle` повторяет замер.

Проверять: `tri entry`, `tri throttle`.

### 12. Две вкладки читались как кража токена — и выбрасывали человека

**PR #2100, #2101.** `refreshInFlight` в мини-аппе — переменная модуля, одна НА
ВКЛАДКУ, а refresh лежит в общем `localStorage`, и таймер заводится от общего
срока. Две открытые вкладки приходили обновляться с разницей в миллисекунды;
сервер читал второй приход как кражу и гасил ВСЮ семью. Человека выбрасывало из
приложения, вернуться он мог только через восьмизначный код. Ежедневно, без
злоумышленника.

Комментарий в `rotateRefreshToken` утверждал, что честный повтор и кражу
«снаружи различить невозможно». Невозможно по факту повтора — но не по
ПРОМЕЖУТКУ: вкладки сталкиваются за миллисекунды, вор приходит между
обновлениями клиента, то есть через минуты. Повтор в пределах
`REUSE_GRACE_SECONDS = 10` — теперь `raced`: отказ 409 без гашения, клиент
перечитывает хранилище.

Вторая половина: проигрыш в `consumeAndInsert` — гонка ПО ПОСТРОЕНИЮ, и гашение
семьи там было прямой ошибкой. Тест с именем «two refreshes race» до этой ветки
не доходил вовсе.

iOS чинился отдельно (#2101): там защиты от одновременных обновлений нет вовсе,
а на любой отказ звался `forget()`.

Заодно: «Выйти» с живым токеном гасил ОДНУ сессию, а с протухшим — всю семью,
то есть обычный путь шёл по слабой ветке. Сегодня совпадало случайно (семья и
сессия один к одному); теперь семья определяется всегда.

Проверять: `tri session`.

### 13. Суточная initData плодила двухмесячные семьи токенов

**PR (луп 07.09).** Подпись Login Widget одноразовая — `app_widget_assertions`
не даёт предъявить её дважды. У initData такой защиты не было ВОВСЕ, а живёт
она сутки: одну строку можно предъявлять сколько угодно раз, и каждый раз
рождалась новая НЕЗАВИСИМАЯ семья на 60 дней. «Выйти» гасило одну из них,
остальные продолжали жить.

Запрет повтора был бы ошибкой (initData выдаётся на запуск, входов внутри
запуска бывает больше одного), поэтому сделана идемпотентность: `один запуск —
одна семья`, таблица `app_launch_families`. После отзыва сессии та же строка
заводит новую семью и переписывает отображение — иначе «Выйти» означало бы
«выйти навсегда до конца суток».

Заодно: убрана таблица `oidc_auth_requests` — заводилась при каждом запуске и
не использовалась нигде; схема описывала четвёртую дверь входа, которой нет.

Проверять: `tri schema`.

### 14. В приложение на iOS нельзя было войти — регрессия от нашей же правки

**PR (луп 07.09).** Длина кода входа поднялась с шести цифр до восьми (#2095).
Правка серверная, с тестом и мутацией. Клиентов никто не посмотрел:

- `SignIn.swift` обрезал ввод на шестой цифре и включал кнопку только при ровно
  шести — восьмизначный код НЕВОЗМОЖНО было набрать;
- `Identity.claimPairing` отказывал до запроса: «Нужны шесть цифр»;
- мини-апп писал «Введите эти 6 цифр», показывая восемь, и резал их как
  «123 45678»;
- кнопка обещала «Показать 6-значный код»;
- английский текст бота обещал «six-digit code» — притом что русский рядом
  длину не называл, и комментарий над ним объяснял, почему.

Вход на iOS не работал четыре итерации, пока шла «работа над входом».

Вылечено не сверкой чисел: iOS принимает диапазон и отдаёт решение серверу,
мини-апп берёт число из самого кода, тексты его не называют. Семь сторожей,
пять мутаций.

Проверять: `tri pairing`.

### 15. Вход на iOS проверен ЖИВЬЁМ — и запуск нашёл то, чего не видели тесты

**PR (луп 07.09).** Прошлый цикл починил длину кода по тексту и типам, но код
руками никто не набирал. Набрал:

- восемь цифр вводятся, кнопка «Войти» включается, запрос уходит на живой
  сервер и возвращает его настоящий отказ («код не найден») — путь доказан
  целиком;
- запуск нашёл ТРЕТЬЮ копию длины в том же файле: `TextField("000000", …)` —
  подсказка из шести нулей. Ни одна из семи текстовых проверок её не видела;
- замена подсказки на «код из Telegram» дала обрезанное «код из Tele…»: поле
  свёрстано под цифры с разрядкой. Итог — короткое «код».

Заодно снята копия срока жизни кода: «Код действует 2 минуты» в мини-аппе была
копией `PAIRING.TTL_SECONDS` — ровно как «6 цифр» была копией `DIGITS`.

И вылечен плавающий тест `autopilot-poster-record`: ожидание сдавалось через 5
секунд при бюджете хука в 20.

Проверять: `tri pairing`, и раз в цикл — живой запуск в симуляторе.

### 16. «Профиль» был тупиком с ложной причиной — и мини-апп нельзя было смотреть

**PR (луп 07.09).** Живой прогон мини-аппа (впервые за цикл) показал: вкладка
«Профиль» отвечает «подпись Telegram сюда не пришла» при живой подписи. Причина
другая — профиль открывается по `/:username`, а имени нет ни в launch-данных,
ни в памяти. У множества аккаунтов @имени нет вовсе, и Telegram кладёт его в
launch-данные не всегда.

Вылечено запросом `GET /api/users/id/:telegram_id`: сервер имя знает. Для
случая «имени нет нигде» — отдельный экран с настоящей причиной.

Чтобы это увидеть, заведена подставка `?mock-telegram=<id>` — только для
разработки. Раньше экраны входа нельзя было посмотреть НИКАК: вне Telegram
мини-апп их не показывает, и правки шли вслепую (что уже дважды стоило входа
на iOS).

Подставка вырезается из любой сборки плагином Vite; проверка собирает бандл и
ищет метку — она же трижды поймала более слабые версии защиты.

### 17. «Выйти» внутри Telegram оставляла экран в противоречии

**PR (луп 07.09).** Живой прогон: нажатие чистило память, автологин возвращал
человека за доли секунды. Шапка предлагала «Войти», хранилище уже содержало
пользователя, профиль отвечал «Пользователь не найден».

Кнопка убрана там, где выходить не из чего (внутри мини-аппа сессии браузера
нет вовсе), и оставлена в вебе, где вход был отдельным действием.

Заодно проверен живьём экран кода спаривания — то, что откладывалось три
итерации: `1234 5678` группируется по четыре, «Введите эти 8 цифр», aria-метка
по цифрам, влезает и на 390, и на 320 пикселях.

И записано обоснование срока 120 секунд: разбирал подъём до 300 и оставил как
есть — окно стоит ради подглядывания, а не ради перебора.

### 18. Мини-апп наконец берёт сессию (половина корня, и это сказано честно)

**PR (луп 07.09).** `/api/auth/telegram` не звал НИКТО: маршрут написан, покрыт
тестами, задеплоен — и не имел ни одного посетителя. Весь мини-апп работал по
заголовку подписи на каждом запросе, а вся работа вокруг сессий (обновление,
гонка вкладок, отзыв семьи, «один запуск — одна семья») касалась только веба.

Теперь мини-апп обменивает подпись запуска на сессию один раз за запуск и
предъявляет ОБА удостоверения. Отказ обмена не ломает ничего — работает ровно
как вчера.

**Чего это не даёт, и это записано в коде.** Личность внутри Telegram НЕ
становится отзываемой: `initData` живёт сутки, и по ней выпускается новая
сессия. Настоящий отзыв — только вместе с коротким окном приёма `initData`, и
делать это надо ПОСЛЕ того, как станет видно, что сессии выпускаются в проде.
Это следующая задача, а не сделанная.

### 19. Бот обещал «откроется окно с кодом», а открывалась вкладка «Шаблоны»

**PR (луп 07.09).** Команда `/app` пишет: «Нажмите кнопку — откроется окно с
кодом». Кнопка вела на `/profile`, профиль открывался на вкладке «Шаблоны», а
код входа живёт во вкладке «Агент» — о которой в сообщении ни слова. Человек,
пришедший ЗА КОДОМ по единственному рекламируемому пути, кода не видел.

Теперь `pair` ведёт на `/profile?tab=agent`, переход на `/:username` сохраняет
строку запроса, а вкладка читается из адреса со сверкой по списку.

Существующий тест назывался «вход в приложение ведёт на экран, где показан
код» и закреплял `pair: '/profile'` — то самое значение, при котором код не
виден. Переписан на проверку назначения.

Проверено живьём: `?tab=agent` доживает до профиля, экран кода виден сразу.

### 20. Первая дверь бота не была покрыта ничем + пять денежных сторожей ожили

**PR (луп 07.09).** `/start` — первая дверь для каждого человека — не имела ни
одного теста: были тесты на промо-ссылки и меню, но не на сам путь «пришёл
незнакомый, написал /start». Три развилки (нет в базе → регистрация; есть →
меню; не личный чат → ответ без сцены), каждая ломается молча, и цена ошибки
выше всего: человек не пишет в поддержку, он уходит.

Написан тест на НАСТОЯЩИЙ обработчик, семь проверок, четыре мутации.

Заодно вылечен весь класс падений по времени и восстановлены пять денежных
сторожей (см. «Долг» ниже).

### 21. Приём рублёвых денег проверялся по форме, а не по поведению

**PR (луп 07.09).** Уточнение к прежней записи очереди: маршрут импортируют
ПЯТЬ файлов, он не беззащитен. Но все они читают его ИСХОДНЫЙ ТЕКСТ, а 32
теста в `robokassaWebhook.test.ts` держат собственную копию логики. Обработчик
не ИСПОЛНЯЛ ни один тест.

Написан поведенческий: обработчик снимается с настоящего роутера (проверяется
и проводка), шесть проверок, четыре мутации. Главная — поддельная подпись не
начисляет и получает 400; рядом опорная — верная подпись начисляет, иначе
первая ничего не доказывает. Отдельно проверен СТАРЫЙ адрес
`/robokassa-result`: правки исторически шли во второй, и потерянная там
проверка подписи — бесплатные деньги.

Мимоходом: индекс денежных сторожей (`docs/money-ratchets.md`) поймал меня
самого — добавил сторожа, не обновив индекс. Обновлён, 104.

### 22. Удаление чужих файлов не проверялось исполнением

**PR (луп 07.09).** Уточнение к прежней записи очереди («без единого теста»):
тест был — `assets-identity-order.test.ts`. Но он ТЕКСТОВЫЙ и сам о себе
пишет: «проверяет порядок; НЕ проверяет, что функция личности верна».

`DELETE /api/assets` удаляет файлы людей, и вся защита от «удалить чужое» —
одно условие `AND telegram_id = $2`. Исполнить его было нечем:
`render-server.ts` не импортирует ни один тест, потому что импорт поднимает
сервер.

Условие вынесено в `src/assets/delete-own-photo.ts` — ровно защищающая часть,
без HTTP. Восемь проверок, четыре мутации: убрана сверка владельца (чужое
удаляется), убрана сверка типа, ноль строк засчитан как успех, владелец
склеен в текст запроса.

Подделка пула берёт условия ИЗ ЗАПРОСА, а не повторяет их: копия WHERE в
подделке зеленеет ровно тогда, когда условие из настоящего кода убрали.

### 23. Полный диск остановил цикл — защита встала перед едоком места

**PR (луп 07.09).** Том заполнился, и оболочка перестала запускать что-либо:
файл под вывод создаётся ДО команды, поэтому при ENOSPC не стартуют ни `rm`,
ни `df`, ни `tri disk`. Автономная работа из этого состояния не выходит.

`run.sh` теперь: отказывает при <2 ГБ свободного места внятным сообщением и
чистит `build2` ПЕРЕД сборкой. Чистилка `free-disk.sh` научена про `build2` —
раньше она знала только `/tmp/dd`, каталог `tri ios`, а ночные сборки шли
через `run.sh`.

**Поправка к отчёту прошлой итерации:** причиной я назвал `/tmp/dd` и свои
сборки. Замер на следующий день: `run.sh` пишет в `build2`, там было 94 МБ, а
Data-том занят на **197 ГБ** — это не мои данные. Заполнение диска остаётся
необъяснённым и требует владельца.

В брошенном worktree `.claude/worktrees/wf_8c91dcf2-6ef-1` (286 МБ) есть
НЕСОХРАНЁННЫЕ правки пяти тестовых файлов — не удалял.

## Долг, замеченный по пути

**~~Пять денежных сторожей не работают на чистом `main`.~~ ЗАКРЫТО 07.09.2026.**
Причина была не в тестах: `money-map.mjs` звал `process.exit(0)` сразу после
`console.log`. В терминале незаметно (stdout — TTY, запись синхронная), но
когда вывод читает другая программа, stdout — канал, запись асинхронна, и
выход обрубает хвост ровно на буфере канала (8192 байта). Отчёт весит 25 КБ.

Сейчас: 3728 тестов бота проходят, падающих НЕТ. Три файла не загружаются —
`create-*-excel-*.test.ts` и `metamuse-final-from-backup.test.ts`: это
генераторы отчётов, названные `.test.ts`, и пакет `exceljs` не установлен. Они
не запускались никогда. Решение владельца: перенести из `__tests__` или
поставить пакет.
