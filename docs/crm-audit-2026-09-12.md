# Аудит CRM («личный продавец») — repo999 @ main

Дата: 2026-09-12. Файлы не изменялись, git-состояние не трогалось.
Тесты: `npx vitest run src/__tests__/services/crmProactive.test.ts` — 26/26 ✓;
`apps/vibee-editor/render`: `npx vitest run src/agent` — 10 файлов, 78/78 ✓ (CRM-тесты лежат в корне render, не в `src/agent`);
дополнительно прогнаны `crm-* tg-proposals chat-memory zep-memory system-prompt-by-surface telegram-tools* telegram-session-honest sync-from-telegram-verify` — 18 файлов, 303/303 ✓.
Известный ранее падавший `system-prompt-by-surface › «sells tokens, not a club»` **сейчас проходит** — падение не воспроизведено.

Пути: `bot/` = `/home/user/workspace/repo999/src/...`, `render/` = `apps/vibee-editor/render/src/agent/...`.

---

## P0 — двойной контакт / деньги / утечка

### 1. `tg_send` не передаёт `lead` → подтверждённый ответ не записывается ни как касание, ни в память → человек остаётся «ждёт ответа» и получает второе письмо
- `render/telegram-tools.ts:736-742`
  ```ts
  return propose('send', args.chat, args.text,
    'Отправка ждёт подтверждения человека. …', ctx)   // 6-й аргумент lead не передан
  ```
- `render/tg-proposals.ts:798` `if (!p.lead || !ctx.pool) return` (mirrorSent) и `:953` `if (p.lead && ctx.pool) { … recordTouch(… kind: 'written') }`.
- Почему баг: `next=reply`/`next=talk` по правилам обхода (`bot/crmSweepScope.ts:25-26`) идут именно через `tg_send`. После нажатия «✅ Отправить» ни `written`-касание, ни исходящее сообщение в `crm_messages` не появляются. В `chat-memory.ts:498` `unanswered` считается по `crm_messages` → человек по-прежнему `next=reply`, `isDue` истинно, а `noteResolved` (`bot/crmProactive.ts:207`) сразу сбрасывает `lastPushAt=0` → следующий тик (≤30 мин) готовит **второй ответ тому же человеку**. Спасает только удачный ingest, который увидит наше исходящее в Telegram; при `stopped`/FLOOD_WAIT/протухшей сессии (см. п. 4) не спасает. Бот на своей стороне уже умеет выводить lead из числового target (`bot/telegramProposals.ts:68-75 cardLeadOf`), сервер — нет.
- Фикс (минимальный): в `propose()` (`telegram-tools.ts:183`) или в `execute()` перед `mirrorSent`: `const lead = p.lead ?? (/^\d{5,15}$/.test(p.target) ? p.target : undefined)`. Для `@username` — резолвить id один раз в `execute` (клиент уже подключён) и писать касание по нему.

---

## P1 — тихие сбои / неверный тенант

### 2. `whoPaid()` — платформенный запрос без фильтра владельца [вопрос: считается ли CRM однопользовательской]
- `render/crm-tools.ts:206-211`
  ```ts
  const строки = await запрос(
    'payments_v2?select=telegram_id&status=eq.COMPLETED&type=eq.MONEY_INCOME')
  ```
  используется в `crm-memory-tools.ts:285`, `crm-summary-tool.ts:243` → `paid`, `stageOf(...)='client'`, сегмент `winback`, счётчик `paid` в сводке. Аналогично `balanceOf(pool, lead.id)` (`crm-memory-tools.ts:216`) — баланс без привязки к боту владельца.
- Почему баг: `leadCandidates`/`touchedSince`/`reachable` фильтруют по `owner_id`/`bot_name`, а признак «клиент» берётся из платежей **всей платформы**: человек, заплативший другому боту, у этого владельца станет `client`/`winback`, обход пропустит прогрев («keeps clients out of warming», `chat-memory.ts:450`). Пока `requireOwner` пускает одного владельца — это неточность данных; при обещанном «CRM for clients» (`telegram-tools.ts:80-97`) — утечка факта оплаты между владельцами.
- Фикс: `payments_v2?…&bot_name=in.(<visibleScope>)` (как в `люди()` рядом, `crm-tools.ts:213+`), либо пересечение с `users.bot_name` владельца.

### 3. Флаг `running` может остаться `true` навсегда → каждый обход отвечает `busy`, и это логируется как info
- `bot/crmProactive.ts:105` `let running = false`; `:231-232` `if (running) return { did:'busy' … }; running = true`.
- `bot/trinityAgent.ts:138-141` (`readConversation`) и `:311-318` (`recordTurns`) — `fetch` без `AbortController`/таймаута; `readConversation` вызывается **до** запуска 180-секундного таймера (`:174` vs `:182-183`).
- Почему баг: подвисший `GET /api/agent/history` (Railway cold-start, обрыв без FIN) держит `sweepOnce` в `finally`-не-достигнуто состоянии; все последующие тики — `busy`. `reportSweepOutcome` (`:190-197`) считает `busy` не-ошибкой, `noteSweepToHive` его пропускает (`hiveNote.ts:167`) → продавец молчит часами, владелец не узнаёт.
- Фикс: таймаут 15–20 с на `readConversation`/`recordTurns`; в `sweepOnce` фиксировать `runningSince` и сбрасывать `running` при `now - runningSince > 10 мин` с ошибкой в лог; `busy` ≥3 раз подряд — эскалировать как failed.

### 4. Ингест на отозванной MTProto-сессии падает громко на сервере, но бот его проглатывает — обход «работает» на протухшей памяти неограниченно долго
- `bot/crmProactive.ts:239-247`
  ```ts
  try { if (opts.ingest !== false) await deps.ingest(ownerId) }
  catch (e) { logger.warn('[crm-proactive] ingest failed, sweeping on stale memory', …) }
  ```
- `render/telegram-tools.ts:372-400` — `checkAuthorization()===false` → `throw new Error('Сессия Telegram истекла или отозвана…')` (корректно). Но `SweepOutcome` не несёт признака ingest-ошибки; `reportSweepOutcome`/`noteSweepToHive` видят `idle`/`card`. `render/crm-ingest-on-connect.ts:100-105` — тоже только `console.warn`.
- Почему баг: «сессия истекла» — постоянное состояние, а не FLOOD_WAIT. Каждые 30 мин пишется warn, карточки готовятся по данным на момент смерти сессии (см. п. 1 — исходящие не видны), при нажатии «Отправить» владелец получит «❌ Не отправлено: Сессия истекла» — единственный момент, когда он узнаёт.
- Фикс: пробрасывать `ingestError` в outcome; отличать по тексту/классу «сессия истекла/не подключён» и один раз в сутки слать владельцу сообщение с инструкцией переподключения; ≥3 ingest-ошибок подряд → `failed`.

### 5. Сервер сохраняет полный бриф обхода и «ответ без инструментов» в разговор владельца — вопреки замыслу бота; транскрипт из 40 реплик вымывается за ~10 обходов, ломается дедуп дневного плана
- `bot/crmProactive.ts:306-313`
  ```ts
  // Only a turn that looked is worth remembering: a no-tools answer left
  // in the transcript teaches the next sweep to answer the same way.
  if (toolsOf(answer).length || answer.proposal) { void deps.record?.(ownerId, [{role:'user', content: opts.label ?? '[проактивный обход продавца]'}, …]) }
  ```
- `render/routes.ts:469-478` — `POST /api/agent/chat` **всегда** пишет последнюю user-реплику (весь `SWEEP_PROMPT + leadsNote`, ~1 КБ) и `:494-499` ответ ассистента (включая `[[Подпись|no_one_available]]`).
- Почему баг: (а) защита «не учить модель плохому ответу» не работает — сервер уже записал и бриф, и маркер-ответ; (б) за обход в `agent_messages` ложится 4–6 реплик, `ГЛУБИНА_ИСТОРИИ = 40` (`trinityAgent.ts:35`) → через ~10 обходов (5 ч) контекст владельца состоит только из брифов; (в) `alreadyPlannedToday` (`crmProactive.ts:853-861`) ищет маркер плана в тех же 40 репликах и при `catch → return false` — после редеплоя во второй половине окна план уходит **второй раз** за день.
- Фикс: для `tools_only`/обхода слать флаг `ephemeral: true`, по которому `routes.ts` не пишет реплики (запись остаётся за ботом, как задумано); или хранить `planSentOn` вне транскрипта (таблица/KV).

### 6. `next=offer/deliver` не остывает после `written`-касания → человеку, которому счёт уже отправлен, обход предлагает его снова
- `render/chat-memory.ts:565-571`
  ```ts
  else if (signals.includes('service') && (signals.includes('price') || signals.includes('buy'))) next = 'deliver'
  else if (signals.includes('price') || signals.includes('buy')) next = 'offer'
  ```
  `:541-544` — `written ≤2 дн.` даёт только `score -= 1`; кулдаун 2 дня есть лишь у `talk` (`:575`). Сигналы берутся из входящих за 90 дней (`:468-472`).
- Почему баг: после подтверждённого `crm_offer` (касание `written`, наше исходящее зеркалится) `unanswered=false`, но слово «цена» в истории остаётся → `next=offer`, `isDue` истинно (`crmProactive.ts:137`), правила брифа (`crmSweepScope.ts:28`) велят `crm_offer`. Через `HOLD 2 ч` карточка на того же человека повторяется до тех пор, пока он не ответит; модель — единственный барьер.
- Фикс: в `leadCandidates`: `if (touch?.kind === 'written' && touchDays < 3 && !unanswered) next = 'wait'` (симметрично `refused`/`later`), и/или в `sweepOnce` исключать из `looked` тех, у кого `lastTouch.kind==='written'` свежее `holdMs`.

---

## P2 — мелочи, но с последствиями

### 7. Zep: пользователь ключуется только по lead, потерянная партия не докатывается никогда
- `render/zep-memory.ts:83` `zepUserId = (lead) => \`tg-${lead}\``; `:244` `if (!r.ok) break` в `zepAddMessages`; `crm-mirror.ts:40-46` — только `console.warn`.
- Postgres уже получил `fresh` (`crm-mirror.ts:36`), Zep — нет; повторный ingest их не вернёт (идемпотентность по `crm_messages`). Зеркало расходится навсегда, `zep_mirrored` в отчёте молча меньше `fresh`. При нескольких владельцах один user-graph `tg-<lead>` общий → факты из переписки одного владельца попадут в `zepContext` другого [вопрос].
- Фикс: очередь недокатанных (owner, lead, msg_id) с ретраем; user_id вида `tg-${owner}-${lead}`.

### 8. Захардкоженный владелец `'144022504'` остаётся единственным «замком» для всех CRM-инструментов — вопреки собственному комментарию о переезде на `requireIdentity`
- `render/telegram-tools.ts:65` `const OWNER_TELEGRAM_ID = (process.env.OWNER_TELEGRAM_ID || '144022504').trim()`; `:72-78 requireOwner` сравнивает `ctx.telegramId` именно с ним.
- Используется: `crm-memory-tools.ts:101,200,271`, `crm-summary-tool.ts:236`, `crm-offer-tool.ts:104,186,295`, `crm-playbook.ts:21`, `chat.ts:189-190` (`dmHistoryBlock`: история берётся у платформенного владельца, а не у владельца бота, через который пишет клиент).
- Комментарий `telegram-tools.ts:80-97` заявляет «CRM for clients»: любой подключивший аккаунт клиент получит от всех `crm_*` «Telegram-аккаунт принадлежит владельцу». Не утечка, а мёртвый путь + один id в 9 местах.
- Фикс: единый `requireOwner(ctx)` → `requireIdentity(ctx)` + `owner = ctx.telegramId`; `dmHistoryBlock` — брать владельца по `bot_name`.

### 9. Отправка медиа не зеркалится: `sent` для `sendFile` не присваивается
- `render/tg-proposals.ts:914-918`
  ```ts
  let sent: unknown = null
  if (p.media) await sendFileWithAddressBook(c, p.target, p.media, p.what)
  else sent = await sendWithAddressBook(c, p.target, p.what ?? '')
  ```
  `:952` `if (sent !== null) await mirrorSent(...)`. `crm_deliver_photo` записывает `bought`, но исходящее не попадает в `crm_messages` → `unanswered`/`last_outbound` неверны, `waiting_for_reply` в сводке завышен.
- Фикс: `sendFileWithAddressBook` возвращает сообщение; `sent = await …`.

### 10. Ожидание занятого обхода 5 мин короче возможной длительности обхода → scoped-обход снимается «слишком долго»
- `bot/crmProactive.ts:587-588` `BUSY_RETRY_MS = 30_000; BUSY_RETRY_MAX = 10`.
- Обычный обход = ingest до 170 с (`:103`) + до двух вызовов модели по 180 с (`trinityAgent.ts:32`, повтор `:300-304`) ≈ 8,8 мин. Сценарий: `/sweep hot` во время таймерного обхода → 10×busy → `dropScope`.
- Фикс: `BUSY_RETRY_MAX = 20` или считать бюджет от `INGEST_TIMEOUT_MS + 2*ЖДАТЬ_МС`.

### 11. Прерывание ingest на стороне бота не останавливает обход диалогов на сервере; частичный ingest возвращается как успех
- `bot/crmProactive.ts:431` `setTimeout(() => ac.abort(), INGEST_TIMEOUT_MS)` — сервер (`crm-memory-tools.ts:100-165`) не получает сигнала и продолжает `getDialogs/getMessages` (без таймаутов, `:120,:136`) → через 30 мин стартует второй ingest поверх первого (FLOOD_WAIT-риск) [вопрос: длительность реального ingest].
- `crm-memory-tools.ts:136-141` — ошибка `getMessages` → `report.stopped=…; break`, результат — обычный успех; бот `stopped` не читает. Описание инструмента (`:90`) «максимум 200» при `DIALOGS_MAX=2000` (`:48`).
- Фикс: mutex ingest per-owner на сервере (`inFlight` map) и `stopped` → warn в боте.

### 12. Таймауты в вызовах бота → render: таймер снимается до чтения тела; `call`/`mirrorDm` без таймаута; частичный ответ с ошибкой глотается
- `bot/modelSwitch.ts:105-107` `finally { clearTimeout(timer) }` затем `await r.json()` — зависание при чтении тела не ограничено. `bot/modelSwitch.ts:34 call`, `:352 mirrorDm` — без AbortController.
- `bot/trinityAgent.ts:267` `if (!собрано && ошибка) throw` — событие `ошибка` при непустом тексте не поднимается: обход получает «ответ», хотя модель оборвалась.
- Фикс: `clearTimeout` после `r.json()`; общий `fetchWithTimeout`; `ошибка` → `answer.error` и в `why`.

### 13. Разбор фильтров `/sweep`: `days=7` молча превращается в `days<=7`; строки с плохим id выбрасываются без следа
- `bot/crmSweepScope.ts:163-165`
  ```ts
  const days = /^days(<=|>=|=)(\d{1,4})$/.exec(low)
  if (days) { const op = days[1] === '>=' ? '>=' : '<=' …
  ```
- `bot/crmSweepScope.ts:344` (`itemsFromRows`) — `ID_RE` не прошёл → человек пропущен без записи в label/лог.
- Фикс: `=` → `op: '='` или отвергать с подсказкой; счётчик пропущенных в ответе `/sweep`.

### 14. Маркеры кнопок: парсер удаляет только `[[…|act:[a-z_]]]`, всё остальное уходит клиенту как текст; обход получает инструкцию про маркеры, хотя они там запрещены
- `bot/src/navigation/helpers/actionButtons.ts:141` `/\[\[([^\]|]{1,40})\|act:([a-z_]{1,24})\]\]/g` — `[[Подпись|tg_send]]`, `[[Оплатить|act:Topup]]`, `[[Пополнить|act:topup2]]` не матчатся и **остаются в тексте**, хотя промпт обещает «будут молча выброшены» (`render/chat.ts:100-101`).
- `bot/trinityAgent.ts:197` `surface: opts.surface ?? 'bot'` → `render/chat.ts:391` добавляет `BUTTON_MARKERS` и обходу — первопричина `[[Подпись|no_one_available]]` из `crmProactive.ts:108-111`; лечится вторым заходом, а не устранением.
- Фикс: второй проход `text.replace(/\[\[[^\]]{1,60}\]\]/g,'')` после разбора; для `tools_only` не добавлять `BUTTON_MARKERS`.

### 15. LOOK FIRST смотрит только 5 верхних по score → «тихо», когда должник стоит шестым [вопрос: частота]
- `bot/crmProactive.ts:134` `LOOK_LIMIT = 5`; `:278-285` `if (looked && !looked.some(isDue)) return { did:'idle' … }`; сортировка `chat-memory.ts:611-615` по `score`, а не по `next`.
- `wait`-строка может набрать больше очков, чем due-строка: `bought +2, inbound≥5 +1, days≤30 +1 = 4` против `talk`-кандидата с `3`. Пять таких «клиентов» сверху → обход честно пишет «все next=wait», хотя ждущие есть.
- Фикс: `fetchLeadRows(owner, 20)` и `looked.filter(isDue).slice(0, LOOK_LIMIT)` для брифа.

---

## Не подтверждено / не найдено
- Часовой пояс дневного плана (`bot/crmPlan.ts`): `localDayKey`/`localHour` через `Intl.DateTimeFormat`, `% 24` обрабатывает «24», окно `[hour, hour+11)` — ошибок не найдено.
- Двойное нажатие карточки: `claim()` забирает черновик до `execute` (`tg-proposals.ts:672`), бот при обрыве связи честно отвечает «не знаю, ушло ли» и не повторяет (`telegramProposals.ts:177-197`) — корректно.
- Все SQL в `chat-memory.ts`, `crm-touches`, `tg-connect.ts` фильтруют по `owner_id`/`telegram_id` — утечек между владельцами в Postgres не найдено.
- GramJS-клиенты закрываются в `finally` во всех проверенных местах (`crm-memory-tools.ts:165`, `tg-proposals.ts:1016`, `crm-offer-tool.ts:194`).

---

## Что исправлено в PR `fix/crm-anomalies` (12.09.2026)

| № | Пункт | Правка |
|---|---|---|
| 1 | P0 lead в `tg_send` | `leadOfTarget(args.chat)` → числовой chat id идёт в черновик; касание `written` и зеркало пишутся |
| 2 | P1 `whoPaid()` без владельца | `платившие(область)` фильтрует `payments_v2.bot_name` по ботам владельца во всех 6 вызовах |
| 3 | P1 вечный `running` | `STUCK_SWEEP_MS` = 10 мин: флаг старше — освобождается с ошибкой в лог; `AbortSignal.timeout(20 с)` на `readConversation`/`recordTurns` |
| 4 | P1 отозванная сессия | `INGEST_FAILURES_BEFORE_FAILED` = 3: с третьего сбоя подряд обход = `failed` с подсказкой «переподключи»; идёт владельцу и в журнал улья |
| 5 | P1 бриф в транскрипте | `tools_only` → сервер не пишет реплики (`ephemeral`), запись остаётся за ботом |
| 6 | P1 `offer/deliver` после `written` | касание `written` < 3 дн. и нет неотвеченного → `wait`; `reply` не паркуется |
| 9 | P2 медиа не зеркалится | `sendFileWithAddressBook` возвращает сообщение → `mirrorSent` |
| 10 | P2 BUSY_RETRY | `BUSY_RETRY_MAX` 10 → 20 |
| 14 | P2 маркеры | второй проход `[[…]]` в `parseAgentButtons`; `tools_only` не получает `BUTTON_MARKERS` |

Не тронуто (отдельные PR): 7 (Zep-очередь и ключ user), 8 (хардкод владельца в 9 местах), 11 (mutex ingest на сервере), 12 (`clearTimeout` после тела), 13 (`days=`), 15 (`LOOK_LIMIT`).
