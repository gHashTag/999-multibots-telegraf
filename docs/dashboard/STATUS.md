# Статус готовности к продакшену

Снято: **2026-08-23 14:43 UTC** · коммит `c4a39d10` · ветка `main`

> Каждое число здесь получено запуском. Что измерить не удалось —
> помечено «не проверено» и НЕ засчитано как успех. Файл переписывается
> командой `npm run dashboard`.

**Проверок 8: ок 8, сломано 0, не проверено 0**

## Живые службы

| Служба        | Код | Статус |
| ------------- | --- | ------ |
| Рендер-сервер | 200 | ок     |
| Мини-апп      | 200 | ок     |

## Лента (проверено запросом)

| Маршрут                      | Статус | Деталь                             |
| ---------------------------- | ------ | ---------------------------------- |
| `GET /api/feed/stats`        | ок     | авторов 1, роликов 3, просмотров 0 |
| `GET /api/feed?page=`        | ок     | page=0 → [3], page=1 → [2]         |
| `GET /api/feed/:id?user_id=` | ок     | код 200                            |
| `POST /api/feed/:id/like`    | ок     | обработчик есть, нужна подпись     |
| `POST /api/feed/:id/view`    | ок     | обработчик есть, нужна подпись     |
| `POST /api/feed/:id/use`     | ок     | обработчик есть, нужна подпись     |

## Качество кода

| Что            | Значение                                                 |
| -------------- | -------------------------------------------------------- |
| Линт           | ошибок 30, предупреждений 3162                           |
| Типы: корень   | 0 ошибок                                                 |
| Типы: мини-апп | 0 ошибок                                                 |
| Типы: рендер   | 7 ошибок                                                 |
| Тесты          | падает 69 файлов из 183 (207 тестов), база от 2026-08-23 |

## Секреты в отслеживаемых файлах

| Форма ключа         | Файлов |
| ------------------- | ------ |
| Inngest event key   | 0      |
| Inngest signing key | 14     |
| HeyGen              | 2      |
| GLM/Zhipu           | 1      |
| FAL                 | 1      |

> Значения не печатаются намеренно. Правка файлов ключ не отзывает —
> найденное подлежит перевыпуску, а не удалению.
