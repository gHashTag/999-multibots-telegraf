# Что ждёт вашего решения

Один список вместо двадцати семи разборов в `docs/audit/`. Каждый пункт сверен
с данными **20 августа 2026** — не переписан из старых отчётов.

Порядок — по цене промедления, а не по размеру работы.

---

## 1. Перевыпустить ключи · срочно

Ключи лежат открытым текстом там, где их видно многим. Утечка уже произошла;
вопрос только в том, воспользуются ли ей.

| что                                      | где                           | состояние               |
| ---------------------------------------- | ----------------------------- | ----------------------- |
| токены **10 ботов**                      | `users.photo_url`, 264 строки | действующие             |
| служебный ключ Supabase (`service_role`) | **15 файлов** репозитория     | работает, годен до 2035 |
| 2 ключа ElevenLabs                       | история коммитов              | работают                |
| токен Replicate                          | мой вывод в итерации 47       | скомпрометирован мной   |

**Что даёт токен бота:** читать все входящие сообщения, писать от имени бота,
менять настройки. **Что даёт служебный ключ:** полный доступ к базе в обход
всех проверок.

**Порядок действий:** перевыпустить у @BotFather и в панелях → обновить в
Infisical → запустить `scripts/redact-tokens-in-photo-url.cjs --apply`
(по умолчанию сухой прогон). Чистку базы я намеренно НЕ запускал: пока ключи не
перевыпущены, она не защищает, а создаёт ощущение решённого.

**Уточнение от 20 августа:** файлов пятнадцать, а не одиннадцать. Четыре с
кириллицей в имени выпадали из осмотра — `git ls-files` без `-z` экранирует
такие имена, и проверка их пропускала. Ошибка была в моём инструменте, теперь
исправлена.

---

## 2. Вернуть @Ludmila 1210 звёзд · деньги человека

`7007992081`, бот `Gaia_Kamskaia_bot`. Заплатила за обучение пять раз 27 и 29
июля 2025. Две записи висят в `running` **тринадцать месяцев**, готовых моделей
ноль. Единственный такой человек из 69 плативших за обучение — проверено дважды
разными способами.

Баланс на сегодня — **98,19**, то есть возврата не было.

Что она видела в боте: «У вас нет обученных моделей». С 20 августа видит ещё и
список незавершённых обучений с приглашением написать в поддержку — но деньги
сами не вернутся.

Разбор: `audit/training-money-truth.md`, `audit/five-without-training.md`.

---

## 3. Шесть отрицательных балансов · −6944 звезды

| номер                 |  баланс |
| --------------------- | ------: |
| 409972991             |   −4994 |
| 5781166218            |   −1288 |
| 7641943640            |  −250,5 |
| 752224685             | −226,81 |
| 89962285              | −176,86 |
| 999999997 (служебный) |    −7,5 |

Люди получили услуги, за которые не заплатили. Все случаи старше десяти
месяцев, свежих нет: похоже на состязание «проверил — списал» в старом коде.

**Решение:** списать в убыток или взыскать. Сторожа я не ставил намеренно —
запрет «никогда не уходить в минус» может сломать законные пути вроде
подарочной генерации.

---

## 4. Подключить сторож зависших обучений

Пятнадцать записей в подвешенном состоянии. Функция `checkStuckTrainings`
(расписание раз в 30 минут) в проекте есть и **не зарегистрирована**.

Подключение меняет статусы в базе — поэтому решение ваше, а не моё.

Без неё обучение, не получившее завершающего уведомления, висит вечно: ровно это и
случилось с @Ludmila.

---

## 5. Награда за приглашение · единственный работавший канал роста

738 профилей из 2354 пришли по ссылке-приглашению, **611 из них за один сентябрь
2025**. В августе 2026 — ноль.

Сцена приглашения годами обещала бонусные звёзды. Проверено: **не выдано ни
одной** на 17 136 платежей. С 20 августа механизм готов и выключен:
`REFERRAL_BONUS_STARS = 0`. Пока ноль — награда не начисляется и не обещается.

**Решение: назвать число.** Посчитано, во что обойдётся —
`audit/referral-economics.md`:

| способ                             | рекомендуемая награда | доля от дохода |
| ---------------------------------- | --------------------: | -------------: |
| за первое пополнение приглашённого |     **100–200 звёзд** |          6–13% |
| за регистрацию                     |               5 звёзд |             7% |

За регистрацию платить дорого: из 738 пришедших по ссылке пополняли **33 (4%)**
— двадцать пять выплат на одного плательщика, и регистрацию в Telegram дёшево
подделать. Поэтому дополнительно реализован второй способ — награда за ПЕРВОЕ
ПОПОЛНЕНИЕ (`core/referral/rewardOnFirstTopUp.ts`), подключена к обработчику
Robokassa. Двойная выплата невозможна.

**Теперь сторон две.** Приглашённому по ссылке до сих пор не доставалось
ничего — платили только пригласившему. Вторая сторона реализована
(`core/referral/rewardInvited.ts`) и тоже выключена в ноль:
`REFERRAL_INVITED_BONUS_STARS = 0`. Стороны независимы: у каждой свой номер
счёта, и сбой на одной не отменяет другую.

**Бюджет не удваивается, а делится.** Это важнее самой второй стороны:

| настройка                | выплата за плательщика | доля от дохода |
| ------------------------ | ---------------------: | -------------: |
| 100 пригласившему        |             100 звёзд |             6% |
| 200 пригласившему        |             200 звёзд |            13% |
| **60 + 40** (две стороны) |         **100 звёзд** |         **6%** |
| 200 + 200 (две стороны)  |             400 звёзд |        **26%** |

Потолок по рынку — 15%. То есть 200+200 выходит за него почти вдвое. Если
включать обе стороны, разумно взять одно число из первой таблицы и разрезать
его, например `REFERRAL_BONUS_STARS=60` и `REFERRAL_INVITED_BONUS_STARS=40`.

Честно о доказательствах: выгода двусторонней схемы на НАШИХ данных не
измерена и не могла быть — односторонняя не срабатывала ни разу. Это
заимствованная гипотеза. Разумный порядок: сначала включить одну сторону,
дождаться первых выплат, потом резать бюджет на две.

Как проверить за неделю: считать (1) пришедших по ссылкам, (2) пополнивших из
них, (3) выплаченных наград. Успех — если (2) растёт быстрее (3); накрутка —
если (1) растёт, а (2) стоит.

Разбор: `audit/why-idle.md`, `audit/referral-economics.md`.

---

## 6. Два поставщика не отвечают

`openai` — HTTP 401, `elevenlabs` — HTTP 400. Тянется несколько недель.

Для ElevenLabs это не «нет ключа»: два рабочих ключа лежат в истории коммитов,
то есть учётная запись жива и нужен перевыпуск (см. пункт 1).

---

## 7. Девять кривых ссылок на модели

`model_trainings.model_url` вида
`jalisawallet-coder/Anneya:jalisawallet-coder/anneya-1773937126432:d365…`
вместо `owner/slug:hash`. Из девяти шесть — тестовые, **три принадлежат живым
людям**. Сами модели целы, потеряна ссылка.

Код, собиравший такие ссылки, исправлен (`core/replicate/buildModelUrl.ts`);
старые строки — правка данных.

---

## 8. 265 плательщиков без профиля

Из них подходящих под автоматическое заведение — **ноль**: 242 только
миграция и тестовые данные, остальные с неположительным балансом или
неправдоподобным номером. Двенадцать заведено 19 августа.

Пункт закрыт, оставлен для полноты.

---

## 9. Семь таблиц под удаление вместе с кодом

`ai_requests`, `bots`, `white_label_configs`, `marketplace_items`,
`marketplace_purchases`, `service_usage_stats`, `voice_models` — настоящих
импортёров ноль.

Ещё 18 таблиц отсутствуют в базе, но код к ним обращается и он живой: конвейер
рендера пишет промежуточные результаты в никуда.

**Решение:** завести таблицы или удалить код. Я не делаю ни того, ни другого:
схема — ваша территория, а часть кода может оказаться нужной.

Разбор: `audit/table-seams.md`.

---

## 10. Обучение голоса: включить или убрать из меню

Кнопка «обучение голоса» жива в меню и вела к списанию 100⭐ за услугу,
которая не может быть оказана: таблицы `voice_models` в базе нет (404),
обработчик события не зарегистрирован. За всю историю одна попытка
(2025-12-16, 100⭐, возвращены). С 21 августа сцена честно отказывает на
входе, денег не берёт.

**Решение:** либо завести таблицу и зарегистрировать `voiceTrainingRVC`
(меняет статусы — ваше решение, как и пункт 4), либо убрать кнопку из меню
совсем. Разбор: `audit/training-charge-gone.md`.

К сведению (чинится кодом, решения не требует): обучение МОДЕЛЕЙ с ноября
2025 не списывает ничего — денежный код остался в незарегистрированной
копии функции. Четыре успешных обучения у живых людей прошли бесплатно.
Возврат списания — тема следующего лупа.

---

## 11. Лид-магнет «ИИ Герои» списывает за бесплатный первый раз

`avatarTransformScene` включает `bypass_payment_check = true` для бесплатного
первого преображения (лид-магнет). Но проверка обхода в
`processBalanceOperation.ts:56` сравнивает `session.mode === 'AvatarTransform'`
(строка-литерал), тогда как значение режима — `'avatar_transform'` (ModeEnum), и
сама сцена `session.mode` вообще не выставляет. Итог: обход **никогда** не
срабатывает → бесплатное преображение списывается (или падает при нехватке
баланса). Первое впечатление новичка испорчено.

**Решение ваше:** починка ВКЛЮЧАЕТ обход платежа — это влияет на выручку,
безопасность и продуктовый замысел цены. Нужно подтвердить, что первый раз
должен быть бесплатным, и разрешить сцене ставить `session.mode`. Верифицировано
в лупе, разбор: issue #1339.

---

## 12. Сбой рендера: деньги списаны, возврата нет

Render-визарды (hedra / heygen / veed / ai-reels) списывают ⭐ до отправки на
render-сервер. Если сервер падает, его обратный вызов (`handleFailedRender`)
только уведомляет — деньги не возвращаются. Человек заплатил, видео нет.

**Решение ваше:** возврат — это движение денег (риск двойного возврата при
неверной реализации), поэтому автономно его не делаю. Безопасный шаг-подготовку
(сохранить сумму `cost` в задачу, чтобы callback знал, сколько вернуть) можно
сделать в лупе; сам возврат нужен с идемпотентным ключом по задаче. Разбор:
issue #1333.

---

## Чего в списке нет

Всё, что можно было починить кодом, починено и влито: открытые эндпоинты,
подделываемые обратные вызовы, молчащие отказы, возвраты из воздуха, промо при
сбое базы, утечка вложений. Здесь только то, что требует ваших ключей, ваших
денег или вашего решения о схеме.
