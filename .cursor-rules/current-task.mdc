---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Текущая Задача: НейроКодер

**Принцип:** Сосредоточение на одной задаче в каждый момент времени ведет к ясности и эффективности. Этот документ отражает текущий фокус работы.

**Дата Обновления:** {current_date}

**Текущая Задача:** 🎯 Миграция тестов на Vitest и Наведение Порядка

*   **Контекст:** Предыдущие попытки настроить тестирование (сначала Vitest, затем обсуждение Jest) выявили проблемы с мокированием сцен и разрешением путей.
*   **Решение Гуру:** Вернуться к Vitest, но использовать наработки/подходы, которые были успешны (или предполагались успешными) для Jest в других ветках/стешах.
*   **Цель:**
    1.  **Идентифицировать** существующие тестовые файлы (в текущем состоянии или после применения стеша).
    2.  **Мигрировать** синтаксис этих тестов с Jest-подобного на Vitest (`jest.` -> `vi.`, `require` -> `import`, `jest.mock` -> `vi.mock` и т.д.).
    3.  **Решить** оставшиеся проблемы с запуском тестов в Vitest (разрешение путей `@/`, мокирование контекста Telegraf).
    4.  **Удалить** зависимости и конфигурацию Jest, если они присутствуют.
    5.  **Добиться** успешного выполнения тестов (`pnpm vitest run`) для мигрированных файлов.
*   **Затронутые Файлы (Предположительно):**
    *   Файлы `*.test.ts` или `*.spec.ts` в `src/` или `__tests__/`.
    *   `vitest.config.ts`
    *   `tsconfig.json`
    *   `package.json`
    *   `jest.config.js` (если существует, для удаления)

**Статус:** ⏳ В Работе

---
*Ом Шанти. Приведем тесты к единому, рабочему формату.* 🙏
