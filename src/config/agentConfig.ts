/**
 * Agent configuration
 */

// Заглушка для конфигурации агентов
export const agentConfig = {
  // Конфигурация агентов
  default: {
    timeout: 30000,
    retries: 3,
  },
}

export default agentConfig
