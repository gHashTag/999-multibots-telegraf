/**
 * TODO: add unit tests for incrementLimit
 */
import { incrementLimit } from '../../../../src/core/supabase/incrementLimit'
import { supabase } from '../../../../src/core/supabase'

describe('incrementLimit', () => {
  it('TODO: implement tests for incrementLimit', async () => {
    // e.g. mock supabase.from(...)
  })
})
