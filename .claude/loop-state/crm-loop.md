# CRM: состояние цикла улучшений

Единственная память между витками крона. Читай ПЕРВЫМ делом, пиши В КОНЦЕ витка.

## ⛔ ЧТОБЫ НОВЫЙ ВИТОК НЕ СЛОМАЛ ПРОШЛУЮ РАБОТУ

Крон стоит на `7,22,37,52 * * * *` (каждые 15 мин, job f0ee9fda, с 16.09.2026).
Виток короче, чем большинство задач. Правила, выведенные из этого:

1. **Сначала прочитай «СЕЙЧАС В РАБОТЕ» ниже.** Если там стоит задача и она не
   твоя — НЕ начинай её заново. Возьми следующую из плана.
2. **Ветка одна.** Не создавай новую ветку, если в «СЕЙЧАС В РАБОТЕ» уже
   названа. Две ветки на одну задачу — это конфликт, который потом разбирать
   владельцу.
3. **Коммить часто и мелко.** Виток может кончиться в любой момент; всё, что
   не закоммичено, следующий виток не увидит и, вероятно, сделает заново.
4. **Никогда не откатывай чужой коммит.** Если кажется, что предыдущий виток
   сделал неверно, — напиши это в отчёт, а не `git revert`.
5. **Не сливай PR без слова владельца.** Он спит; слияние необратимо.
6. **Перед началом:** `git pull --ff-only` и `git log --oneline -5`. Чужие
   сессии льют в main параллельно (см. память `concurrent-sessions-on-main`).
7. **`tri waiting` ДО того, как что-то строить — не после.** Виток 36: я
   потратил половину времени, перенося 16 хелперов `tri` на main, и лишь при
   выводе списка PR увидел #2430 — та же работа, те же 16 файлов, открыта
   прошлым витком и MERGEABLE. Команда, которая показала бы это за две
   секунды, лежала у меня же в руках и была написана мной же для ровно этого.
   Дубликат закрыт (#2445), уцелевшая часть встала НАД #2430 отдельным PR
   (#2446). Правило: список открытых PR — первая команда витка, а не
   иллюстрация к отчёту.

## СЕЙЧАС В РАБОТЕ

### ВИТОК 55: продавец выбирает ПРАВИЛЬНО, и это меняет вопрос

Замер: 56 карточек за 4.4 суток ушли десяти людям, трое собрали 66%. Похоже
на дефект выбора — оказалось, нет. Первые пять очереди работы (`crm_leads`)
это ровно те, кому карточки и делаются. Обход исполняет поручение буквально.

**Не вычитайте один список из другого** (форма 84). «17 горячих без карточки»
я получил, вычтя очередь ВОЗВРАТА (`crm_hot_leads`: говорил о покупке за 14
дней и молчит месяц) из очереди РАБОТЫ (`crm_leads`: пишет сейчас). Пересечение
пусто по построению.

**Что действительно стоит внимания:** из 18 в очереди возврата 17 не получили
ничего ни разу, и поручение обхода на этот список НЕ СМОТРИТ вовсе. Это дыра в
замысле, а не ошибка модели.

**Следующий шаг, который не требует решения владельца:** если по человеку
карточка уже готовилась и её не нажали — не готовить ему же на следующем тике.
Ничего не отправляется, меняется только чья карточка готовится следующей;
экономит генерацию и расширяет охват.

### Инструменты витка

`tri attention` — на кого уходят карточки, рядом обе очереди с их
определениями. `tri stale` — что из открытых PR уже лежит на main (нашёл один:
работа приехала другим коммитом). `tri queue` — порядок слияния.

### ВИТОК 48: сторож короче охраняемого — это ЦЕЛЫЙ КЛАСС

Слито по слову владельца: **#2461** (вытеснение карточек) и **#2430** (16
инструментов `tri`). Деплой бота SUCCESS 15:13 UTC, сразу после слияния.

`tri deadlines` — все сроки проекта в один столбик по возрастанию. Пара,
живущая в двух файлах, невидима по одному файлу; в отсортированном столбике
она бросается в глаза. Нашлась вторая такая пара: **`CARD_LEAD_TTL_MS` = 15
мин** сторожит память о карточке, которую можно нажимать **12 часов**. Значит,
для любого нажатия «после сна» память уже стёрта: у «✅ Отправлено» нет кнопок
человека, а «напиши короче» нечего делать короче. → **#2462**.

Сама команда в первой версии соврала: печатала «60 мс DAILY_GENERATION_CAP» —
шестьдесят генераций в сутки как миллисекунды. Единица бралась из ФОРМЫ имени.
Форма **73**.

### Спасение знаний из неслитых веток

Формы 56–72 и этот файл существовали только в ветке `fix/reel-text-and-file-reading`
(PR #2414). Любая свежая ветка от main получала скилл, обрывающийся на 55, и
план от 8 сентября вместо передачи смены. → **#2463** (формы), этот PR (файл).

**Правило:** знание, которое живёт только в неслитой ветке, для следующего
витка не существует. Проверять `wc -l` спасаемого файла на main и на ветке.

### Что осталось непроверенным

Починка #2461 в проде с 15:13. База отсчёта: последнее вытеснение
`2026-09-16T15:03:40`, карточка Геи жива примерно до 03:03 UTC. Если починка
работает, новых строк `draft-unsent … replaced` до истечения этой карточки
быть НЕ должно. Проверять: `tri orphans`.

### Разобранная аномалия: «пустая причина» была обрезкой

В витке 47 я написал, что `sweep-failed` пришёл с пустой причиной. Неправда:
причина была — `This operation was aborted`, её обрезал мой же наблюдатель по
ширине строки. Это наш AbortController на 180 с (`ЖДАТЬ_МС` в trinityAgent),
то есть ход модели не уложился в бюджет. Ход, который уже сделал картинку.
**Следующий кандидат на разбор.**

### ВИТОК 47: 26 картинок, сделанных и выброшенных (PR #2461)

Очередь держит ОДНУ карточку на владельца: новая вытесняет ждущую. Сторож
против этого был **таймером** — отсрочка с двух часов. А карточку можно
нажимать двенадцать. Обход возвращался на четвёртом часу, рисовал вторую и
убивал первую вместе с уже оплаченной картинкой. И так по кругу.

`tri orphans` (новая команда) спросил у журнала улья: **26 фото-черновиков
`replaced` за двое с половиной суток**, ровно по кривой отсрочки.

Починка: держать, пока карточку ЕЩЁ МОЖНО НАЖАТЬ. Срок едет вместе с
карточкой от рендера (`expiresAt`), а не копией константы на стороне бота.
Нет срока — сторож выключается, но не выдумывает дедлайн.

Формы **71** (отказ, разобранный как пустой список) и **72** (таймер вместо
факта, и он короче охраняемого).

### Продавец жив, и молчание было законным

С 12:31 до 15:00 в журнале пусто — это держатель, а не смерть крона. Тик в
15:00 отработал: карточка в 15:02. Сразу за ней `sweep-failed` на шаге «ход
модели» с ПУСТОЙ причиной — следующий кандидат на разбор.

### ВИТОК 46: `tri alive` — дышит ли продавец

Обход, которого НЕ БЫЛО, и обход, прошедший тихо, в журнале улья
неразличимы: туда пишутся только состоявшиеся (idle/card/failed), а `held` и
несработавший тик молчат. Значит, если крон однажды перестанет срабатывать
совсем, доска будет выглядеть как в спокойный день.

`tri alive` отвечает на один вопрос: сколько прошло с последнего следа обхода
и не больше ли это самой длинной ЗАКОННОЙ паузы (`BACKOFF_CAP_MS`, 24 ч).
Предел читается с **origin/main**, и без него команда отказывается судить.

### 🛑 УМОЛЧАНИЕ, СОВПАВШЕЕ С ПРАВДОЙ (форма 70)

Первая версия брала предел из рабочей копии и падала в `${CAP_H:-24}`.
Печатала «24 ч» — верно. **По совпадению**: константы в рабочей ветке нет
вовсе (приехала на main с #2451), grep не находил ничего, срабатывало
умолчание, списанное мной по памяти.

Это хуже ошибки: ошибка видна, а здесь всё выглядело правильно и перестало бы
быть правильным ровно тогда, когда предел изменят. **Проверка:** сломай путь к
источнику нарочно — если вывод не изменился, ты читал умолчание.

### Правило слияний: сливать СРАЗУ ПОСЛЕ тика

Деплой съедает тик крона, если попадает в него (форма 69). Для `*/30` окно —
первые двадцать минут получаса. Слияния, трогающие только дерево рендера,
бот не перезапускают (деплой бота SKIPPED) и тика не стоят.

### ВИТОК 45: канал тревог был на 100% шумом (#2456, не слит)

41 из 41 события `failure` в окне журнала — одна и та же строка про
незаменённую картинку. Это ожидаемая цена правила «один черновик на владельца»,
а не провал. И она **прятала настоящую беду**: неудавшийся ВОЗВРАТ денег
пишется тем же видом (`back.ok ? 'tokens-refunded' : 'failure'`).

Дано своё имя `draft-unsent` и важность `normal`. `tri funnel` ищет по тексту,
а не по виду, поэтому доска не ослепла — но это было везение, теперь проверено.

### ⚠️ ЗАМЕР НАЖАТИЙ: взведён, НЕ отвечен

Оба сервиса задеплоены 16.09 в 14:16 UTC, `RENDER_API_KEY` у бота есть.
`card-pressed` = 0, но прошло 13 минут, а не сутки. **Ноль здесь ничего не
значит** — не принимай его за находку (форма 62).

Последний обход в журнале — 12:31. Между ним и деплоем всё было `held`
(удержание не журналится), это согласуется. Первый обход ПОСЛЕ деплоя —
проверка, что слияния не сломали продавца; на момент записи ещё не случился.

### ВИТОК 44: ТРИ PR СЛИТЫ ВЛАДЕЛЬЦЕМ — не считай их открытыми

Владелец сказал «все три». Слиты: **#2451** (удержание по продавцу + растущая
пауза), **#2453** (подарок доходит до тех, у кого нет кошелька), **#2454**
(нажатие пишется в журнал улья, обе кнопки). Перед слиянием все три были
сведены вместе в проверочной копии: типы чисты, рендер 2005, службы бота 380.

### ✅ ПОДТВЕРЖДЕНО В ПРОДЕ: `replied = 1`

Первое такое касание за всё время. Писатель, слитый витком 43, сработал:
цепочка ingest → зеркало → касание живая. Это не догадка, это запись.

### НАЙДЕНО: бриф противоречил сам себе (#2455, НЕ слит)

Шаг 2: «возьми первого, у кого next НЕ wait» (пропускай wait).
Шаг 4: «если у ПЕРВОГО next=wait — ответь «тихо»» (остановись на первом).
Оба сразу невыполнимы, и модель слушалась хвоста. В проде `wait` — 544 из 881,
так что кандидат №1 часто именно такой; в журнале продавец так и говорит:
«1 кандидат(ов), все next=wait» — и молчит, имея четыре непросмотренные строки.

Плюс три предложения из находки о конкурентах: нажатие — это продукт, значит
дефицит не время обхода, а внимание владельца; слабая карточка ВЫТЕСНЯЕТ
следующую (черновик один на владельца).

### ВИТОК 43: КОНКУРЕНТЫ — наша ось не занята никем (и это обоюдоостро)

Прочитал вторую половину исследования витка 41, которую пропустил. 16 страниц
конкурентов скачаны и перепроверены по исходному HTML. Главное:

**Ни одна страница не рекламирует человеческое нажатие перед отправкой.**
Grep по всем файлам на «перед отправкой», «подтвердить отправку»,
«approve each», «human approval» — **ноль совпадений**. Человек у них бывает
трёх видов: кому эскалируют; издержка, которую убирают; оператор, чью нагрузку
меряет дашборд. Radist пишет инверсию нашего продукта своей продающей фразой:
«больше не придётся открывать каждый диалог и отправлять вручную».
TextBack сам шлёт на 1-й, 3-й, 7-й день. amoCRM передаёт человеку «бесшумно».

Вывод для позиционирования: **нажатие — это и есть продукт, а не дефект.**
Все витки, где я называл нажатие «узким местом», говорили правду о пропускной
способности и неправду о ценности.

Не установлено: salebot.ai не существует как продукт (302 на биржу доменов);
manychat/wazzup отдали 403.

### ПОЧИНЕНО: нажатие теперь оставляет след (#2454)

Исследование не смогло установить частоту нажатий — и причина хуже отсутствия
телеметрии: **записывать было нечего.** Касание пишется только при наличии
lead, лога успешного нажатия нет вовсе, в журнале улья не было такого вида.
Добавлен `card-pressed`, пишутся ОБЕ кнопки.

⚠️ 24-часовой grep логов на `tgp:ok` даёт ноль — и этот ноль **ничего не
доказывает**. Так выглядит замер, которым он не является.

### ВИТОК 42: цена выброшенных картинок УСТАНОВЛЕНА — она маленькая

41 картинка за 5 суток стоила **только кредитов провайдера**: ~246 кредитов
Kie ≈ **$1.23**. Ни одного токена. Доказательство — баланс владельца **7571**,
не двинувшийся ни на единицу за всё это время; объяснение —
`HOUSE_TELEGRAM_IDS`, дом себе не платит.

**Виток 41 назвал это «нашёл, куда уходят деньги» — это преувеличение, снято.**
Прежде чем называть цену, смотри ведомость (форма 64): я дал три разных ответа,
читая код, и только баланс закрыл вопрос.

### НАЙДЕНО И ПОЧИНЕНО: подарок не доходил до чужих (#2453)

`resolveLead` берёт числовой id только для тех, кто уже в `users` — защита от
опечатки в счёте. Но обход отдаёт модели именно числовой id, и лид-магнит
отказывал ровно тем, ради кого существует. В журнале слова самого продавца:
«deliver по нему не собрать — нет в базе, назовите по @username».

Подарок бесплатен и никого не кредитует, поэтому для НЕГО добавлен путь через
@username из нашей же `crm_people`. Платный путь не тронут, проверено тестом.

### Продавец ведёт себя правильно — это видно в журнале

Три раза за день ушёл в простой со словами «карточка уже готова и ждёт твоей
кнопки». Узкое место — нажатие, и это теперь не догадка.

### ВИТОК 41: ДВА PR СЛИТЫ ВЛАДЕЛЬЦЕМ — не считай их нерешёнными

Владелец сказал «все три» и «запусти подарок». Сделано:

- **#2447 слит** — карточка по @username пишет касание, и вместе с тем
  возвращается ряд «Позже / Отказ» после «Отмены» (без числового lead
  `afterCancelKeyboard` его не рисует вовсе).
- **#2449 слит** — `replied` доставлен; добавлен сторож НА ПРОВОДКУ, потому
  что тринадцать тестов писателя не замечали отключения вызова из зеркала.
- **#2450 открыт, НЕ слит** — запись живого прогона модели подарка + две
  починки синхронизации каталога. Ждёт слова владельца.
- **`note` НЕ доставлен намеренно:** его проводка — часть фичи подписок на
  176 строк (счета-подписки, защита от двойной подписки, потолки звёзд).
  Платёжный код внутри «доставки бухгалтерии» не протаскиваем.

### ПОДАРОК ЗАПУЩЕН. Модель: `gpt-image-2-5-flare-image-to-image`

75 с, 6 кредитов, 12 токенов, 941×1672 (9:16). Цена из прайса сошлась с
замером до единицы. **Первый прогон дал не ту модель** — `image_edit` по
умолчанию берёт `google/nano-banana-edit`, и я принял это за нестыковку в
коде; нестыковка была в пробе. Подарок явно передаёт свою модель.

### 🛑 НЕ ПОВТОРЯТЬ: не суди факт раньше, чем у него был повод (форма 62)

Через 20 минут после слияния мой же `tri books` объявил `replied` мёртвым.
Записей не было потому, что ingest вернул `messages_new: 0` — повода не
случилось. Инструмент исправлен: писатель моложе 48 ч в проде — не находка.
Сейчас настоящие находки только эти: `later`, `refused`, `note` (7 дн),
`bought` (8 дн).

### ВИТОК 40: четыре нуля — четыре РАЗНЫХ причины, и все мои прошлые выводы неверны

Шёл делать «дописать недостающих писателей фактов» (свой же вариант 1). Это
была бы **третья подряд работа над тем, что уже написано**. Точная картина:

| факт               | почему ноль                                                                      |
| ------------------ | -------------------------------------------------------------------------------- |
| `written`          | касание за `if (p.lead && ...)`, у карточки по @username lead нет — **#2447**    |
| `later`, `refused` | писатель И кнопка **на main**; кнопка не РИСУЕТСЯ без числового lead — **#2447** |
| `replied`, `note`  | писатель написан и **никогда не слит**. Строить нечего                           |

Цепочка для `later`/`refused`: `cardLeadOf` → null при @username → `takeCardLead`
→ null → `afterCancelKeyboard(null)` → ряд «⏰ Позже / 🚫 Отказ» **не рисуется
вовсе**. Записано комментарием на #2447 — он стоит дороже, чем я вчера заявил.

### 🛑 ЧЕТЫРЕ НЕВЕРНЫХ ЧТЕНИЯ ЗА ОДИН ВИТОК (форма 61)

«refused никто не пишет» (пишет `touchLead`); «bought никто не пишет» (пишет
тернарник); «у карточки нет способа записать отказ» (есть, после «Отмены»);
«replied — дефект логики» (писателя просто нет в проде). Плюс вчерашнее
«FAL 403» при живом Kie.

**Ноль — это вопрос из трёх слоёв:** пишет ли кто-нибудь где-угодно → лежит ли
писатель в проде → доходит ли до него управление. Второй слой самый дорогой:
переписать уже написанное и неслитое я успел трижды.

`tri books` переписан и даёт три приговора вместо одного. Его вторая версия
сама считала ЧИТАТЕЛЯ писателем (`crm-touches.ts`, где тип объявлен) — ровно
та ложь, ради которой команда написана. Теперь писателем считается ВЫЗОВ.

### ⚠️ ВИТОК 39: «отправлено 5» — ЭТО БЫЛА ДЫРА В УЧЁТЕ, а не поведение владельца

Семь витков я докладывал 64/5 как утечку между карточкой и нажатием. Потом
посмотрел, ЧТО пишет пятёрку: касание `written` пишется только
`if (p.lead && ctx.pool)`, а карточка по `@username` lead не получает вовсе.
Нажатие отправляло письмо и **не писало ничего**. В тот же день карточка,
ждавшая нажатия 119 минут, была адресована по @username.

Починено: **#2447** — `leadOfTargetIn` разрешает @username через `crm_people`,
с ограничением по владельцу и отказом от неоднозначности. Четыре обратные
мутации убиты. Рендер 192 файла / 1983 теста.

### 🛑 И ШИРЕ: 46 ветвлений логики стоят на фактах, которых прод не писал НИ РАЗУ

Из шести видов касаний записан **один**. `later` 13 мест, `refused` 13 (включая
штраф −10 к счёту), `replied` 8, `bought` 7, `note` 5 — по нулю за всё время.
Новая команда **`tri books`** показывает это за две секунды.

### ПОПРАВКА К ВИТКУ 38

Я назвал причиной «FAL 403 — картинку нечем нарисовать». **Неверно.** Доставка
портрета идёт через `image_edit` (Kie), у Kie 4935 кредитов, ключ настроен,
инструмент зарегистрирован. Сам код #2437 от этого не зависит — он реагирует на
НАБЛЮДАЁМЫЙ отказ, а не на конкретного провайдера. Тело PR и комментарий
поправлены. Верной осталась измеренная половина: в окне из пяти, которое видит
обход, **ноль** помеченных `waiting_for_reply`, а при limit 50 их тридцать.

### ВИТОК 38 (16.09, 18:2x): найдено, ПОЧЕМУ 64 карточки против 5 отправленных

Замер в проде: **FAL отвечает HTTP 403** (баланс исчерпан) — и в окне из пяти,
которое единственное видит обход, **четверо несут `next=deliver`**. Продавец
готовит картинку, которую нечем нарисовать.

| окно                | что в нём                                 | ждут ответа |
| ------------------- | ----------------------------------------- | ----------- |
| limit **5** (обход) | deliver 4, wait 1                         | **0**       |
| limit 20            | deliver 10, reply 6, offer 2, wait 2      | 6           |
| limit 50            | deliver 13, **reply 30**, offer 4, talk 1 | 30          |

Починено в **#2437** (не новой веткой — там уже жил `image-health`): `deliver`
понижается до `offer`, когда провайдер лежит, причина едет в `because`, оба
читателя `leadCandidates` проведены (иначе доска и очередь показали бы разное).
Обратная мутация убивает. Рендер 192 файла / 1986 тестов зелен.

**Гипотеза НЕ подтвердилась, и это важно:** я шёл проверять, что окно забито
`wait`. Нет — `unanswered → reply` честно обгоняет всё. Настоящий дефект стоял
рядом и был виден только потому, что я напечатал распределение целиком.

**Чего это НЕ делает:** не пополняет FAL и не переводит подарок на Kie
(кредиты есть, `gpt_image_edit` не звали НИ РАЗУ). Это трата — нужно слово
владельца.

**Оснастка:** `self` и `rotten` перенесены на рабочую ветку — прошлый виток
положил их только в ветку PR, и `tri rotten` провалилась в справку.

### ВИТОК 37 (16.09, 18:0x): девять PR разблокированы одной правкой

`tri waiting` первой командой (правило 7) — и сразу аномалия: **девять моих
PR за ночь стали CONFLICTING**, прошлым витком были чистыми.

Причина одна на все девять: `render/crm-memory-tools.test.ts`, побайтово
одинаковый блоб во всех. Пока они ждали, **другая сессия починила ту же
стареющую фикстуру на main** — независимо, другими словами, аккуратнее моего
(округление к границе суток + 10:00 UTC, строки round-trip точно).

Взял содержимое main в этом одном файле → **восемь PR стали MERGEABLE без
merge-коммитов**; #2428, состоявший из этой починки целиком, закрыт как
лишний. Конфликтных PR: было 9 моих, стало 0 (осталось 5 чужих и старых).

**Новое в оснастке:** `tri rotten` — печатает ФАЙЛЫ и сколько PR каждый
держит, а не список конфликтных PR. Ушла в #2446 поверх #2446/#2430.
Её первый прогон нашёл следующую общую причину: **`render/src/agent/tools.ts`
держит 4 открытых PR (#2138 #2161 #2350 #2414), `routes.ts` — 3.** Это чужие
PR — не трогать без слова владельца, но знать.

**Воронка за пять витков не сдвинулась:** подготовлено 63 → 64, отправлено
5 → 5, `talking` 0. Утечка между карточкой и нажатием, и она не чинится
кодом: нужно либо слияние PR, либо нажатие владельца.

### 🛑 ВИТОК 36 (16.09, 10:44-11:5x): МЕРДЖ #2414 ПОПРОБОВАН И ОТМЕНЁН — НЕ НАЧИНАЙ ЗАНОВО

Я завёл изолированный worktree, слил в него origin/main и получил **21 файл
в конфликте, 55 кусков**. Разбирать не стал — сначала померил, и замер
перевернул мою же посылку.

**Что показал замер (числа, не впечатление):**

|                                                           |                               |
| --------------------------------------------------------- | ----------------------------- |
| main с точки расхождения                                  | +74 168 строк, 503 файла      |
| ветка с той же точки                                      | +16 111 строк, 167 файлов     |
| из добавленного веткой в конфликтных файлах main НЕ ВИДЕЛ | **89 %** (1068 из 1206 строк) |
| файлов ветки, которые НЕ конфликтуют                      | **146 из 167**                |
| из них существуют только на ветке                         | 54                            |

Я ждал, что main обогнал ветку и её работа устарела. **Нет.** Ветка не
устарела — она заперта. Полностью доехал ровно один файл: `conversation.ts`.

**Вывод про форму работы.** Мердж — неверная форма. Верная: брать то, что
не может столкнуться, и лить отдельными PR от свежего main; 21 спорный файл
разбирать по одному, а не все сразу под таймером витка.

**Сделано в этот виток:** PR #2445 — `tri` доезжает до main.
17 команд (`mutate`, `wired`, `scan`, `owners`, `promises`, `clock`, `binary`,
`lost`, `landing`, `waiting`, `loop`, `loop-note`, `facts`, `funnel`, `sweep`,
`headroom`, `api`) жили ТОЛЬКО на этой ветке — то есть вся оснастка, которую
владелец просит расширять каждый виток, не доезжала никуда. Плюс новая
`tri self`. Шелл, рантайма не касается, до клиента дойти не может.

**Осторожно:** `bin/tri` на main УШЁЛ ВПЕРЁД, пока я мерил. Сплошной
`git checkout ветка -- bin/tri` удалил бы `tri app-smoke` из чужого PR #2405
(diff обещал +77/-0, на деле +85/-8). Брать надо файл main и ДОБАВЛЯТЬ в него.

### Что брать следующим витком (по убыванию отношения польза/риск)

1. **Один полный вертикальный срез, а не куча файлов.** Модули ветки без
   проводки — мёртвый код, и `tri wired` это же и ловит. Наименьшие конфликты
   проводки: `crm-summary-tool.ts` (1 кусок, ветка ведёт 69+/24- против 6+/4-),
   `crm-touch-tools.ts` (1), `crmMenu.ts` (1), `routes.ts` (1).
   Известные стороны для `crm-summary-tool.ts`: HEAD `whoPaid(pool as never)`
   против main `visibleScope(ctx).then(scope => whoPaid(scope))` — **брать
   main**, это починка видимости; остальные 50 строк ветки не конфликтуют.
   `conversation.ts` не трогать: уже на main целиком.
2. Файлы ветки, которых на main нет вовсе (54 шт.), — только ВМЕСТЕ с их
   проводкой, иначе это дежурный мёртвый код.
3. Тяжёлые (`tg-proposals.ts`, `telegram-tools.ts`, `crmProactive.ts`,
   `dm-delivery.test.ts`) — main переписал их целиком (main +845/-154 против
   ветки +107/-4 в `tg-proposals.ts`). Это НЕ работа на один виток.

- ветка: `fix/reel-text-and-file-reading` (PR #2414, ОТКРЫТ)
- аудит слабых мест ЗАВЕРШЁН (workflow `ws9ccnsff`, 12 агентов): 21 находка
  выжила, 4 убиты. Полный разбор: `/private/tmp/claude-501/.../ws9ccnsff.output`
  (файл сессионный — если его нет, список ниже единственный источник).

### ⚠️ ВТОРОЙ АУДИТ (workflow `wi1pe319h`, 16.09.2026): 19 выжило, 4 убито

Он бьёт по правкам ТОЙ ЖЕ смены. Первым делом починено самое тяжёлое:

- [x] **ДЕНЬГИ БРАЛИ, ТОКЕНЫ НЕ ПРИХОДИЛИ.** `creditStarsPayment` брала
      клиента из пула, открывала транзакцию и передавала ЭТОГО клиента в
      `moveTokens`, а та решала «пул или клиент» по наличию `connect` — у
      одолженного pg-клиента он ЕСТЬ. Второй `connect()` pg отвергает, всё
      зачисление откатывалось. Каждый платёж звёздами, на обоих маршрутах.
      Дыра НЕ сегодняшняя, но тест её не видел: двойник был добрее pg —
      без `connect`. Теперь `release` считается признаком одолженного
      соединения, а двойник бросает ровно то, что бросает pg.

СЛЕДУЮЩИЕ по тому же аудиту (все — про сегодняшние правки):

- [x] **Кнопка оплаты не называет чужую цену** (`b913baefa`). Сумма бралась из
      ПЕРВОГО числа со звёздами в ответе, а ответ часто называет несколько.
      Теперь: одна сумма на весь текст — она и на кнопке; две разные — кнопка
      без числа, Telegram покажет настоящую сам. Две мутации убиты.
- [x] **Бросок ≠ «вопрос не записан»** (`6939ee938`). Рендер пишет head 200,
      ПОТОМ сохраняет реплику человека, ПОТОМ зовёт агента. Значит ошибка
      внутри потока оставляет вопрос записанным, а бот писал его второй раз —
      и вдвое сокращал окно памяти в 40 реплик. Флаг `questionStored` едет на
      самой ошибке. Три мутации убиты.
- [x] **Подписка гасит подписку, разовая — разовую** (`812b9e090`). Счёт
      выбирался по человеку и числу токенов; человек может держать оба.
      Payload знает (`subtokens:` против `tokens:`) — теперь знание едет до
      конца цепочки. Отсутствие флага по-прежнему значит «не сказали».
      Три мутации убиты, у среднего звена свой сторож.
- [x] **АНОМАЛИЯ: ворота не видели сторожей по исходнику** (`7f768030c`).
      `vitest related` выбирает по графу импортов, а сторож, читающий файлы
      через fs, не связан с кодом ни одним импортом. Реестр
      `callbackNumberParseRegistry` краснел с `ace97435b` и пережил несколько
      push. Два места CRM записаны в реестр; `tri scan` гоняет 72 обходчика
      дерева за 8 секунд и добавлен в pre-push как `tree-guards`.

- [x] **Счёт гасится ПОСЛЕ зачисления** (`992ce30b4`). `/api/tokens/verify`
      ставил `redeemed = TRUE` до зачисления; бросок внутри зачисления уводил
      в catch, ответ 500, а счёт оставался закрытым навсегда — следующий
      verify ищет `redeemed = FALSE`. Деньги взяты, токены не пришли, повтор
      невозможен. Теперь наоборот; двойного зачисления это не открывает —
      замок на первичном ключе `star_payments`. Мини-апп больше не обещает
      «ничего не поделать»: счёт открыт, повторим при следующем входе.
      Две мутации убиты; два соседних сторожа были привязаны к соседству,
      а не к своему свойству — перепривязаны (форма 24 в скиллe).

- [x] **Подписку снова можно продать** — ПОЧИНЕНО с двух концов.
      `cancelled_at` ставил ровно один писатель — отмена ЧЕРНОВИКА в очереди,
      поэтому отправленная карточка оставляла строку, которую не гасило
      ничто. Теперь «живой» считается ОПЛАЧЕННАЯ и неотменённая подписка либо
      неоплаченный счёт свежее 7 дней; а `BotSubscriptionUpdated` со
      state=canceled штампует `cancelled_at`. На `failed` НЕ гасим: это
      непрошедшее списание, а не конец подписки — иначе человек заплатит
      дважды.
- [x] **`note` больше не затирает отказ нигде** — ПОЧИНЕНО. `lastAct`
      переехал из `crm-stages.ts` в `crm-supersede.ts` (модуль «что ещё
      считается») и применяется в `touchedSince` — а это единственный `touch`
      за тремя решениями очереди: штраф −10, запрет `next='talk'` и
      `refusedLately`. Мутация на проводке сначала ВЫЖИЛА: тестов на неё не
      было вовсе.
- [x] **Окно отрицания — теперь клауза, а не 24 знака** (`b451b1814`).
      Было неверно в ОБЕ стороны: «нет, хочу купить сейчас» терял buy (голое
      «нет» дотягивалось через запятую), «не надо, и оплачивать я не буду»
      давал buy (отрицание стоит ПОСЛЕ основы, туда никто не смотрел), и
      «I don't want to pay» проходил — два слова расстояния вместо одного.
      Теперь взгляд назад упирается в ближайшую границу клаузы (, . ! ? ; :
      тире, перевод строки — он же стык двух сообщений), и клауза читается
      ЦЕЛИКОМ. Вперёд читается тоже, но только «не + вспомогательный» из
      короткого списка: любое «не» убило бы «оплатить не проблема».
      Три мутации убиты.
- [x] **Альбом с подписью отвечается один раз** (`9339d55ea`). Гвард стоял
      ВНУТРИ ветки «без текста» и видел только альбом без подписи. С
      подписью выходило два ответа: элемент с подписью шёл в модель, а
      следующий не находил ни текста, ни заявки на альбом (модельный путь её
      не делал) и дописывал снизу шаблонную строку. Теперь заявка стоит НАД
      развилкой — после пересылки владельцу, до любого из ответов. Считается
      как `albumQuiet`. Три мутации убиты, включая возврат гварда обратно
      в ветку.
- [ ] СРЕДНЕЕ. В маршруте verify `redeemed = TRUE` ставится ДО зачисления
      отдельным автокоммитом: падение оставляет счёт закрытым навсегда.
- [x] СРЕДНЕЕ. `recordedQuestion` — ПОЧИНЕНО (`6939ee938`), флаг едет на ошибке.
- [x] СРЕДНЕЕ. `redeemInvoiceFor` — ПОЧИНЕНО (`812b9e090`), вид продажи едет
      всю цепочку от payload до SQL.
- [ ] Остальное — в `/private/tmp/.../wi1pe319h.output` (файл сессионный).

### План цикла 8 — по находкам аудита

- [x] Время касания = время события (`985ac6b17`). `recordTouch` принимает `at`.
- [x] Наш собственный автоответ пишет касание `written` (`985ac6b17`). Чинит
      «ours навсегда».
- [x] `stageOf` получает историю, а не одну строку (`c274bf2bc`) — в
      `crm_summary` и `crm_leads`.
- [x] `note` перестал притворяться последним действием (`c274bf2bc`).
- [x] **Ответ ИИ поверх перехвата владельцем** — ПОЧИНЕНО. Пауза
      перечитывается ПОСЛЕ того, как ответ составлен, и ДО отправки; ход
      выбрасывается, а не ставится в очередь (он собран без реплики владельца
      в истории). Тест проверяет сам порядок, не текст комментария.
- [x] **Фото не попадало в зеркало** — ПОЧИНЕНО.
      `sendFileWithAddressBook` была `Promise<void>` и теряла отправленное
      сообщение. Подделки `sendFile`/`sendMessage` в тесте теперь ВОЗВРАЩАЮТ
      сообщение, как Telegram: без этого правка осталась бы непроверяемой
      (скептик предупредил заранее, и был прав).
- [x] **«Платил ли человек» читает ОБЕ книги** — ПОЧИНЕНО. `платившие()`
      берёт необязательный пул и объединяет архив `payments_v2` с живой
      `star_payments` (строка на списание, в той же транзакции, что двигает
      баланс). Объединяет, а не заменяет: потерять годы плательщиков — тот
      же дефект наоборот. Молчащая живая книга не стирает ответ архива.
- [x] **Счёт бота фермы гасится** — ПОЧИНЕНО. `redeemed` писал только
      `/api/tokens/verify`, который ищет платёж в транзакциях ОДНОГО бота-
      кассира, а личный продавец чеканит токеном СВОЕГО бота — совпадения не
      было никогда. Деньги зачислялись штатно, врала только утренняя сводка
      («оплачено: 0, напомни человеку» про уже заплативших). Гашение
      перенесено в `/api/stars/credit`, правило «только при реальном
      зачислении» живёт ВНУТРИ `redeemInvoiceFor` — в render-server.ts его
      негде проверить тестом.
- [x] **`recordTurns` в личке вызывался наоборот** — ПОЧИНЕНО. Рендер сам
      пишет ОБЕ реплики (routes.ts:468-479 и 517-528), а личка писала на
      УСПЕШНОЙ ветке — то есть дублировала — и молчала на запасной. Дедупа
      нет ни с одной стороны: каждый обмен ложился дважды, окно памяти в 40
      реплик фактически вдвое короче. Теперь запись только в fallback, и
      `recordedQuestion` различает «агент не отвечен» (обе реплики) от
      «ответил пустым» (только ответ). ВАЖНО: перевёрнутое поведение было
      ЗАКРЕПЛЕНО тестом `businessAgentFirst.test.ts` — пришлось переписать
      его вместе с кодом.
- [x] **Альбом отвечается ОДИН раз** — ПОЧИНЕНО. Ветка `!text` отвечала и
      делала `return` ВЫШЕ единственного дедупа, а `media_group_id` не был
      даже объявлен в типе. Сторож вынесен в `src/services/albumOnce.ts`:
      импорт `businessBotService` тянет весь граф сцен, поэтому соседние
      тесты там проверяют текст исходника, а не поведение. Отдача файла
      владельцу осталась для КАЖДОГО элемента.
- [x] **Заготовки на медиа знают язык** — ПОЧИНЕНО. У каждого вида появился
      `replyEn`, выбор — `src/services/mediaLanguage.ts` по `language_code`.
      Здесь это уместно (слов клиента нет вовсе), на текстовом пути — нет, и
      причина записана в обоих местах. Русский остаётся по умолчанию: пустая
      локаль не повод переключать человека на английский.
- [x] **«не нужно»/«не хочу» больше не сигнал покупки** — ПОЧИНЕНО.
      Отрицание проверяется по КАЖДОМУ вхождению в окне 24 знака, поэтому
      «не хочу ждать, давайте оплачу» по-прежнему считается покупкой. Гасится
      только группа `buy`: «не подскажете цену» — это вопрос, а не отказ.
      Фразы «не нужно/не нужен/не хочу/не буду» добавлены в objection.
- [ ] СРЕДНЕЕ. `/sweep` с фильтром смотрит верхние 50 и молча говорит
      «никого нет».
- [ ] СРЕДНЕЕ. Память личек пишется на одного владельца, читается на другого.
- [x] **Канарейка владельца покрывает ВСЕ crm-инструменты** — ПОЧИНЕНО.
      `crm_deliver_photo` собирается фабрикой и не входил ни в один из пяти
      реестров; у `crm_offer` в ARGS стоял `telegram_id` вместо `chat`, и без
      него снятие `requireOwner` уводило в ветку без утечки — мутация выживала.
      Добавлен шестой реестр, ключи исправлены, и теперь падает сам ДРЕЙФ:
      инструмент с обязательным параметром без записи в ARGS или запись без
      инструмента роняют прогон.
- [x] СРЕДНЕЕ. `crm-replies.ts` — ПОЧИНЕНО (`ed4591edc`). Подделка больше не
      отвечает по имени таблицы: она отвечает ПО ДАННЫМ и проверяет, что
      каждое условие, на которое опирается, ещё есть в SQL. Замерено: мутация
      «убрать "out"» против СТАРОЙ подделки ВЫЖИЛА, против новой — умерла.
      Список нарушений читается после КАЖДОГО теста, а не там, где вспомнили.
- [x] **У модели отмены появился писатель** — `crm_touch {undo: true}`.
      Отменяет последнее ЖИВОЕ касание того же вида; ничего не стирает —
      поверх ложится строка с `reverts_id`. `crm_history` намеренно остаётся
      СЫРОЙ (владелец просил не стирать), свёртку делает `effectiveTouches`.
      ЗАМЕТЬТЕ РАЗНИЦУ: свёртка умеет отмену отмены, писатель — нет, и это
      намеренно (`latestToRevert` называет только живой АКТ). Путь назад —
      записать акт заново.
- [x] НИЗКОЕ. Дневной лимит — ПОЧИНЕНО (`82a783171`): план дня показывает
      `day_budget` — лимит, сколько карточек ушло за сутки и сколько осталось.
      Скользящие сутки, не календарные: часового пояса владельца здесь никто
      не знает.
- [x] НИЗКОЕ → оказалось ВАЖНЕЕ. `warm` не «нечем действовать»: он НАРОЧНО
      запрещён как очередь (`BATCH_PRESETS`, «только пакетом»). Аномалия была
      в другом: отказ звал `/batch`, которой не зарегистрировано НИГДЕ —
      владелец набирал сказанное и получал тишину. Сообщение теперь называет
      работающий путь (`be6a3e321`), и сторож
      `everyCommandNamedExists.test.ts` держит это правило.
      ⚠️ Пакетный режим прогрева ТАК И НЕ СДЕЛАН — это вопрос владельцу, а не
      моё решение: он про то, что уходит клиентам.
- [ ] НИЗКОЕ. Ключи `/api/agent/keys` не работают на `/a2a`.
- [x] `crm-owner-gate.test.ts` больше не слеп для `crm_offer` (там же).

### ТРЕТИЙ ПРОХОД (17.09.2026): по НЕТРОНУТОМУ — движок обхода и план дня

Два прошлых аудита били по правкам смены; сюда не смотрел никто.

- [x] **План дня уходил ДВАЖДЫ после деплоя** (`582ea3620`). Маркер в общей
      переписке — единственное, что стоит между рестартом и вторым планом
      поверх прочитанного. Искали его в окне МОДЕЛИ (40 реплик), а обход
      пишет ДВЕ реплики каждые полчаса: к обеду утренний маркер уже вытеснен.
      Теперь ищем на 300 назад. Сторожа ДВА: один на «просят глубину», второй
      на «глубина доезжает до запроса» — первая мутация доказала, что второй
      половины не было.
- [x] **Пять админов делили одно состояние обхода** (`339dc142b`). `ADMIN_IDS`
      в проде — пятеро, у каждого своя переписка и свои карточки, а `running`
      и `lastPushAt` были модульными переменными: ненажатая карточка одного
      держала ВСЕХ два часа, идущий виток одного отвечал «занято» остальным.
      Теперь по ключу владельца. Три мутации убиты (третья выжила в первом
      заходе — случая с двумя админами не было вовсе).
- [x] **Цифры дня складывали владельцев** (`6358be91c`). Ферма держит всех
      ботов в ОДНОМ процессе, `ADMIN_IDS` — пятеро. Счётчики `/business` были
      одним объектом, а список подключений — всеми подключениями процесса, с
      telegram_id чужих владельцев. Теперь по ключу владельца, `getBusinessStats`
      ТРЕБУЕТ владельца (вызывающий знает, кто нажал). Две мутации убиты.
- [x] **`/a2a` не принимал ключ, за которым сам же посылал** (`2cc6ef9d1`).
      Отказ звал «возьми ключ в мини-аппе (POST /api/agent/keys)», а личность
      брал через `chatIdentity` — тот читает ключи из ОКРУЖЕНИЯ. Выданный
      человеком ключ лежит в таблице `agent_keys`, и читает её только
      `resolveIdentity` — дверь, на которой давно стоят `/mcp` и
      `/api/agent/chat`. Та же форма, что «команда, которой нет».
      Сторож пинит ОБА конца, чтобы двери не разошлись.
- [x] `tri owners` — форма 29 механически: модульное состояние в коде «на
      каждого». Триаж, судит человек. Из 28 «общих» большинство честно
      процессные (флаги готовности таблиц, дедуп предупреждений).
- [x] **Разбор состояния теперь в коде** (`41dea30d6`). 43 объявления в обоих
      деревьях несут строку `// owner-scope: <почему>` — чем ключуют или
      почему процесс и есть верная область. `tri owners` их не показывает, а
      считает: сегодня НЕРАЗОБРАННОГО НЕТ. Значит любая новая строка в списке
      завтра — настоящий вопрос, а не 44-е повторение. Поведение не менялось:
      только комментарии и фильтр инструмента.
- [ ] ЗАМЕЧЕНО, НЕ ПОЧИНЕНО (вопрос владельцу, не мой): системный промпт
      берёт голос из ФАЙЛА `SOUL.md` (один на деплой), а личный `user_soul`
      из базы читается, только если модель сама позовёт `soul_get`. Для
      второго владельца это значит, что его продавец говорит голосом первого.
      Это решение о продукте, не дефект.

### ВОСЕМНАДЦАТЫЙ ПРОХОД (16.09.2026): часы как инструмент

- [x] **`tri clock N`** (`d997c3bab`) — прогон набора так, будто прошло N дней.
      Сдвигается ТОЛЬКО «сейчас»; явные даты остаются. Вчерашняя протухшая
      фикстура остановила выкладку всем — читать 116 файлов с датами глазами
      плохой способ, прогнать вперёд хороший.
      ⚠️ ПЕРВАЯ ВЕРСИЯ СОВРАЛА: сдвигала часы только в процессе vitest, а два
      теста запускают скрипт дочерним процессом — фикстура по сдвинутому
      «сегодня», сравнение с настоящим. Едва не отчитался «два теста умрут
      завтра». Починка: тот же файл в каждый дочерний node через
      `NODE_OPTIONS=--import`.
- [x] Честный ответ по рендеру: **один** гниющий тест из 1659 (окно отказа,
      умирал на ~23-й день). Починен; на 1000 дней вперёд набор зелен.
- [ ] НЕ ПРОВЕРЕНО: набор БОТА тем же способом. Там 69 файлов красны на main,
      поэтому сигнал придётся отделять от фона — отдельная работа.

### СЕМНАДЦАТЫЙ ПРОХОД (16.09.2026): ⚠️ ТРИ ФАКТА, КОТОРЫЕ МЕНЯЮТ ПЛАН

1. **В проде живы обе денежные дыры** (замерено в прошлый виток и
   подтверждено): `isBorrowedConnection` и порядок «зачислить → погасить» в
   main ОТСУТСТВУЮТ.
2. **Эта ветка отстала от main на 132 коммита** (и впереди на 87). Файлы,
   которых я не трогал, в main НОВЕЕ моих: например
   `crm-memory-tools.test.ts` — у меня 15 тестов, в main 18. Слить как есть
   значит откатить чужую работу. ЭТО МЕНЯЕТ СОВЕТ: сначала влить main в
   ветку и разрешить конфликты, только потом просить о слиянии.
3. **main КРАСЕН** на `crm-memory-tools.test.ts` (сегмент: ждали `hot`,
   получили `waiting`) — проверено на ЧИСТОМ main, без моих правок.
   И этот красный **блокирует push любой новой ветки**: `vitest related`
   тянет этот тест через граф импортов даже для правки в двух файлах.
   То есть путь выкладки в репозитории сейчас заклинен для всех.

- [x] Подготовлена маленькая ветка `fix/stars-credit-rollback` ОТ СЕГОДНЯШНЕГО
      main: только две денежные починки + их тесты (без правки текста в
      мини-аппе — она тащит за собой шлюз типов плеера). Рендер на ней:
      1971 прошло, 1 упал — тот самый чужой красный.
      **НЕ ЗАПУШЕНА**: ворота отказывают, а обходить их я не стал.
      Лежит в worktree: scratchpad/money-wt.
- [x] **КРАСНЫЙ В MAIN РАЗОБРАН И ПОЧИНЕН** — PR #2428. Это была не чужая
      регрессия: фикстура датирована `2026-09-07`, утверждение читается
      «спросил цену СЕГОДНЯ», окно `hot` — семь дней, часы настоящие. На
      16.09 фикстуре девять дней → `waiting` вместо `hot`. Тест падал сам по
      себе, по расписанию. Даты стали смещениями от now, посчитанными ОДИН
      раз. После починки на main: рендер 1972 прошло, 0 упало.
      Форма 33 в скилле.
- [x] **Деньги поданы отдельным PR #2429** поверх #2428: две починки, их
      тесты, два перепривязанных сторожа. Без правки текста мини-аппа.
      Обе ветки ЗАПУШЕНЫ, ворота пройдены честно (никакого --no-verify).
      ⚠️ СЛИЯНИЕ — ЗА ВЛАДЕЛЬЦЕМ. Я не сливаю.

### ШЕСТНАДЦАТЫЙ ПРОХОД (16.09.2026): ⚠️ В ПРОДЕ ЖИВЫ ОБЕ ДЕНЕЖНЫЕ ДЫРЫ

Хотел снять первый замер воронки новым `crm_money` — и обнаружил, что его в
проде нет. Проверил главное:

    в main есть isBorrowedConnection?             НЕТ
    в main есть «счёт гасится после зачисления»?  НЕТ

**Значит в проде: каждый платёж звёздами откатывается (деньги взяты, токены не
пришли), и погашенный заранее счёт делает тот платёж невосстановимым.** Обе
починки лежат в этой ветке. PR #2414 ждёт слова владельца — сливать без него
нельзя, и я не сливаю.

В журнале за доступное окно строк `[STARS]` НЕТ — то есть дыра не выстрелила
недавно просто потому, что почти ничего не продавалось. Первый настоящий
покупатель её найдёт.

- [x] **`tri waiting`** — что лежит в открытом PR, разложенное по риску:
      деньги / видно клиенту / тесты / инструменты. «86 коммитов» — не то, по
      чему принимают решение; «8 файлов трогают деньги» — то.
- [x] В скилл цикла: проверять, что починка доехала до ПРОДА, а не до ветки.
      «Починено» для владельца значит «работает у клиентов».

### ПЯТНАДЦАТЫЙ ПРОХОД (16.09.2026): воронка денег и сторож, который молчал

- [x] **`crm_money`** (`95dc4662d`) — воронка, которую никто не мог увидеть:
      выписано / оплачено / умерло черновиком / ждут, звёзды и токены, и
      ПОЧЕМУ умерли. Только чтение, только владельцу, только числа.
      Появился из нужды: чтобы замерить конверсию, надо читать
      `token_invoices`, а база — в приватной сети Railway (и правильно).
      Число, которое нельзя спросить, никто не посмотрит.
      ⚠️ ЧЕСТНО СКАЗАНО В ОТВЕТЕ: у таблицы нет колонки владельца, считается
      весь сервис. Я СНАЧАЛА НАПИСАЛ фильтр, который не фильтрует
      (`$2 = '' OR ...`), и удалил его до запуска — область, которая не
      ограничивает, хуже отсутствующей: ей верят.
- [x] **Форма 32 в скилле**: сторож колонок не мог сработать, пока все
      псевдонимы были кириллическими — матчер читает `[a-z_]`. Первый
      английский `AS stars_paid` его уронил. Псевдоним не колонка; послабление
      проверено мутацией (выдуманная колонка по-прежнему роняет тест).
      Правило: когда текстовый тест впервые краснеет на верном коде — спроси
      не «как успокоить», а «на чём он молчал всё это время».

### ЧЕТЫРНАДЦАТЫЙ ПРОХОД (16.09.2026): нажатие стало дешевле

Замер прошлого витка сказал: предел — нажатие владельца. Перед нажатием он
вынужден открыть переписку, чтобы вспомнить, кто это и что человек писал.

- [x] **Карточка говорит ПОЧЕМУ этот человек** (`f01193ca9`). Сервер собирает
      одну строку из фактов, которые память уже держит: что о человеке
      говорит очередь (`waitingOn`: «ответил, а мы молчим, 3 дн.») и его
      СОБСТВЕННЫЕ последние слова — цитатой. Чужой текст: сплющен и обрезан
      на сервере, сплющен и обрезан ещё раз на карточке; карточка — простой
      текст, отрисовать там нечего. Без причины карточка выглядит ровно как
      раньше. Всё в catch: сбой памяти стоит строки, а не продажи.
      Три мутации убиты; третья пережила ПЕРВУЮ версию своего теста —
      утверждение искало вторую половину двустрочной цитаты по всей карточке,
      а находило её и при сохранённом переводе строки.
- [x] **Причина есть у КАЖДОЙ карточки** (`ec8973594`). Обычное письмо из
      обхода (`tg_send`) — большинство карточек — уходило только с адресатом и
      текстом. Лид для него — сам адрес, когда он числовой (ровно так же
      выводит `cardLeadOf` на другой стороне); про @username память не
      спрашивают вовсе — она ключуется по id и только гадала бы.
      `reasonFor` переехал в свой модуль: обе стороны пути карточки нуждаются
      в нём, а они уже смотрят друг на друга (`crm-offer-tool` импортирует
      `propose` из `telegram-tools`).
      ⚠️ Аргумент `lead` у `propose()` НЕ выставлен намеренно: он же решает,
      писать ли касание «написали» после подтверждения, а эта правка — про
      карточку, не про бухгалтерию. Три мутации убиты.

### ТРИНАДЦАТЫЙ ПРОХОД (16.09.2026): откуда берётся 5 карточек за 30 дней

Замер (сводно, продакшен): за 90 дней — 5 карточек, за 30 — 5, за 7 — **4**.
То есть продавец начал работать НЕДЕЛЮ назад, а не «стоит». Предел не в
модели и не в удержании (2 часа → до 12 карточек в день), а в нажатии.

- [x] **Карточка, которой больше нет, говорит ВЛАДЕЛЬЦУ почему** (`fbd0de457`).
      Очередь держит один черновик на владельца: новая карточка гасит старую
      как `replaced`. Обход предлагает снова через 2 часа, а карточка живёт
      12 — значит вечером владелец жмёт кнопки, заменённые несколько часов
      назад, и каждая отвечала «уже подтверждено или истекло». Эти половины
      различаются ровно тем, что ему важно: «подтверждено» — клиент что-то
      получил, «заменено» — никто ничего не получил.
      Точную причину получает ТОЛЬКО тот, кто докажет, что карточка его:
      id владельца И секрет из кнопки. Остальным — прежняя расплывчатая
      фраза (она верна для чужого). Три мутации убиты.
- [ ] ОТКРЫТО ДЛЯ ВЛАДЕЛЬЦА: 316 ждущих при 0,57 карточки в день — это
      ~550 дней очереди. При нажатии каждой карточки (12/день) — 26 дней.
      Рычаг один: его нажатия либо решение о пакете.

### ДВЕНАДЦАТЫЙ ПРОХОД (16.09.2026): замер ПРОДАКШЕНА

Впервые за цикл смотрел не в код, а в живые данные и журнал.

- [x] **Два обхода на каждый тик** (`4d60340ad`), секунда в секунду, три тика
      подряд — `bot.ts` и `index.ts` оба зовут `startCrmProactive`. Сегодня
      безвредно (оба удержаны одной карточкой), опасно в момент, когда
      удержание истечёт при свободной модели: два хода за одного человека,
      две карточки в одну секунду, двойная трата. Цикл теперь заявляется по
      владельцу. Три мутации убиты.
- [x] **ЛОЖНАЯ ТРЕВОГА, пойманная на себе.** Посчитал возраст `last_ingest_at`
      от даты из головы и получил «32 часа тишины». `date -u` показал, что
      обновление было ЧАС назад. Записано в скилл цикла: время берётся
      командой, а не памятью.
- [x] **Живые числа (сводно, без имён и id):** 1014 человек, ждут ответа 316,
      горячих 19, winback 67, прогрев **3** (я повторял «12» из старого
      отчёта несколько витков — цифры стареют), продавец отправил **5
      карточек за 30 дней**.
      ⚠️ ЭТО ГЛАВНОЕ ЧИСЛО ПРОДУКТА: 5 против 316 — пропускная способность
      упирается в нажатия владельца, по замыслу. Стоит показать владельцу.

### ОДИННАДЦАТЫЙ ПРОХОД (17.09.2026): имя бота и 50 обещаний до нуля

- [x] **Имя бота: мы отказывали валидным и пропускали невалидные**
      (`f4432d91b`). Отказ обещает «от 5 до 32», выражение допускало 7..34.
      Замерено: «aabot» (5) — отказ, 34 знака — пропуск. Это путь создания
      бота НОВОМУ владельцу. Починено в ОБЕИХ копиях (бот и рендер).
      ⚠️ Старый тест УТВЕРЖДАЛ неверное поведение и тем держал ошибку —
      записано в скилл отдельным правилом.
- [x] **`tri promises` научился читать ОПИСАНИЯ ИНСТРУМЕНТОВ** — там живёт
      форма 31, и команда её пропускала: у числа «максимум 200» нет единицы,
      строка не попадала в выборку, а счётчик показывал ноль. Ноль, который
      врёт, равен ложному «разобрано».
- [x] Все 50 числовых обещаний разобраны (20 новых за этот виток), у каждого
      рядом написано, чем оно держится.

### ДЕСЯТЫЙ ПРОХОД (17.09.2026): описания инструментов врали модели

- [x] **`crm_ingest_chats` говорил «максимум 200», зажим допускал 2000**
      (`157863a0f`). Владелец просил ВСЕ диалоги — это написано под самой
      константой. Описание параметра читает МОДЕЛЬ: устаревшее число там —
      потолок, которого никто не ставил и которого не видно в журналах
      (автоматический путь бота уже просил 2000, придержана была только
      модель). Оба числа теперь из констант; окна `crm_waiting` — тоже
      (вчера я их назвал и тут же оставил рукописные копии в описаниях).
      Две мутации убиты, третья выжила НАМЕРЕННО — объяснено в тесте.
- [x] Форма 31 в скилле: «описание инструмента, которое врёт модели».
      Признак — число в описании и `clamp`/`Math.min` рядом в обработчике.
- [ ] ПРОВЕРЕНО и НЕ требует правки: `tg_dialogs` (50/200) и `hive_journal`
      (20/200) — числа написаны дважды, но в соседних строках одной функции.

### ДЕВЯТЫЙ ПРОХОД (17.09.2026): обещания разобраны до нуля

- [x] **`tri promises`: 0 неразобранных, 25 разобрано** (`d75c01fae`).
      Настоящих находок три: пауза перехвата обещана ДВАЖДЫ (держали одну);
      окно «ответа нет 3 дня» жило в ТРЁХ копиях (теперь имя
      `NO_ANSWER_AFTER_DAYS`, и фраза для модели собирается из него);
      «тихо 2–8 недель» против сегмента 14..60 дней — правил ТЕКСТ, не окно.
- [x] **Каскад в плейбуке помечен как НЕ ДЕРЖИМЫЙ** и объяснён на месте:
      модель просят считать напоминания (через 2 дня, потом через 5, стоп
      после двух), а не считает их никто. Это фича (вид касания или счётчик
      у лида), и она решает, что уходит клиенту, — поэтому названо, а не
      сделано тихо. ⚠️ КАНДИДАТ В РАБОТУ, если владелец скажет.
- [x] **Инструмент начал врать — и это поймано мутацией.** Послабление «смотри
      15 строк вверх» пряло НОВОЕ обещание под чужой пометкой. Подъём теперь
      кончается на первой строке настоящего кода. Записано в скилл: любое
      послабление в инструменте проверяется той же обратной мутацией, что и
      код; «стало меньше шума» — не результат.

### ВОСЬМОЙ ПРОХОД (17.09.2026): форма 30 стала командой

- [x] **`tri promises`** (`6e4fa0b58`) — ищет фразы, за которыми стоит число из
      другого файла. Руками я находил такие пары три витка; команда нашла
      ЕЩЁ ЧЕТЫРЕ за один прогон, и все — в БРИФАХ: окно отказа названо модели
      в двух описаниях `crm_leads` и дважды в плейбуке. Сторож их не читал.
      Это хуже, чем фраза владельцу: он бы заметил расхождение, модель — нет,
      а решает, кого предлагать, именно бриф. Сторож теперь покрывает все
      ШЕСТЬ мест.
- [x] Протокол разбора тот же, что у `tri owners`: строка
      `// promise-checked: <чем держится>` рядом, и промис уходит из списка.
      Сегодня: разобрано 6, открыто 22.
- [ ] ОСТАЛОСЬ 22 открытых обещания — дешёвая половина: сроки рендеров,
      «1 звезда» у админа, Robokassa. Там число живёт в той же строке или в
      соседней функции, но пройти и пометить стоит: тогда любое НОВОЕ будет
      видно сразу.

### СЕДЬМОЙ ПРОХОД (17.09.2026): числа на кнопках и обещание новичку

- [x] **Лимит на кнопке = лимит очереди** (`91c72a92d`). Кнопка обзора
      «🔥 Горячие (10 из 47)» берёт 10 из РЕНДЕРА (`segmentCaps` в сводке), а
      очередь после нажатия — из БОТА (`PRESET_CAPS`). Две таблицы в двух
      сервисах, между ними не было ничего. Сегодня сходятся; сторож держит и
      пинит два сегмента, которые бот НЕ ставит в очередь намеренно:
      `objection` — «hand only» словами самого рендера, `warm` — только
      пакетом. Три мутации убиты.
- [x] **Обещание новичку помещается в грант** (`e6acc9213`). Промпт говорит
      «фото, рилс, озвучку — делай сразу, стартовые токены есть». Фразу держат
      ТРИ числа в трёх файлах: грант 20, цены 2/2/12, список в промпте. Видео
      (40) в списке нет намеренно — это тоже пришпилено. Три мутации убиты.
      Проверено попутно: грант реально доезжает до клиента в личке — путь
      списания зовёт `ensureTokenRow` до дебета.
- [x] Проверено и НЕ требует правки: лимиты на кнопках берутся из сводки в
      рантайме (дрейф невозможен); живых «цен словами» в боте нет.

### ШЕСТОЙ ПРОХОД (17.09.2026): цены словами и ворота рендера

- [x] **Цена рилса в ПРИМЕРЕ промпта теперь из прайса** (`797769c30`). У
      промпта есть строка, которой запрещено гадать (`tokenLine()` из
      `TOKEN_PRICES`), а правило проактивности рядом несло пример с числом
      внутри. Цены выводятся из себестоимости провайдера — поднял провайдер,
      и промпт противоречит сам себе. Модель копирует примеры.
      ⚠️ ГЛАВНОЕ ЗДЕСЬ — НЕ ПРАВКА, А СТОРОЖ: две мутации ВЫЖИЛИ (литерал
      сегодня даёт тот же текст; смена цены двигает оба конца), поэтому
      свойство не «в тексте 2», а «число ВЗЯТО из прайса» — и его видно
      только в исходнике. Исключение из формы 24, записано в скилл.
      Рядом — поведенческое правило: в промпте не бывает цены, которой нет в
      `TOKEN_PRICES` (мутация «за 7 токенов» убита им).
- [x] **Набор тестов рендера — в воротах перед push** (`f8123badb`). Неделя
      работы (164 файла, 1638 тестов) не проверялась перед push НИЧЕМ:
      `related-tests` ходит по графу импортов ботовского дерева. 11 секунд,
      все зелены — замерено до того, как запирать. Весь набор БОТА по-прежнему
      не заперт намеренно: 69 файлов красны на main.
- [ ] ЗАМЕЧЕНО: единственные живые «цены словами» — в промпте. В боте таких
      строк нет (проверено grep-ом по `за N токен*` и `N ⭐`).

### ПЯТЫЙ ПРОХОД (17.09.2026): путь клиента и обещания словами

- [x] **Сроки, которые бот обещает, сверены с кодом** (`b9f28ddeb`). Три пары,
      две из трёх — в РАЗНЫХ сервисах: «молчу 30 минут» ↔ `OWNER_TAKEOVER_MS`,
      «30 дней не трогаем» ↔ `REFUSAL_HOLDS_DAYS`, «вернётся через две недели»
      ↔ окно `later`. Между ними не было ничего. Сегодня сходятся все пять
      (включая кнопку подтверждения и БРИФ МОДЕЛИ — там тоже «отказ за 30
      дней»); сторож держит. Три мутации убиты, по одной на константу.
- [x] Окно «позже» получило имя `LATER_RETURNS_AFTER_DAYS` — сторожу нужен был
      якорь. Соседние (warm 14..60, winback 30) НАМЕРЕННО безымянны: за ними
      не стоит обещаний.
- [x] Прочитан путь «карточка → нажатие → ответ»: карточка честна (показывает
      до 3000 знаков и прямо говорит «показано N из M — отправится целиком»),
      три ответа после нажатия различают «ушло / не ушло / связь прервалась».
      Дефектов не найдено — записываю как проверенное, а не как сделанное.

## Что уже есть (замерено, не на глаз)

`apps/vibee-editor/render/src/agent/crm-tools.ts`, 367 строк, три инструмента:
`crm_overview`, `crm_hot_leads`, `crm_winback`. Видимость — через
`hive/roles.ts` (`visibilityOf`, `botFilter`): каждый владелец видит только
своих людей. Тесты: `crm-tools.test.ts`.

Живой замер продакшена 08.09.2026 (через `/mcp`, ключ из Railway):
2380 человек, 327 платящих (13.7%), пришли за 7 дней — **7**, за 30 — **49**.

## ГЛАВНОЕ СЛАБОЕ МЕСТО (найдено 08.09.2026)

**CRM не пишет ничего.** `grep -c "INSERT|UPDATE|CREATE TABLE" crm-tools.ts` = **0**.
Ни одной таблицы про касание лида во всём репозитории.

Что это значит на практике: открыл горячих лидов, написал пятерым — завтра те
же пятеро снова в списке, потому что ничто не помнит, что им уже писали.
Пишешь второй раз. Это самый быстрый способ получить блокировку аккаунта и
раздражить клиента.

Форма дефекта та же, что была у `tg_send`: одна половина сделана хорошо и
поэтому выглядит законченной. Отчёт — не CRM.

## План (декомпозиция)

- [x] **1. Память касаний.** СДЕЛАНО, PR #2171. `crm_touches` в Railway
      Postgres, `crm_touch` (запись), `crm_history` (что уже делали),
      `crm_hot_leads` откладывает тронутых за 30 дней и НАЗЫВАЕТ число.
      8 мутаций убиты. Новый код целиком по-английски, в отдельном файле
      `crm-touch-tools.ts` — старый `crm-tools.ts` на русских именах, и
      смешивать нельзя.
- [x] **2. Кто ждёт меня.** СДЕЛАНО. `crm_waiting`: ours (ответил, мы молчим —
      первым), theirs, due. Считается ТОЛЬКО из касаний, сессия Telegram не
      нужна.
- [x] **3. Стадии.** СДЕЛАНО. `crm-stages.ts`: стадия выводится из денег и
      касаний, руками не ставится. Деньги перебивают всё; отказ перебивает
      всё, кроме денег; «купил» без платежа в леджере — НЕ клиент.
- [x] **5. Экран CRM в мини-аппе.** СДЕЛАНО. `/crm`, идёт через те же
      инструменты `/mcp` — второго источника правды нет. Текст в словаре,
      ключи сервера переводятся один раз в `lib/crm.ts`.
- [x] **4. Стратегия продаж.** СДЕЛАНО. `crmPlan.ts` + `crmProactive.ts`:
      продавец сам предлагает день — очередь карточек, одна за раз, нажатие
      владельца двигает её дальше. Максимум в день ограничен сегментами
      (`crm-segments.ts`, лимиты на сегмент), не рассылкой.

> ⚠️ 16.09.2026: здесь лежали ДУБЛИ пунктов 2, 3 и 5 — те же задачи ещё раз,
> но непомеченными. `tri loop` показывал их как «не сделано», и виток крона
> взялся бы переписывать готовое. Дубли удалены. Урок общий: в этом файле
> пункт живёт в ОДНОМ экземпляре, статус меняется на месте, второй копии
> задачи быть не должно.

## Границы (не нарушать)

- Никакой массовой рассылки. Одно сообщение = одно подтверждение кнопкой.
  Механизм уже есть: `tg-proposals.ts` + карточка в боте + одноразовый секрет.
- Записывать касание — можно (это не действие наружу). Отправлять — только
  через подтверждение.
- Видимость только через `hive/roles.ts`. Второй источник владения разойдётся
  с первым.

## Известные грабли этого репозитория

- `no-cyrillic` сторож не видит внутрь **регулярок** — кириллица в `/.../`
  проходит мимо строковых литералов и блокирует коммит. Используй `.includes()`.
- prettier снимает кавычки с ключей объекта, и `{ 'тип': … }` превращается в
  `{ тип: … }` — сторож ловит. Нужен маркер `cyrillic-ok` на строке.
- Сообщение коммита — только ASCII (`no-cyrillic-in-message`).
- **`tsc -p tsconfig.json` в плеере НИЧЕГО не проверяет** (`files: []`,
  project references). Настоящая: `tsc -p tsconfig.app.json`. Я на этом
  пропустил 4 настоящие ошибки типов.
- `npx vitest` на node 18 падает с ERR_REQUIRE_ESM. Всегда
  `export PATH="$HOME/.nvm/versions/node/v20.19.0/bin:$PATH"`.
- GitHub Actions мертвы по биллингу — красные галочки в PR это НЕ твоя правка.
  Сигнала от CI нет вовсе, проверяй локально.
- Railway деплоит только `main`; бот — на любой push, render — только при
  изменениях в `apps/vibee-editor/render/**`.

## Две живые базы — важно

Замер 08.09.2026 (`tri crm-source`, команда добавлена в этом витке):

    users        railway   2376 (6d)    supabase   2380 (1d)    свежее: supabase
    payments_v2  railway  17139 (12d)   supabase  17140 (0d)    свежее: supabase

`users` и `payments_v2` лежат ДВАЖДЫ. Бот пишет в облачный Supabase; копия в
Railway — застрявшая миграция, платежи отстают на 12 дней. `crm-tools.ts`
читает Supabase, и сегодня это ПРАВИЛЬНО. Не переводи CRM на Railway, пока
`tri crm-source` не скажет, что свежее там: иначе инструмент продаж покажет
позапрошлую выручку.

Владелец сказал «у нас все на railway» — по инфраструктуре так и есть, но по
этим двум таблицам замер говорит иначе. Новое (`crm_touches`) кладём в Railway.

## Что не удалось

- **WebSearch не работает** (08.09.2026): «There's an issue with the selected
  model (glm-4.5-air)». Исследование конкурентов по вебу в этом витке не
  выполнено. Не выдумывай его результаты — либо повтори попытку, либо честно
  напиши, что не сделано.

## Виток 2 — 08.09.2026, ночь (владелец спит)

Сделано и в `main` (PR #2171, задеплоено, проверено на проде: 4 инструмента CRM
в `/mcp`, `crm_waiting` отвечает, чанк `Crm-*.js` в бандле, оба сервиса 200):

- `crm_waiting` — кто ждёт ответа: ours / theirs / due, ours первым.
- стадии из фактов (`crm-stages.ts`): деньги > отказ > касания; «купил» без
  платежа — НЕ клиент.
- экран CRM в мини-аппе `/crm`, через те же инструменты `/mcp` (один источник
  правды о видимости).

В PR #2215 (ветка `feat/personal-seller`, проверка перед мержем идёт):

- **личный продавец** `crm_offer`: @username → id через сессию владельца →
  Stars-инвойс НА id лида (`tokens:N:<lead>`, бот уже зачисляет по нему) →
  питч со ссылкой → proposal → карточка «Отправить/Отмена» в боте.
- минтинг вынесен в `token-invoice.ts`; касса мини-аппа делегирует ему.
- подтверждённая отправка пишет касание `written` на сервере.

Решение по продукту (владелец не спрашивался — спал; сказано в отчёте):
«оплатить в личке» = ССЫЛКА на инвойс бота, потому что пользовательский
аккаунт инвойс выставить не может. Доставка услуг прямо в личку (агент
генерирует и отправляет в DM) — следующий виток, не этот.

Первое письмо @pilot_client: **я его не отправляю.** Владелец в боте пишет
«предложи @pilot_client 50 токенов под рилсы» → карточка → «Отправить».
@pilot_client нет в 30 последних диалогах владельца — разговора ещё не было.

Найдено чужое и починено по пути: тест `robokassa-result-url-is-mounted`
падал на любой машине с `.env` (dotenv возвращает удалённые переменные при
реимпорте) и блокировал пуш; проходил только в CI. Пустая строка вместо
удаления — dotenv присутствующие не трогает.

WebSearch по конкурентам — не повторял в этом витке. Всё ещё не сделано.

Следующий виток, по убыванию цены:

1. Доставка услуги в личку: лид в DM просит «сделай фото» → агент
   генерирует и шлёт (с подтверждением владельца). Это и есть «все услуги
   через личную переписку».
2. Имя адресата в карточке вместо числового id (tg_dialogs отдаёт title).
3. `requireOwner` vs новый экран входа: клиент подключил Telegram и не может
   ничем пользоваться.
4. Пять читающих инструментов текут сокетами (6 клиентов, 0 disconnect).

### Два `tri`, и между ними не было моста (найдено в витке 2)

Корневой `tri` (3855 строк) и `bin/tri` (649) — два CLI с одним именем.
Корневой не знал НИ ОДНОЙ команды из `bin/tri`: `crm-source` из витка 1,
`hive`, `feed` — всё отвечало «unknown command». Команды добавлялись честно и
были достижимы только по полному пути. Теперь корневой пробует `bin/tri` и
только на код 64 («не моё») говорит «unknown». Проверяй новую команду через
`./tri <имя>` из корня, а не через `bin/tri`.

Проверка перед мержем PR #2215 нашла 6 дефектов в продавце (касса без базы
не продавала; отрицательный id канала становился чужим положительным; минтинг
до проверки поверхности; и др.) — все починены, 7 обратных мутаций убиты.

## Виток 3 — 08.09.2026, день (владелец проснулся и добавил задач)

Сделано и влито (main, задеплоено Railway):

- #2222 — клиенты с подключённым Telegram получили свои инструменты
  (`requireIdentity` вместо `requireOwner` в пяти читателях и в `propose`);
  пять читателей закрывают сокет (`withClient`); структурный тест искал
  `'await client()'` при коде `client(ctx)` и проходил на пустоте — починен.
  Живой замер: `tri tg-as 987654321` — «принадлежит владельцу» → «не подключён».
- #2226 — имя получателя в карточке рядом с id; отменённый/заменённый/
  истёкший/провалившийся черновик помечает свой счёт (`cancelled_at`).
  Ревизия воркфлоу (20 агентов, 11 подтверждённых) нашла два моих серьёзных:
  бот выбрасывал `display`, пересобирая черновик из пяти полей; фильтр
  `cancelled_at IS NULL` закрыл единственный путь зачисления Stars. Оба и ещё
  пять — починены в том же PR.

Открыто, стек PR (ждут ревизию/мерж):

- #2246 — услуга в личке: `crm_deliver_photo`, списание с получателя ПОСЛЕ
  нажатия и ДО отправки, точный возврат при провале, фото-карточка в боте.
- #2251 — память переписки: `crm_messages` (загрузка через сессию владельца),
  `crm_lead_context`, `crm_leads` (правило записано словами), адаптер Zep Cloud
  v2 за `ZEP_API_KEY`/`ZEP_API_URL`, плейбук продаж в системном промпте,
  `tri crm-ingest`, `tri crm-leads`.
- #2252 — обход без запроса: раз в `CRM_PROACTIVE_MINUTES` (30) бот делает
  один ход агента и кладёт карточку владельцу; удержание до нажатия; не два
  обхода сразу. Живёт в боте: токен бота рендера — не из фермы (хэши не
  совпали), кнопки с него были бы мёртвыми.

Не сделано / вопросы владельцу:

- Zep: на Railway (19 сервисов), у рендера/бота (переменные) и в Infisical
  его НЕТ. Адаптер ждёт `ZEP_API_KEY` (+`ZEP_API_URL` для self-hosted).
  Спросить, где именно он «добавлен на сервер».
- Веб-исследование (документация Zep, практики продаж в Telegram) в этой
  сессии невозможно: WebSearch и классификатор браузера идут через псевдоним
  модели, отображённый в `~/.claude/settings.json` на несуществующую glm.
  Плейбук написан из опыта, адаптер — по документированному REST v2.
- Запрос «прозрачные панели над картой» — не мой (сайт t27.ai в `~/trinity`,
  там работает другой агент); worktree, который я успел создать, удалён.

Уроки в скиллах: blind-guards формы 17–19 (одно написание; заглушка на любой
адрес; прогон мутаций, который ничего не запускал), continuous-improvement-loop
(гвард, переживший своё основание; читатели и сокеты; модели сабагентов).

### Хвост витка 3 — вечер 08.09.2026 (владелец онлайн)

Влито в main: #2246 доставка, #2251 память, #2252 обход, #2253 наша модель
в цепочке, #2255 диалект Zep CE, #2256 порог компактного набора 24k,
#2257 обход перенесён в рабочую точку входа (`src/index.ts`).

Ревизия #2246+#2251 (27 агентов, 19 подтверждённых) → 10 дефектов починены:
двойное списание за картинку (chargeLater), знак суммы в журнале, карточка
без цены для админ-получателя, вытесненный фото-черновик в журнал, тест
возврата с квитанцией≠цене; зеркало Zep только новых, служебные аккаунты
777000/support/deleted — не лиды, правая граница у корней слов, плейбук
только владельцу, 400 у Zep — не «уже есть».

Живой замер:

- `tri crm-ingest 5 20` → 5 диалогов, 1 человек, 20 в память, 20 в Zep CE
  (наш JWT принят); `tri crm-leads` → 900000002 (@pilot_client): «живая переписка»,
  next=wait — честно.
- Наша модель qwen3:1.7b: ход 68 с → 38 с после компактного набора (23 из 63
  инструментов), инструменты зовёт верно, но итоговый текст после инструмента —
  мусор («[[Подпись|can]]»). Продавцу нужна модель ≥8B; переключение назад:
  `railway variables --set AGENT_PROVIDER=zai` на vibee-render.
- Zep CE: Nemotron у NVIDIA не успевал (deadline exceeded на каждом вызове);
  LLM Zep переведён на queen-ollama по приватной сети.

Инцидент: значение ZEP_AUTH_SECRET попало в лог сессии (маскирование sed на
macOS не сработало). Секрет ротирован, файл удалён, рендер получает по ссылке.

Обход в боте: код стартует из `src/index.ts`; первая проводка в `bot.ts` час
не работала — 0 проактивных ходов. Перенесено (#2257), проверка — по логу
`[crm-proactive] started` и маркеру «[проактивный обход продавца]» в истории.

### Из бота, без терминала — 08.09.2026, вечер

#2280: `/model` (текущая модель + кнопки «Наша / Платная / Nemotron / GLM lite»;
выбор хранится на рендере в `agent_settings`, перекрывает `AGENT_PROVIDER`,
переживает рестарт; маршрут `GET/POST /api/agent/provider` — только владелец),
`/leads` (кому писать, из памяти переписки), `/sweep` (обход сейчас; карточка
приходит сюда же). Ревизия воркфлоу для #2280 не запускалась — покрыто 12
тестами и 8 обратными мутациями.

## Cycle 4 — 2026-09-08/09: the name, the cashier, full leads, context first

Merged: #2282 (DM: no tariffs, agent-first), #2284 (name from Telegram; invoice from
the person's own farm bot — bot-farm.ts by getMe), #2287 (/leads full data, /lead
brief, crm_people at ingest), #2288 (render: dmHistoryBlock on surface business,
crm-mirror.ts, POST /api/crm/mirror, ingest after tg connect, `talk` step, no
pay-first prompts), #2289 (bot: ingest on business connection, DM mirror at once,
pay button only with an invoice link, sweep prompt with talk).

Found live: the first pitch said «Ольга» (a fixture name I put in the prompt) and was
SENT by the owner's press 16:35 UTC; its link came from @t27ai_bot. Corrected card:
message 146375 (draft ec95bd68e403, «Pilot, привет!», @Pilot_Client_bot, 30/45⭐).

Owner's stance (2026-09-09): never offer payment first; continue each client's
history with full context; Zep filled immediately. Lessons in memory:
fixture-name-leaked-into-prompt, wife-real-telegram-identity,
farm-cashier-is-the-persons-own-bot, no-pay-first-context-first.

Open: the proactive sweep's first live `talk` draft not yet observed; the pay button
and the DM mirror are verified by tests + deploy, not by a live client message.

## Cycle 5 — 2026-09-09: every dialog, the overview, a menu under every message, the seller aimed

Merged: #2294 (ingest takes every dialog: cap 200 → 2000, connect-time ingests ask for
2000×500, tg_dialogs cap 1000), #2297 (render: crm_summary — the overview; crm-touches
touchesByKind / sellerSendsSince; SELLER_NOTE_PREFIXES in crm-notes.ts, a module nothing
mocks; playbook rule 8 — selective work), #2307 "a menu under every owner message"
(crmMenu.ts grammar `crm:<verb>[:<numeric id>]` / `crm:scope:<preset>`; /crm overview
with scoped-sweep buttons; /leads and /lead keyboards; /sweep <@user | id list |
next=… stage=… signal=… days<=… days>=… paid=… limit=… | ждут | горячие | разговор |
где | stop> as a queue in crmProactive — one card, the owner's press advances, timer
pauses, MENU_HOLD_MS 10 min; DM notification menu for admin owners; pauseAiFor;
history row under proposal cards; follow-up keyboards after tgp presses; hub row under
every owner answer).

Live (crm_summary after the bot PR, 2026-09-09 03:04 UTC): 1009 people known, 855 with
messages, 38 067 messages (20 182 theirs), 125 paid, 316 waiting for the owner's reply,
10 hot; by next: reply 316, offer 7, deliver 2, talk 1, wait 529; the bot PR merged as
#2307 (main eabbc9240). Zep summaries are hallucinated by qwen3:1.7b («Led
Zeppelin» in the wife's summary) — decision on Zep's model left to the owner.

Designs came from two workflows (menus: 10 agents; overview+scope: 7 agents), judged
and synthesized; implementation inline, then reverse mutations (5 caught) and a booted-
Telegraf press test.

Lessons: other sessions merge to main concurrently (#2291–#2296 in an hour) — branch
from fresh main and print push output in full; python edit anchors drift after
prettier — brace-aware replacement must start at the BODY brace, not the return type's;
the no-cyrillic guard blocks Russian in comments, regex literals and Cyrillic object
keys (prettier strips quotes) — use English comments, string `.includes`, and
`Object.fromEntries` for Cyrillic aliases.

Open: the first live scoped sweep (`/sweep ждут`) not yet observed; the pay button and
DM mirror verified by tests and deploy only; crm_leads / scope selections see the top
50 by score — a bigger base needs paging.

## Cycle 6 — 2026-09-09: three Explorers, and the one link that prepares

The owner asked for an Explorer over the SKILLS, in the style of `#/specs` and
bound to the t27 specs; then the same for CRM clients; then for the crons on
Railway, "systematised"; and that only he may see his own data.

Shipped in `gHashTag/trinity` PR #973 (branch
`feat/explorer-tabs-skills-clients-crons`): three tabs on one shared shell
(`explorerTheme`, `ExplorerHeader`, `ExplorerLibrary`, `useHashParams`).

- `#/skills` — 26 published skills of the two PUBLIC repositories, each with
  its `.t27` spec. Forward link declared (`specs:` frontmatter, or the site's
  `bindings.json`); backward link derived at index time and never written into
  a spec. A declared reference that stops resolving fails the build;
  `link-baseline.json` caps the unbound count and may only come down.
- `#/crons` — 33 scheduled jobs read out of the source: 6 Inngest crons, 14
  long-lived timers (bot + render), 12 GitHub Actions schedules, 1 Railway cron
  service. The two schedules on the bot repository are marked as not firing.
- `#/clients` — the owner's CRM. Not in the navigation, `noindex`, no data of
  its own: live `/mcp` calls, refused server-side for anyone but the owner.

Bot side, PR on `feat/crm-prep-deep-link`: `/start crm-prep-<id>` from an admin
in a private chat runs the same `prepare` the menu button runs, so the console's
single action prepares a card and sends nothing; plus `crm-owner-gate.test.ts`,
which asserts every tool in all five CRM registries refuses a stranger BEFORE
touching the database (the pool throws on any query, so read-then-check would
show up as a database error rather than a refusal).

Decisions taken, both reversible with one line and both stated in the PR: the
private bot repository's 45 skills are withheld from the public site (the site
republishes only what is already public); ten trinity skills carry a hardcoded
home directory and were published as they are (already public on GitHub) with a
follow-up task raised to fix them at the source.

A four-agent workflow built and then adversarially verified the two catalogs.
The verification earned its cost: three of ten spec bindings were plausible but
FALSE (a directory-name coincidence, a homonym, and the wrong registry), eight
classes of lying manifest passed both gates, and four guard rails in the cron
scan excused timers by file rather than by line. All fixed.

Lessons: `--experimental-strip-types` needs Node 22, the syncs run on Node 20,
and there is no Chrome on this Mac — the browser audits only run in CI, so
generated text must be marked `data-lang-exempt` by construction rather than
discovered by a local audit. A contract that greps for a literal header name
also catches the comment explaining why the header is banned; keep the rule
literal and reword the comment. And a display bug survives every contract: the
console printed `summary.people` and `summary.messages` when the tool returns
`people_known` and `messages.total` — only reading the tool's own return
statement caught it.

Open: BotFather `/setdomain t27.ai` (owner's action) before the Login Widget
door works; the agent-key door works today. Batch mode (a warm batch under one
press) is designed and not started.

## Цикл 7 — 15–16.09.2026: Telegram API 2026, и пять задач эпика

Владелец: «изучи глубже апи телеграм и новые обновления за этот год и подумай
как еще улучшить crm», затем — делать по одной.

**Обстановка.** За 2026 год вышло СЕМЬ версий Bot API: 9.4 (09.02) … 10.3
(24.08). Telegraf у нас 4.16.3 и знает 7.1 (март 2024) — но это почти нигде не
преграда: сырые вызовы (`businessBotService.ts:408`), сырое middleware
(`:801-823`) и сквозной payload уже работают. Ни один пункт эпика не потребовал
поднимать версию.

**Разбор:** 14 агентов, 25 идей, скептики убили 16. Эпик — issue #2416,
девять дочерних #2418–#2426.

**Сделано (PR #2414, ветка `fix/reel-text-and-file-reading`):**

- [x] Язык ответа (`bfbffd8a4`). Строка «Отвечай по-русски» лежала в ОБЩЕМ
      хвосте `SYSTEM_TEMPLATE`, поэтому клиент в бизнес-личке получал русский
      ответ на любом языке. Источник языка — его собственные слова, НЕ
      `language_code` (это локаль телефона; русскоязычный с английским
      интерфейсом стал бы получать английский).
- [x] Погашенные карточки #2418 (`ace97435b`). `DisabledButton` (10.3).
      ВАЖНО: `disabled` нельзя вместе с `callback_data` — справочник требует
      ровно одно поле-тип. Иначе Telegram отвергает кнопку на каждом нажатии,
      а запасной путь делает отказ неотличимым от старого поведения.
- [x] Подписки #2419 (`dc6d740ef`, `ff34f39b0`, `b81c3f189`). Период только
      2592000, потолок 10000 звёзд. payload `subtokens:<n>:<id>` — без него
      продление не зачисляется. `BotSubscriptionUpdated.state` — canceled /
      active / **failed** (последнее теплее отмены). Защита от двух
      параллельных подписок одному человеку.
- [x] Стриминг ответа владельцу #2420 (`be2cb1f92`). `sendMessageDraft`.
      Клиенту в бизнес-личке НЕ стримим: сырые дельты несут маркеры кнопок.
- [x] Касание `replied` #2426 (`a2b1290de`). Пишется в `mirrorNow` — одна
      воронка. Окно свежести 2 часа: обход не должен штамповать сегодняшнюю
      дату на ответах трёхнедельной давности.
- [x] Управляемые боты #2421 (`545726c15`). Токен читается ОДИН раз, хранится
      в `managed_bots`, ферма его пока НЕ читает (решение владельца).
- [x] Обрыв витка #2423 (`a4a6d4542`). Маршрут рвёт виток при закрытии сокета;
      проверка перед моделью и перед КАЖДЫМ инструментом.
- [x] `tri loop` / `loop-note` / `mutate` / `api` (`46735d1d3`).

**Осталось из эпика:** #2422 (топик под очередь), #2424 (`deleteBusinessMessages`),
#2425 (отсев scam/fake в обходе).

## Конкуренты (замер 16.09.2026, страницы взяты curl-ом)

Четыре разных рынка, и ни один не занимает нашу клетку целиком.

1. **Конструкторы ботов** — salebot.pro, puzzlebot.top, BotHelp. Продают
   КОНСТРУКТОР: холст сценариев, каналы (MAX, Telegram, Авито, VK, WhatsApp,
   Viber, TikTok). Puzzlebot прямо сейчас продаёт «опиши задачу словами — AI
   соберёт бота». Слабость против нас: клиент приходит В БОТА, а не к
   человеку; у бота брендинг сервиса.
2. **Мессенджеры → CRM** — textback.ru, Wazzup, Radist. Продают ТРУБУ в
   amoCRM/Битрикс: рассылки WhatsApp, «работа с базами 100к+», WABA. У
   TextBack уже есть «AI Agent — агент для продаж». Слабость: воронку двигают
   руками, карточки тащит человек.
3. **AI SDR** — artisan.co (Ava), 11x.ai (Julian/Alice, $70M+ от a16z и
   Benchmark). Продают ОБЪЁМ: автономный исходящий поток, почта + звонки,
   enterprise. Прямо противоположны нашему правилу «владелец жмёт кнопку».
4. **CRM с ИИ сверху** — Kommo (ex-amoCRM): «AI CRM for sales & messaging
   automation».

**Где мы одни.** Продавец живёт в ЛИЧНОМ аккаунте владельца (Telegram
Business) — клиент пишет человеку, а не боту с чужим логотипом. Факты CRM
ВЫВОДЯТСЯ (деньги + касания), а не проставляются руками. Ничего не уходит
клиенту без нажатия. И генерация (фото/видео/рилсы) продаётся тем же
леджером, что и CRM, — у перечисленных этого нет вовсе.

**Где мы слабее, честно.** Нет визуального холста сценариев (у Puzzlebot он
уже собирается словами). Один канал — Telegram; у Salebot и TextBack их
шесть-семь. Нет дашбордов воронки. Нет команд и ролей (у Kommo есть). Цены
конкурентов НЕ проверены: страницы тарифов рисуются скриптом, curl отдаёт
пустое — выдумывать числа не стал.

- 16.09.2026 01:05: Сторож по тексту исходника пишется под ОТФОРМАТИРОВАННЫЙ код: prettier перенёс длинную строку при коммите, и тест покраснел на вёрстке при верном поведении. В регулярке ставить \s\* там, где форматтер может разорвать строку.

## Виток 19 (16.09.2026 06:34 UTC): работа, которая не могла доехать

**Главная находка — про сам цикл, а не про CRM.** Новый инструмент
`tri landing` замерил ветку, в которую я коммитил восемнадцать витков подряд:

```
ветка: fix/reel-text-and-file-reading
впереди origin/main: 91; позади: 132
из них действительно нет в origin/main: 91 (уже наверху другим хешем: 0)
файлов есть только здесь: 47
  · bin/helpers/{mutate,wired,scan,owners,promises,waiting,clock,tg-api}.sh
конфликтов при слиянии origin/main сюда: 20 файлов
```

Девять помощников `tri` существовали в одном месте, куда не заглядывает ни
человек, ни следующий виток крона. Слить ветку КАК ЕСТЬ нельзя: она везёт
наверх старые копии 20 файлов, которые main менял без неё.

Число конфликтов считала команда, а не глаз: первый ручной подсчёт дал 84 —
`git merge-tree --name-only` после списка имён печатает ещё и свой рассказ про
Auto-merging. Честных конфликтов 20. Разница между «84» и «20» — это разница
между «трогать нельзя» и «работа на один виток».

### Что сделано

- [x] `tri clock` теперь гонит набор ДВАЖДЫ (сегодня и со сдвигом) и печатает
      только разницу. Замер: бот — 6815 тестов, 5 красных файлов до и после
      сдвига на 90 дней. **В дереве бота не гниёт ничего.** Прогон бота
      наследует корневой конфиг, а не копирует его.
- [x] `tri landing` — доедет ли работа ветки до main и что застрянет с ней.
- [x] **PR #2430**: девять инструментов `tri` + `landing`, срезано от
      СЕГОДНЯШНЕГО main, без продуктового кода. `bin/tri` взят патчем
      `git apply -3`, а не копией: на main была команда `app-t27-smoke`,
      которой нет в ветке, и копия молча откатила бы её.
- [x] **PR #2432**: в `render/src/agent/tools.ts` на main лежал настоящий
      байт 0x00 (разделитель хеша, записанный байтом вместо `'\0'`). Сервис
      работал — в хеш попадает тот же байт, дайджесты сверены. Но `grep`
      объявлял ДВОИЧНЫМИ все 2888 строк реестра инструментов агента, и файл,
      решающий какие инструменты предложены модели, был невидим для каждой
      проверки на shell. Сторож `noControlBytesInSource.test.ts` написан ДО
      правки и упал на этом самом файле.
- [x] `tri promises` читает с `-a` и вслух говорит, когда grep отвечает про
      себя. До этого он молча писал «не разобрано: 0», не прочитав файл.
- [x] Формы 34 (работа, которая не может доехать), 35 (один байт слепит все
      проверки на shell), 36 (фон нельзя пересказывать как находку) — в скилле
      blind-guards. Порядок витка «ветка от origin/main» — в скилле цикла.

### Порядок следующего витка

1. `tri landing` ПЕРВЫМ делом. Отстал больше 30 — новое сюда не класть.
2. Работа витка — `git worktree add -b <имя> <путь> origin/main` + три
   симлинка node_modules (корень, render, player).
3. Маленький PR в конце витка.

### Ждёт слова владельца (не трогаю)

- **#2428** — фикстура лидов; main КРАСЕН без неё (проверено сегодня в свежем
  worktree: `crm-memory-tools.test.ts`, 'waiting' вместо 'hot').
- **#2429** — деньги в проде: возврат кредита и счёт, закрытый слишком рано.
- **#2430** — инструменты (ничего продуктового, слить безопасно).
- **#2432** — нулевой байт (одна строка + сторож).
- **#2414** — НЕ сливать. Сначала влить в неё main: 20 файлов конфликтов,
  это отдельная работа на виток, и я её не начинал без слова.

## Виток 20 (16.09.2026 07:03 UTC): два ответа на один вопрос

**Замер в проде, один владелец, одна минута:** `crm_waiting` → 2 человека ждут
ответа; `crm_summary` → 316. Оба честны, из разных фактов: в `crm_touches`
ПЯТЬ строк на 2394 человека и ни одной `replied`, в `crm_messages` — 25 302
входящих. Инструмент, у которого вся работа «кто ждёт ответа», читал пустую
таблицу. Журнал ведёт модель, и она забывает.

### Сделано

- [x] **PR #2433**: `waitingOn` принимает последнее входящее и последнее
      исходящее сообщение. Ответивший, которому касание не записали, больше не
      читается как «мы написали, ответа нет» (самый дорогой случай, вывернутый
      наизнанку). Разговор вообще без касаний тоже может ждать — но только как
      `ours`: исходящее сообщение обычно бот отдаёт работу, а не догоняет.
      Владелец без подключённого Telegram получает ровно прежний ответ.
- [x] Каскад плейбука ИСПОЛНЯЕТСЯ, а не обещается: `nudgesSince` считает
      отправки после последних слов человека, `waitingOn` останавливается на
      `maxNudges` (2). Без нового вида касания, без колонки, без миграции.
- [x] Молчание старше 30 дней СОСЧИТАНО в `older_than_window`, а не выброшено.
- [x] **tri facts** — сравнивает два источника одного ответа в проде и кричит,
      когда вывод стоит на пустом журнале. Этой команды не хватало три витка.
- [x] Формы 37 (два инструмента, один вопрос, разные факты), 38 (тест зелен
      под чужим раннером), 39 (бюджет, который уже исчерпан).

### Самокритика витка

- Мои 10 тестов «прошли» под КОРНЕВЫМ раннером, который раздаёт
  `SUPABASE_URL`; у рендера свой конфиг, и из его каталога все 10 падали.
  Замер без указания, чем меряли, — не замер (форма 38).
- Первая версия `tri facts` печатала «ответ не разобран» на исправном ответе:
  heredoc — это и есть stdin. Третий раз за смену вход оказался не тем, чем я
  его считал.
- Первое описание инструмента стоило 196 токенов из двух десятых оставшихся.
  Убрал предложение, а не поднял предел (форма 39).
- Ребейз на ветку #2428 притащил два ЧУЖИХ коммита — откатил и взял
  `cherry-pick` только нужного. Ровно та опасность, о которой я писал витком
  раньше.

### Конкуренты по этой теме (страницы взяты curl-ом 16.09.2026)

Каскад напоминаний у всех — ПРОДУКТ, у нас до сегодня был предложением в
промпте.

- **TextBack**: «каскадные цепочки» по каналам (Telegram, MAX, VK, WhatsApp,
  WABA) и названные сценарии: «Напоминание о встрече», «Напоминание об
  оплате», «Сообщение при недозвоне».
- **Salebot**: «умные триггеры напоминают, возвращают и мотивируют клиентов
  на покупку», задачи и напоминания сотрудникам.
- **Artisan (Ava)**: «multi-channel sequences», «Closed lost follow-up»,
  «Event follow-up», «CRM leads reactivation», встречи на автопилоте.

**Где мы одни:** их каскады срабатывают САМИ — это и есть их обещание. Наш
останавливается на двух и не отправляет ничего без кнопки владельца.
**Где слабее:** у них шесть-семь каналов, у нас один.

## Виток 21 (16.09.2026 07:16 UTC): продавцов двое, а состояние одно

**Замер в проде:** `crm_sellers` отдаёт **двух** продавцов, и один из них —
**не владелец этого развёртывания**. В `crmProactive` три флага жили на уровне
модуля, то есть были общими на обоих:

- `lastPushAt` — после карточки ОДНОГО обход другого отвечал «карточка ещё
  ждёт нажатия владельца» про **чужую** карточку, все два часа удержания.
  Второму продавцу нельзя было продать вовсе. **Это про деньги.**
- `running` — обход одного делал остальных «занятыми»; драйверов два (таймер
  в процессе и крон Inngest).
- `ingestFailStreak` — одна отозванная сессия помечала `failed` ВСЕХ, и
  тревога называла не того человека.
- `noteResolved` — зеркало: нажатие одного снимало удержание у всех.

### Сделано

- [x] **PR #2434**: всё это — в маленькую запись на владельца. Два
      существующих теста не называли владельца и обязаны были: три новых
      случая на изоляцию, две мутации (ключ-константа и «снять удержание
      всем») убиты. `vitest related`: 151 файл, 2285 тестов.
- [x] `// owner-scope:` у `failStreak` и `carrier` — они честно процессные.
- [x] **tri owners чинен трижды** (форма 40): принимал путь и выбрасывал его;
      обходил только каталоги, поэтому одиночный файл пропускался и команда
      отвечала «чисто» про файл с пятью объявлениями; пометку разбора читал
      лишь на строку вверх. Теперь печатает «смотрю: …», отказывается от
      несуществующего пути и ищет пометку по сплошному блоку комментариев,
      останавливаясь на первой строке кода. Проверено мутацией: свежее
      непомеченное объявление ПРЯМО под помеченным всё равно в списке.
- [x] Формы 40 (проверка, пропустившая вход, отвечает «чисто») и 41
      (состояние на процесс там, где арендаторов двое).

### Самокритика

- Мой же инструмент ещё в прошлом витке печатал `let running = false` как
  ОБЩЕЕ — я прочитал это и не пошёл смотреть, что там на **main**. Находка
  лежала в выводе команды три витка.
- Первая попытка сузить обход дала «✅ чисто» на файле с пятью объявлениями:
  добавил аргумент и не проверил, что обход вообще до него доходит.

## Виток 22 (16.09.2026 07:27 UTC): лог показал бой, но не сказал, чей

**Живое подтверждение #2434 из прода.** `railway logs`, 16.09.2026 07:02:19 —
две строки обхода в ОДНУ секунду: `did=card`, следом `did=held` («карточка
ещё ждёт нажатия владельца»). Карточку сделали, и тут же следующий обход
придержали — ею.

**Но утверждать нечего:** в строке нет владельца. Это читается и как «второго
продавца держит карточка первого» (та самая поломка), и как «один продавец,
обойдённый двумя драйверами, честно придержан». Различить по логу нельзя — и
я не стал выбирать.

### Сделано

- [x] В **PR #2434** добавлено: `reportSweepOutcome` принимает владельца и
      ставит его в КАЖДУЮ строку, включая тревогу о падении. Проводка
      закрыта тестом — и это было нужно: с одним прямым вызовом удаление
      аргумента В МЕСТЕ ВЫЗОВА оставляло все тесты зелёными.
- [x] **tri sweep** — исходы обходов из прода по видам. Три отказа внутри:
      не выносит вердикт меньше чем на шести наблюдениях, вырезает имя и
      username клиента из вывода, и пустое чтение — это отказ с инструкцией,
      а не вывод о проде.
- [x] Формы 42 (команды нет — молчание неотличимо от ответа) и 43 (вердикт на
      двух наблюдениях).

### Самокритика

- `timeout` на macOS НЕ существует. `timeout 60 railway logs 2>/dev/null`
  отдал ноль строк, и я почти написал «обход в проде не запускается ни разу».
  Спасло только то, что я пошёл проверять, ПОЧЕМУ пусто.
- Первый прогон `tri sweep` напечатал зелёный вердикт на одной карточке и
  одном удержании. Свой же инструмент — и сразу форма 43.
- Коммит с цитатой лога по-русски отклонил шлюз. Правильно: сообщения — по-
  английски, и цитировать лог надо смыслом, а не буквами.
- `railway logs | head -n 3000` отдаёт 497 строк, сколько ни проси: без
  флагов CLI стримит. История — через `--since`/`--lines`.
- Лог Railway начинается с последнего деплоя (окно 48 минут). Долгая история
  обходов — в журнале улья, куда пишет `noteSweepToHive`; `tri hive` сейчас
  гоняет тесты, а не читает журнал. Это кандидат на следующий виток.

## Виток 23 (16.09.2026 07:37 UTC): 62 карточки против пяти касаний

**Четыре витка подряд я повторял «продавец отправил пять карточек за 90 дней»
и чинил всё, что могло ему мешать. Журнал улья показал обратное:**

```
2026-09-11  card 5   failed 4
2026-09-12  card 11  idle 4
2026-09-13  card 18  failed 2
2026-09-14  card 12
2026-09-15  card 12  idle 3  failed 1
2026-09-16  card 4
                     итого 62 карточки за 5,5 дней
```

**Продавец работает** — одиннадцать карточек в день. А касаний «written» в
CRM пять за всё время. Разница не в продавце: хранилище держит ОДИН черновик
на человека, и каждая новая карточка ЗАМЕНЯЕТ прежнюю, которую уже нельзя
подтвердить. Занятый день владельца = двенадцать предложений в мусор, и
каждое стоило обхода памяти и двух вызовов модели.

Не нашлось раньше потому, что я мерил логом Railway, а он начинается с
последнего деплоя: окно 48 минут при кроне раз в полчаса. «Сказать нечего»
было выводом об ИСТОЧНИКЕ, а я принимал его за вывод о продукте.

Там же видно и перекос между продавцами: **71 обход против 5** — то самое,
что чинит per-owner состояние в #2434, теперь измеренное, а не предполагаемое.
И семь падений: у провайдеров моделей кончались лимиты (zai, zai-lite),
nemotron отвечал 404.

### Сделано

- [x] **В PR #2434**: обход ЗАМЕДЛЯЕТСЯ, когда карточки не нажимают. Три
      карточки по два часа, потом 4, 8, 16, потолок сутки; нажатие
      возвращает к первому шагу. На явный holdMs (`/sweep`, очередь) не
      распространяется: попросивший карточку получает её сразу.
      Четыре мутации убиты.
- [x] **tri sweep читает журнал улья** (по умолчанию), а не лог: 76 обходов
      за пять дней вместо одного-двух. Имена клиентов вырезаются, id
      владельцев не печатаются — только порядковые номера.
- [x] Форма 44: считать надо там, где данные ХРАНЯТСЯ, а не там, где светлее.

### Самокритика

- Четыре витка я строил работу на числе «пять карточек», ни разу не спросив,
  где эти данные лежат целиком. Журнал улья существовал всё это время, и
  `tri hive` заведён ровно от болезни «журнал, в который перестали смотреть».
  В него перестал смотреть я сам.
- Мутация «нажатие не сбрасывает счётчик» СНАЧАЛА выжила: тест проверял
  следующий обход после нажатия, а тот проходит в любом случае — нажатие
  снимает и удержание. Доказательство должно смотреть на обход ПОСЛЕ
  следующего.

## Виток 24 (16.09.2026 07:56 UTC): карточка, которая молчит о том, что её заменили

Продолжение находки прошлого витка (62 карточки против пяти касаний). Что
происходит с карточкой, которую заменили: её кнопки остаются в чате живыми на
вид, а нажатие отвечает **«это действие уже подтверждено или истекло»**.
Половина этой фразы говорит, что сообщение ушло клиенту. Не ушло. При
одиннадцати карточках в день это не край, а обычное состояние.

### Сделано

- [x] **PR #2435**: причина хранится сутки рядом с владельцем и ДАЙДЖЕСТОМ
      секрета. Точную фразу («заменён новым — ничего не ушло») получает только
      тот, кто предъявил и id владельца, и секрет из кнопки. Чужой с id,
      владелец без секрета и выдуманный id получают прежнюю расплывчатую —
      она не должна выдавать, существовал ли черновик. Ограничено 200
      записями: это любезность, а не журнал.
- [x] **Путь восстановления после деплоя тоже помнит** — там это важнее
      всего: при передеплое все карточки прошлого процесса сбрасываются, и
      именно тогда человек жмёт старую. В застрявшей ветке #2414 этой части
      не было; здесь — все пять мест сброса.
- [x] **tri lost** — коммиты, до которых не достаёт ни одна ветка.
- [x] Форма 45.

### Самокритика — сегодня я чуть не потерял смену

`git reset --hard origin/main`, затем два `git cherry-pick -q`. У
`cherry-pick` **нет флага -q**: обе команды отказались, `| tail -1` показал
пустоту, HEAD уже уехал — коммит повис без ветки. **Заметил не по ошибке, а по
числу: тестов стало 1971 вместо 1977.** Правка, не меняющая число тестов,
уехала бы незамеченной, и я «чинил» бы то же самое второй раз.

Из этого: `reset --hard` — ПОСЛЕ того, как замена легла. И `tri lost` как
сеть: окно в часах, а коммит, чья тема есть на любой достижимой ссылке, —
сквошен, а не потерян (первая версия выдала двадцать давно слитых коммитов,
то есть ровно тот список, который читают один раз).

## Виток 25 (16.09.2026 08:06 UTC): 57 карточек в никуда — и строка, из-за которой их не считали

**Почему «62 подготовлено, 5 отправлено, 57 в никуда» нельзя было ни
посчитать, ни опровергнуть.** Одна строка в `reportOrphan`:

```ts
// An invoice to un-pend, or a picture already made and never sent: both
// are worth a line somewhere. A plain text draft is not.
if ((p.invoiceId === undefined && !p.media) || !orphanListener) return
```

Комментарий честен: когда это писали, слушатель был ОДИН — бухгалтерия
счетов, и текстовый черновик её не касается. Но фильтр поставили в
ИСТОЧНИКЕ. Через восемь дней понадобилось считать воронку — и источник молчал
ровно про обычную карточку, ту, которую продавец делает одиннадцать раз в
день.

### Сделано

- [x] **PR #2436**: очередь раздаёт КАЖДЫЙ ушедший черновик, фильтр переехал
      к бухгалтерии счетов, слушателей теперь набор, а не один. Новый
      `card-journal.ts` пишет событие `card-dropped`: владелец, причина, вид
      действия. НЕ клиент, не текст, не адресат — и это закреплено тестом,
      который ищет и текст, и id адресата в сериализованной строке.
- [x] Три существующих теста утверждали СТАРЫЙ контракт («черновик без счёта
      никогда не будит слушателя») — переписаны на новый, каждый сохранил
      свою настоящую защиту. Среди них `cards-survive-a-deploy`: карточка,
      истёкшая незамеченной при передеплое, не оставляла следа, а передеплой
      — ровно тот момент, когда это происходит.
- [x] `tri sweep` показывает долю умерших карточек, а пока записей нет —
      говорит **НЕИЗВЕСТНО**, а не «0 потеряно». Отсутствие записи — это
      молчание журнала, а не отсутствие события.
- [x] Форма 46: фильтр живёт у того, кому он нужен, а не там, где событие
      рождается. Один слушатель — это ещё и один фильтр, и побеждает самый
      строгий.

### Стопка PR

main ← #2428 (красная фикстура) ← #2435 (карточка говорит, почему её нет) ←
#2436 (и сколько их таких). Порядок естественный: сначала сказать владельцу,
что карточку заменили, потом считать, как часто это бывает. Но стопка из трёх
— предел; дальше резать только независимое.

## Виток 26 (16.09.2026 08:32 UTC): продавец обещает подарок, которого платформа не может сделать

**Замер в проде сейчас:** `providers_status` отвечает
«FAL — картинки: HTTP 403, User is locked. Reason: Exhausted balance».
А пункт 4 плейбука БЕЗУСЛОВНО велит модели дарить первому контакту портрет из
его же аватарки. Обещание, которого платформа не может сдержать, — живому
клиенту, от имени владельца.

### Сделано

- [x] **PR #2437**: не проба, а ПАМЯТЬ. Очевидная починка — спросить
      провайдера перед обещанием — стоит генерации: проверка FAL в
      render-server ПОСТит настоящую задачу в очередь. Теперь помним, чем
      кончилась последняя НАСТОЯЩАЯ попытка: бесплатно и честнее пинга.
      Помним только отказы провайдера (401/402/403/429, баланс, лимит,
      ключ); «промпт отклонён фильтром» и «аватарка не читается» — не повод
      лишать подарка всех. Забывается через полчаса: пополненный баланс не
      должен ждать деплоя. Когда картинок нет, пункт 4 говорит об этом и
      предлагает то, что работает: разбор задачи словами, план, честную цену.
      Четыре мутации убиты.
- [x] Формы 47 (проверка, которая стоит того, что проверяет) и 48.

### Самокритика — я потерял работу ВТОРОЙ раз, через виток после формы 45

```bash
git add -A && git commit -F …   # ОТКЛОНЁН шлюзом no-cyrillic (4 комментария)
MINE=$(git rev-parse HEAD)      # это ещё СТАРЫЙ коммит
git reset --hard -q origin/main # и работа уничтожена вместе с индексом
```

Из четырёх файлов уцелел ОДИН — тест: prettier переписал его внутри хука и тем
самым положил отдельным блобом в объектную базу. Нашёлся
`git fsck --lost-found` + grep по содержимому блобов. Остальное написал
заново.

Правило теперь с ПРОВЕРКОЙ, а не с намерением: сравнить `git rev-parse HEAD`
до и после коммита, и только потом `reset --hard`. Отклонённый хуком коммит —
нормальный исход: шлюзов пять, и я попадаю в них регулярно.

### Аномалия для владельца (не чинил сам)

`providers_status` при КАЖДОМ вызове ставит настоящую задачу в очередь FAL.
Сейчас баланс пуст и это бесплатный 403, но на здоровом балансе проверка
«работают ли картинки» рисует картинку за ваши деньги. Менять форму биллинга
проверки без вашего слова не стал.

## Виток 27 (16.09.2026 08:44 UTC): я два витка чинил не тот провайдер

**Поправка, и она важнее ошибки.** Лид-магнит рисует НЕ FAL, а Kie:
`gpt-image-2-5-flare-image-to-image`. И Kie жив:

```
GET api.kie.ai/api/v1/chat/credit → {"code":200,"data":4947.15}
```

**4947 кредитов ≈ 820 подарков** (6 кредитов за 1K img2img). Пустой баланс FAL
реален и мешает другим путям картинок, но подарок он не трогает. Поправку
записал комментарием в PR #2437 — сам механизм там верен (помнить настоящий
отказ, чей бы он ни был), неверна была причина.

### Сделано

- [x] **PR #2438**: Kie давно пингуется в `providers_status` и считается среди
      работающих, но в справочнике `PROVIDERS` (что даёт / как починить) его
      не было вовсе. Владелец видел шесть провайдеров — и не видел того, на
      котором держится его первый ход к новому клиенту. Сторож требует обе
      половины: у каждого пингуемого есть запись, у каждой записи — что даёт,
      какая переменная, как починить. Две мутации убиты.
- [x] Формы 49 (HTTP 200 с кодом 401 в ТЕЛЕ) и 50 (пингуется, но не описан).

### Замер, который стоит вашего решения

**`gpt_image_edit` — операция лид-магнита — не встречается в журнале НИ РАЗУ.**
31 движение токенов за окно: 15 `reel_render`, 15 `image_generate`, 1 возврат,
ноль подарков. Код говорит о себе то же: «no live run of this model has been
made from this code base». Главный первый ход продавца **ни разу не
выполнялся**. Проверить стоит 6 кредитов (~$0.03) ваших денег — не тратил.

### Самокритика

- Мой пробник сравнил коды ответа (200 и 200) и объявил endpoint
  неаутентифицированным. Kie отвечает 200 всегда, а настоящий код кладёт в
  тело. Сравнивать надо ТЕЛО и обязательно с заведомо неверным ключом.
- Шлюз no-cyrillic отклонил коммит ТРИЖДЫ подряд (поля справочника по-русски,
  prettier переносил строки и уводил пометку). Помогло: сначала прогнать
  prettier, потом дописать `// cyrillic-ok` к каждой строке с кириллицей.
- **Правило формы 48 сработало в тот же час, как было записано:** проверка
  «HEAD сдвинулся?» поймала отклонённый коммит и не дала сделать
  `reset --hard` поверх несохранённой работы.

## Виток 28 (16.09.2026 08:51 UTC): меня ослепила моя же починка, лежащая в неслитом PR

Искал, кто в проде подключает инструмент лид-магнита:

```
grep -rn "makeCrmDeliverTools" apps/vibee-editor/render/src  →  только тест
```

и записал «в проде не подключён вовсе». **Неправда.** Подключён — строка 2749
файла `tools.ts`, вместе с аватаркой лида (`leadPhoto: leadAvatarUrl`). В этом
файле лежит нулевой байт, и grep пропускает его целиком.

**Тот самый байт я нашёл и починил неделю назад, и починка ждёт слияния в
PR #2432.** Меня ослепила собственная незакрытая работа — это самая дорогая
цена очереди неслитых PR, какую я видел: не «фича не доехала», а «инструмент
снова мне врёт, потому что лекарство лежит в ветке».

### Сделано

- [x] **tri binary** — исходники, невидимые для grep. Замер: на свежем main
      такой файл один (`tools.ts`, строка 410), на починенной ветке — ноль.
      Команда считает и то, что НАШЛА, и то, что ПРОЧИТАЛА: «найдено ноль» и
      «прочитано ноль» выглядят одинаково, и именно этот счётчик поймал две
      ошибки в ней самой, пока я её писал.
- [x] Форма 51.

### Про лид-магнит — вывод, который пришлось переписать

Инструмент подключён, аватарка подключена, `crm_deliver_photo` есть в
компактном наборе (1477 знаков — самое большое описание там). Значит, дело не
в проводке: модель его просто **не выбирает**, предпочитая текстовую карточку
(62 штуки). Это вопрос брифа и поведения, а не сломанной трубы, и чинить его
вслепую я не стал.

### Три ловушки за час, все в самом `tri binary`

1. `grep -qU $'\x00'` объявил двоичными ВСЕ файлы: ноль внутри аргумента
   обрывает строку C, образец пустеет и совпадает с чем угодно.
2. Список файлов, переданный трубой в `python3 - <<EOF`, не дошёл: heredoc и
   ЕСТЬ стандартный ввод. Третий раз за смену.
3. Поймала обе — собственная строка «прочитано файлов: 0 — подозрительно мало».

## Виток 29 (16.09.2026 09:07 UTC): бриф отдавал выбор модели, и она выбирала одно и то же

Искал, почему лид-магнит не срабатывает. Замер **опроверг** мою гипотезу
(«очередь reply вытесняет всё»):

```
crm_leads limit 5   →  deliver 4, wait 1   (все hot)
crm_leads limit 25  →  deliver 12, reply 6, offer 4, talk 1, wait 2
подготовлено за пять дней → 62 карточки, ВСЕ текстовые
gpt_image_edit в журнале  → ноль, никогда
```

**Четверо из пяти первых кандидатов просят подарок.** Модель получала их и
писала текст — шестьдесят два раза.

Всё, что подарку нужно, на месте: инструмент подключён вместе с аватаркой
(`tools.ts:2749`), у владельца **7571 токен** при цене **12**, у Kie **4947
кредитов**, инструмент в компактном наборе с самым длинным описанием.

Оставалось одно: бриф перечислял пятерых и повторял правило «возьми первого, у
кого next не wait» — то есть **отдавал выбор модели**.

### Сделано

- [x] **PR #2439**: строку выбирает КОД (по тому же правилу, так что «кто» не
      меняется) и называет поимённо вместе с единственным инструментом шага.
      Когда все ждут — бриф так и говорит, вместо того чтобы не назвать
      никого. Три мутации убиты.
- [x] Форма 52: правило, которое модель перерешает каждый ход, — не правило.

### Честная граница

Это убирает ОДНУ причину, а не «чинит подарок». Если после слияния
`gpt_image_edit` всё ещё ноль — причина внутри `crm_deliver_photo`, и это
другой поиск. Следующий замер станет решающим именно потому, что эта причина
снята.

### Самокритика

Три витка подряд я называл причиной то, что ею не было: сначала баланс FAL
(подарок рисует Kie), потом проводку (подключена), потом переполненную очередь
reply (её нет в первой пятёрке). Каждый раз меня разворачивал замер, и каждый
раз — новый. Вывод: **сначала мерить распределение, потом строить теорию.**

## Виток 30 (16.09.2026 09:20 UTC): четыре витка, три неверные причины, одна недостающая строка

Прошёл путь подарка глазами до конца и проверил ВСЁ, до чего можно дотянуться
чтением:

| проверено                    | результат                               |
| ---------------------------- | --------------------------------------- |
| инструмент подключён         | да, `tools.ts:2749`, вместе с аватаркой |
| кошелёк владельца            | 7571 токен при цене 12                  |
| Kie (рисует)                 | 4947 кредитов                           |
| в компактном наборе          | да, самое длинное описание              |
| суточный лимит генераций     | НА ЧЕЛОВЕКА, 60 — далеко не выбран      |
| проверка `surface === 'bot'` | ПРОХОДИТ: бот по умолчанию шлёт `'bot'` |

Последнюю я почти записал в «причину недели» — и проверил вовремя:
`спроситьАгента` подставляет `surface: opts.surface ?? 'bot'`, так что обход
проходит проверку кнопки.

**Причину назвать по-прежнему нельзя** — потому что отказ возвращается модели
полем, которое она вправе проигнорировать. Она и игнорирует: пишет текст.

### Сделано

- [x] **PR #2440**: каждый отказ подарка пишется в журнал — три места
      (поверхность, нечем платить, провайдер не отдал картинку). Стадия и НАША
      причина, без человека и его слов. После деплоя вопрос закрывается
      запросом, а не четвёртой догадкой. Две мутации убиты, третья выжила
      ЧЕСТНО (локальный `catch` избыточен, `record` глотает свои ошибки) — и
      это написано в комментарии, чтобы защита не выглядела несущей.
- [x] Форма 53: отказ, который видит только модель, — это отсутствующий журнал.

### Самокритика

Четвёртый виток подряд про одно и то же, и каждый раз я называл причиной то,
что ею не было. Разница этого витка: я перестал угадывать и поставил
измеритель. Чтение кода показывает, что МОЖЕТ случиться; журнал показывает,
что случается.

## Виток 31 (16.09.2026 09:34 UTC): воронка показывала не бизнес, а пустой журнал

```
by_stage:  talking   0     при 316 ждущих ответа
           new     753     из 881 человека с настоящей перепиской
```

Никто никогда не «в разговоре», потому что касание `replied` не пишет никто.
И это ровно та картинка, которую продавец читает перед решением, а владелец —
когда спрашивает, как дела.

### Сделано

- [x] **PR #2441**: ОДНА стадия выводится из переписки — и намеренно только
      одна. Если его слово свежее нашего И свежее последнего касания, мы в
      разговоре и ход за нами: это не догадка, это то, что говорят две
      отметки времени. `written` из исходящего НЕ выводится — иначе каждая
      готовая картинка и каждый чек стали бы «подходом, на который не
      ответили». Деньги и отказ по-прежнему впереди всего. Три мутации убиты.
- [x] Факты переписки переехали из `WaitingInput` ВВЕРХ в `StageInput`: оба
      вопроса — «какая стадия» и «чей ход» — отвечались из одного пустого
      журнала. Три места вызова теперь их передают.

### Стопка

main ← #2428 ← #2433 ← #2441. Третий этаж, и это предел: дальше только
независимое.

## Виток 32 (16.09.2026 09:47 UTC): у всех конкурентов воронка — ЭКРАН, у нас — числа внутри инструмента

Замер конкурентов (страницы взяты curl-ом; читал меню и заголовки, внутренности
их продуктов мне недоступны — так и говорю):

- **amoCRM / Kommo** — «Воронка продаж», «Digital воронка», доска: _«a
  board-like view that gives you the big picture»_, «Аналитика».
- **Salebot** — CRM, «просматривайте аналитику продаж», умные триггеры
  «напоминают, возвращают и мотивируют».
- **TextBack** — каскадные цепочки по каналам, напоминания, AI Agent для
  продаж.

**У каждого воронка — первоклассный объект продукта, ЭКРАН.** У нас она есть
только числами внутри `crm_summary`, и владелец видит их, лишь если модель их
процитирует. Отсюда и главная беда недели: 63 карточки против 5 отправок пять
дней никто не видел — потому что смотреть было негде.

Честная оговорка: у трёх из трёх сайтов страницы рисуются скриптом, и curl
отдаёт в основном меню. Что именно они делают с непрожатой карточкой, по этим
данным сказать НЕЛЬЗЯ. Я сравнил то, что реально прочитал: состав меню и
заголовки — то есть какие объекты продукт считает первоклассными.

### Сделано

- [x] **tri funnel** — тот самый экран, пока его нет в интерфейсе: кто на
      каком этапе, какой следующий шаг, и утечка между «карточка готова» и
      «карточка нажата». Замер в момент написания: new 753, winback 121,
      client 5, talking 0; подготовлено 63, отправлено 5.
- [x] Команда печатает **НЕИЗВЕСТНО**, а не ноль, там, где журнал ещё не умеет
      ответить (#2436, #2440).

### Вывод для продукта

Три конкурента из трёх продают воронку как экран. Мы продаём продавца, который
работает сам, — и это сильнее, но **только если владелец видит, что именно
работа принесла**. Пока «63 против 5» живёт в терминале у меня, а не у него,
это не продукт, а мой отчёт.

## Виток 33 (16.09.2026 10:05 UTC): поправка к себе вчерашнему и утечка на экране владельца

**Сначала поправка.** Вчера я написал, что у конкурентов воронка — экран, а у
нас нет. **Неправда.** В боте есть `/crm summary`, и там уже месяцами: этапы,
шаги, сигналы, касания. Не было в нём **утечки**.

### Сделано

- [x] **PR #2443**: на экран сводки добавлены три числа из журнала улья —
      подготовлено / отправлено / умерло, и строка «подготовлено втрое больше,
      чем отправлено — теряется между карточкой и нажатием». Замер, ради
      которого это сделано: 63 против 5.
- [x] `dropped` — **null, а не 0**, пока журнал не умеет отвечать; на экране
      «неизвестно». Ноль от невключённого счётчика неотличим от нуля здоровой
      воронки (#2436 включит, #2440 добавит отказы подарка).
- [x] Второй вызов не роняет экран: сводка работала без этих чисел месяцами,
      и половина экрана лучше ошибки. Закреплено тестом.
- [x] Три мутации убиты.

### Самокритика

Вчерашнее «у нас нет экрана» — это вывод, сделанный по МОЕМУ инструменту
(`tri funnel` я писал, глядя на `crm_summary`), а не по продукту владельца. Я
не открыл бота и не посмотрел, что там уже есть. Это ровно та же ошибка, что
вся неделя: судить о продукте по тому, что видно с моей стороны.

Правило на будущее: прежде чем сказать «у нас этого нет», найди экран, где
это могло бы быть, и прочитай его код.

## Виток 34 (16.09.2026 10:22 UTC): ревизия экранов бота — и врёт УТРЕННИЙ

Обещал вчера пройти экраны глазами, как код. Прошёл: home, leads, lead,
summary, model, plan, sweep, ingest.

**Экран сводки был прав, но неполон** (починено вчера, #2443). **А утренний
план врёт прямо:**

```
Сегодня ушло: 4 из 30.
```

Первая строка, которую владелец читает каждое утро, говорит «можно ещё
двадцать шесть». А в те же дни продавец подготовил **63** карточки, каждая
сменяла прошлую ненажатую. Ограничение не в лимите — оно в том, что карточки
умирают до нажатия, и экран утверждал ровно обратное.

### Сделано

- [x] **PR #2444**: строка называет подготовленные, когда их больше
      отправленных, и молчит, когда сходится. Никаких нотаций там, где утечки
      нет.
- [x] Двойник журнала в тесте **бросает**, а не отвечает: двойник, который
      отвечает, проверил бы счастливый путь дважды и запасной — ни разу.
- [x] Формы 54 (прежде чем сказать «у нас этого нет» — прочитай экран) и 55
      (двойник, который отвечает, прячет отказоустойчивость).

### Честный отчёт по мутациям

Одна убита (строка печатается всегда). Вторая **выжила честно**: замена
значения по умолчанию с `null` на `0` ненаблюдаема — оба подавляют строку,
когда подготовлено не больше отправленного. Записал, а не подогнал тест,
который доказывал бы лишь эквивалентность мутанта.

### Признак формы 54, который стоит искать дальше

Число вида «сделано N из лимита M» на экране, где M ни разу не достигался.
Оно всегда рассказывает про запас и никогда — про потери.

## Виток 35 (16.09.2026 10:33 UTC): охота на числа про запас

Форма 54 дала конкретный признак, и я пошёл искать остальные такие строки.

### Сделано

- [x] **tri headroom** — числа, где НАША работа считается против предела,
      который поставили МЫ: «ушло N из M», «отправлено N из M». Остаток
      баланса после покупки — не то; лимит длины промпта — тоже не то: они про
      ресурс ЧЕЛОВЕКА, а не про нашу производительность.
- [x] Первая версия ловила и их: **46 кандидатов, из которых 44 в порядке.**
      Список, где почти всё норма, читают один раз — так уже провалились
      первые версии `tri owners` и `tri lost`. Сузил до глаголов делания —
      на main остаётся **ровно одна** строка, та самая из утреннего плана.
- [x] Протокол разбора как у `owner-scope` и `promise-checked`: пометка
      `headroom-ok:` рядом, и строка больше не показывается. Проверено
      сквозь: на ветке с починкой команда отвечает «разобрано».

### Честно про ценность витка

Инструмент нашёл ровно одну вещь — ту, которую я уже починил вчера. То есть
он **не принёс новой находки**, а закрепил найденную и сделал класс
повторяемым. Это меньше, чем витки с настоящими открытиями, и стоит это
признать прямо.

Но у него есть отложенная ценность: следующая такая строка, которую кто-то
добавит, будет поймана не через неделю наблюдений, а сразу.
