---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Протокол Запуска Тестов

**Принцип:** Каждое действие должно иметь ясный результат. Запуск тестов должен завершаться четким вердиктом: ✅ (Прошло) или ❌ (Не прошло).

1.  **Команда для Запуска:** Для выполнения тестов **ОДИН РАЗ** и получения немедленного результата используй команду:
    ```bash
    pnpm vitest run
    ```
    *(Примечание: Мы выяснили, что `pnpm test --run` не работает, поэтому используем `pnpm vitest run`)*
2.  **Цель Команды:** Выполнить все тесты и **завершить** процесс, выводя итоговый отчет, вместо режима наблюдения (`watch mode`).
3.  **Ожидаемый Вывод:** Анализировать **итоговый** вывод команды (количество пройденных/упавших тестов, ошибки).
4.  **Никакого Watch Mode:** Избегать запуска `pnpm test` или `pnpm vitest` без флага `run`, если не указано иное.
5.  **Связанные Файлы:**
    *   Скрипты запуска: `[package.json](mdc:package.json)`
    *   Конфигурация тестов: `[vitest.config.ts](mdc:vitest.config.ts)`

*Ом Шанти. Пусть каждый запуск тестов приносит ясный ответ.* 🙏
