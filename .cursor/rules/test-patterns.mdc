---
description: 
globs: 
alwaysApply: true
---
# Паттерны решения проблем в тестах NeuroBlogger

## 🚀 Главный принцип работы: последовательное решение паттернов

**ВАЖНО:** Когда найден паттерн решения проблемы, немедленно применять его ко всем файлам с подобной проблемой, а не переходить к новому типу проблем. Это обеспечит более эффективное исправление, так как:
1. Мы остаемся в контексте конкретного паттерна решения
2. Можем быстро прогнать все файлы с подобной проблемой
3. Избегаем повторного "вхождения" в ту же проблему позже

## 📋 Выявленные паттерны решения проблем

### 1️⃣ Параметры в функциях handleSelect* и подобных

**Проблема:** Несоответствие сигнатуры функций в тестах и реальном коде.
**Решение:** При тестировании обработчиков всегда проверять актуальную сигнатуру функции в коде.

```typescript
// ❌ Неправильно: Не передан обязательный параметр starAmounts
mockHandleSelectStars({ ctx, isRu: true })

// ✅ Правильно: Переданы все необходимые параметры
mockHandleSelectStars({ ctx, isRu: true, starAmounts })
```

### 2️⃣ Объектные параметры вместо позиционных аргументов

**Проблема:** Функции ожидают объект параметров, а не отдельные аргументы.
**Решение:** Использовать объектные параметры, проверяя актуальную сигнатуру.

```typescript
// ❌ Неправильно: Передача как позиционных аргументов
await handleSelectStars(ctx, true, someArray)

// ✅ Правильно: Передача объекта параметров
await handleSelectStars({ ctx, isRu: true, starAmounts: someArray })
```

### 3️⃣ Проверка вызова с expect.objectContaining()

**Проблема:** При проверке вызванных функций сложно проверить точное соответствие всех полей.
**Решение:** Использовать `expect.objectContaining()` для гибкой проверки объектов параметров.

```typescript
// ✅ Правильно: Проверка с использованием objectContaining
expect(mockedHandlers.handleSelectStars).toHaveBeenCalledWith(
  expect.objectContaining({ 
    ctx: subSessionCtx, 
    isRu: true,
    starAmounts 
  })
)
```

### 4️⃣ Структура mock-контекста для Telegraf

**Проблема:** Неполная структура mock-контекста приводит к ошибкам в тестах.
**Решение:** Создавать полный контекст с необходимыми полями.

```typescript
// Минимальный корректный мок-контекст
const createMockContext = () => {
  return {
    from: {
      id: 1,
      is_bot: false,
      first_name: 'Test',
      username: 'testuser',
      language_code: 'en',
    },
    chat: { 
      id: 1, 
      type: 'private', 
      first_name: 'Test',
      username: 'testuser' 
    },
    session: {
      cursor: 0,
      images: [],
      __scenes: {
        current: 'sceneName',
        state: { step: 0 },
        cursor: 0,
      },
      balance: 500, // Обязательно для многих тестов
      targetUserId: '12345',
    },
    reply: jest.fn(),
    answerCbQuery: jest.fn(),
    scene: {
      enter: jest.fn(),
      leave: jest.fn(),
    },
    update: { update_id: 1 },
  } as unknown as MyContext
}
```

### 5️⃣ Импорты и мокирование модулей

**Проблема:** Некорректные импорты и неполные моки модулей.
**Решение:** Использовать алиасы путей и правильно мокировать модули.

```typescript
// ✅ Правильный импорт с использованием алиасов
import { MyContext } from '@/interfaces'
import * as handlers from '@/handlers'

// ✅ Полное мокирование модуля
jest.mock('@/handlers', () => ({
  handleSelectStars: jest.fn(),
  handleSelectRubAmount: jest.fn(),
  // Другие функции из модуля...
}))

// ✅ Типизированный мок
const mockedHandlers = jest.mocked(handlers)
```

### 6️⃣ Вызов middleware в тестах

**Проблема:** Неправильный вызов middleware функций Telegraf.
**Решение:** Всегда передавать функцию next как второй аргумент.

```typescript
// ❌ Неправильно: Отсутствует next функция
await middleware(ctx)

// ✅ Правильно: Передан второй аргумент next
await middleware(ctx, jest.fn())
```

### 7️⃣ Типизация моков Jest

**Проблема:** Несоответствие типов при мокировании функций.
**Решение:** Использовать правильные типы для моков.

```typescript
// ✅ Правильная типизация mock-функций
const mockedFunction = jest.fn() as jest.MockedFunction<typeof originalFunction>
// или
const mockedModule = jest.mocked(originalModule)
```

### 8️⃣ Обработка read-only свойств в контексте

**Проблема:** Попытка изменить read-only свойства контекста.
**Решение:** Создавать новый контекст с нужными свойствами, а не модифицировать существующий.

```typescript
// ❌ Неправильно: Изменение read-only свойства
ctx.from.id = 12345 

// ✅ Правильно: Создание нового контекста с нужными свойствами
const newCtx = createMockContext({ fromId: 12345 })
```

### 9️⃣ Разрешение модулей в тестах

**Проблема:** Ошибки разрешения путей в тестах.
**Решение:** Использовать настроенные алиасы путей (`@/`) вместо относительных.

```typescript
// ❌ Неправильно: Относительные пути могут вызвать проблемы
import { someFunction } from '../../src/utils/helpers'

// ✅ Правильно: Использование алиасов из tsconfig.json
import { someFunction } from '@/utils/helpers'
```

### 🔟 Тестирование wizard-сцен и шагов

**Проблема:** Некорректное тестирование шагов wizard-сцен.
**Решение:** Правильно извлекать и тестировать шаги сцены.

```typescript
// ✅ Правильное тестирование шага wizard-сцены
const wizard = myWizardScene
const middleware = wizard.middleware()
const step0 = wizard.steps[0] // Получаем первый шаг

// Тестируем шаг
await step0(ctx, jest.fn())

// Проверяем результат
expect(someFunction).toHaveBeenCalled()
```

## 🔍 Дополнительные замечания

1. **Не используйте `@jest/globals`**: Избегайте импорта из `@jest/globals`, так как Jest предоставляет эти функции глобально.

2. **Избегайте `any` и приведений типов**: Используйте правильные интерфейсы и типы из основного кода.

3. **Своевременно обновляйте тесты**: При изменении интерфейсов или сигнатур функций в коде, сразу обновляйте соответствующие тесты.

4. **Используйте перечисления вместо строк**: Предпочитайте `SubscriptionType.STANDARD` вместо строк `'standard'`.

5. **Проверяйте ожидаемые аргументы функций**: Используйте `toHaveBeenCalledWith()` для подтверждения правильных аргументов.
