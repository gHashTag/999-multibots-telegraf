---
description:
globs:
alwaysApply: false
---
# 🕉️ Правило: Стиль Клавиатур (ReplyKeyboard vs InlineKeyboard)

**Принцип:** Форма должна соответствовать содержанию. Выбор типа клавиатуры влияет на поток взаимодействия пользователя.

1.  **`ReplyKeyboard` (Клавиатура Внизу):**
    *   **Использование:** Для **основных меню выбора**, где пользователь должен выбрать один из нескольких вариантов для продолжения сценария (например, выбор модели ИИ, выбор количества шагов, основные команды главного меню).
    *   **Размещение:** Всегда **внизу** чата, заменяя стандартную клавиатуру.
    *   **Реализация:** `Markup.keyboard([...]).resize()`
    *   **Кнопки:** Обычно `Markup.button.text('Текст кнопки')`.

2.  **`InlineKeyboard` (Кнопки в Сообщении):**
    *   **Использование:** Для **контекстных действий**, связанных с конкретным сообщением (например, подтверждение действия "Да/Нет", переход по ссылке, лайк/дизлайк, кнопка "❓ Справка" *после* сообщения с результатом или в сообщении с инструкцией).
    *   **Размещение:** **Внутри** сообщения бота.
    *   **Реализация:** `Markup.inlineKeyboard([...])`
    *   **Кнопки:** Обычно `Markup.button.callback(...)`, `Markup.button.url(...)`, `Markup.button.webApp(...)`.

**Цель:** Обеспечить интуитивно понятный и консистентный пользовательский интерфейс, используя правильный тип клавиатуры для каждой задачи. Не путать пользователя сменой стилей меню без необходимости.

*Ом Шанти. Пусть интерфейс будет ясным, как горный ручей.* 🙏
