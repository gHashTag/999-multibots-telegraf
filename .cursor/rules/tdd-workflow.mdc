---
description: 
globs: 
alwaysApply: true
---
# Руководство по разработке через тестирование (TDD)

Чтобы обеспечить качество кода, удобство обслуживания и сократить регрессии, мы стремимся следовать принципам разработки через тестирование:

**Структура:**
* **Расположение теста:** Все тестовые файлы ДОЛЖНЫ находиться в каталоге `__tests__` в корне проекта.
* **Зеркалирование источника:** Структура каталога в `__tests__` должна отражать структуру каталога `src`. Например, тест для `src/scenes/startScene/index.ts` должен находиться в `__tests__/scenes/startScene.test.ts`.

**Рабочий процесс:**
1. **Сначала напишите тест на провал:** Перед написанием кода реализации для новой функции или исправления ошибки напишите автоматизированный тест, который описывает желаемую функциональность и **проваливается**, потому что функциональность еще не существует. 🔴
2. **Напишите минимальный код для прохождения:** Напишите максимально простой код реализации, необходимый для успешного прохождения неудавшегося теста. Не добавляйте дополнительную функциональность сверх того, что требуется тесту. ✅
3. **Рефакторинг:** После прохождения теста выполните рефакторинг кода реализации (и, возможно, тестового кода), чтобы повысить ясность, удалить дублирование и обеспечить соблюдение принципов проектирования, при этом гарантируя, что все тесты по-прежнему пройдут. ♻️

**Основные преимущества:**
* **Уверенность:** Тесты действуют как страховочная сетка, придавая уверенность при рефакторинге или добавлении новых функций.
* **Проектирование:** Написание тестов в первую очередь способствует лучшему, более тестируемому проектированию кода.
* **Документация:** Тесты служат исполняемой документацией для того, как должен работать код.
* **Сокращение отладки:** Выявляет ошибки на ранних этапах цикла разработки.

Соблюдение TDD приводит к более надежной и надежной кодовой базе. 🙏