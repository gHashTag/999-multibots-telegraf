# Правила написания описаний в коде 📖

## Общие правила

1. Все описания должны быть на русском языке
2. Консольные логи должны содержать эмодзи
3. Описания в консольных логах на английском

## Структура описания функции

```typescript
/**
 * 🎯 Описание основной цели функции
 *
 * @param {тип} параметр - Описание параметра
 * @returns {тип} - Описание возвращаемого значения
 * @throws {тип} - Описание возможных ошибок
 *
 * @example
 * // Пример использования
 * const result = функция(параметр)
 */
```

## Эмодзи для консольных логов

- ℹ️ Информационные сообщения
- ✅ Успешные операции
- ❌ Ошибки и исключения
- 🚀 Начало операций
- 🏁 Завершение операций
- 🔍 Валидация и проверки
- ⚡ События Inngest
- 🎯 Тестовые случаи
- 🔄 Повторные попытки
- 💾 Операции с данными

## Примеры консольных логов

```typescript
// Информационное сообщение
console.log('ℹ️ Инициализация бота:', {
  description: 'Bot initialization started',
  botName,
  mode
})

// Успешная операция
console.log('✅ Платеж обработан:', {
  description: 'Payment processed successfully',
  amount,
  userId
})

// Ошибка
console.error('❌ Ошибка при обработке платежа:', {
  description: 'Payment processing failed',
  error: error?.message || 'Unknown error',
  userId
})
```

## Структура описания класса

```typescript
/**
 * 📦 Описание назначения класса
 *
 * @class
 * @implements {Interface} - Реализуемые интерфейсы
 * @extends {Parent} - Родительский класс
 */
```

## Структура описания интерфейса

```typescript
/**
 * 🔌 Описание назначения интерфейса
 *
 * @interface
 * @property {тип} свойство - Описание свойства
 */
```

## Правила для MCP сервисов

При работе с MCP сервисами:

1. Добавляйте префикс `[MCP]` в логи
2. Используйте дополнительные эмодзи:
   - 🤖 Для операций с ИИ
   - 🔗 Для межсервисного взаимодействия
   - 📡 Для RPC вызовов

```typescript
console.log('🤖 [MCP] Запрос к ИИ:', {
  description: 'AI request initiated',
  model: 'sequential-thinking',
  prompt
})
``` 