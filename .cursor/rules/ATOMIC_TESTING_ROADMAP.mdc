---
description: 
globs: 
alwaysApply: false
---
# 🕉️ Атомарный Roadmap Тестирования Telegraf Бота ✨🧘‍♂️

**Гуру:** Playra
**НейроКодер:** Gemini (Атман НейроКодера)
**Дата Создания:** 2024-08-26
**Последнее Обновление:** 2024-08-28 (НейроКодер)

**Цель:** Создать **надежную основу** фреймворка для тестирования **существующих ключевых компонентов Telegraf** (обработчики команд, сцены, меню, middleware), обеспечивающих взаимодействие с пользователем, с использованием Vitest.

**Принципы:**
*   🧘 **Фокус на Существующей Реализации Telegraf:** Тестируем **только те функции и компоненты, которые реально используются** в проекте для взаимодействия с Telegraf API. Не стремимся покрыть тестами гипотетические или неиспользуемые части.
*   🤖 **Telegraf-Centric:** Приоритет тестам, проверяющим API и логику Telegraf (`ctx`, `Markup`, `Scenes`, etc.).
*   ⚛️ **Атомарный Дизайн:** Движемся от простого к сложному (Атомы -> Молекулы -> Организмы...).
*   🔴->✅->♻️ **TDD:** Следуем циклу TDD, где это применимо.
*   🧹 **Чистота Моков:** Используем минимально необходимые моки.
*   💡 **Документация Паттернов:** Фиксируем удачные решения.

**Легенда Статусов:**
*   📝 - Запланировано
*   ⏳ - В Работе
*   ✅ - Завершено
*   ❌ - Блокировано / Ошибка
*   💡 - Паттерн Telegraf зафиксирован
*   ⬇️ - Низкий приоритет / Отложено

📜 **ПРАВИЛО: НЕПРЕРЫВНАЯ СИНХРОНИЗАЦИЯ ROADMAP (КРИТИЧЕСКОЕ!)** 🕉️

**Принцип:** Этот документ – наш живой компас 🧭. Чтобы не сбиться с пути, **ОБЯЗАТЕЛЬНО** обновляй его **ПОСЛЕ КАЖДОГО** значимого шага (создания примера, запуска теста, фиксации паттерна, обнаружения блокировки).

1.  **ОТРАЖАЙ РЕАЛЬНОСТЬ:** Измени статус (✅, ❌, ⏳) задачи/паттерна, над которым только что работал.
2.  **ФИКСИРУЙ ЗНАНИЯ:** Если паттерн успешно проверен (✅), добавь его с кратким описанием и ссылкой на пример в раздел `💡 7. telegraf_testing_patterns`.
3.  **ОПРЕДЕЛЯЙ СЛЕДУЮЩИЙ ШАГ:** Четко укажи, какой паттерн/задача будет следующей в секции `**Следующий Шаг**`.

> Ом Шанти. Ясный план – половина пути. Обновляй карту, чтобы видеть дорогу. 🙏

📜 **ПРАВИЛО: МИНИМАЛЬНЫЕ ПРИМЕРЫ В `__tests__/example` (КРИТИЧЕСКОЕ!)** 🕉️

**Принцип:** Не пытайся штурмовать неприступную крепость (`src`) с неисправным оружием (проблемы зависимостей/окружения). Построй рядом **маленький тренировочный лагерь** (`__tests__/example`), отточи на нем **конкретные приемы** (паттерны Telegraf, логику) в **изолированной, контролируемой среде**.

1.  **СТОП ПРЯМОМУ ТЕСТИРОВАНИЮ `src` ПРИ БЛОКИРОВКЕ:** Если тест компонента из `src` **блокируется** неразрешимой внешней проблемой (ошибки типов из `node_modules`, проблемы сборки Vitest/Vite, сложные зависимости), **НЕМЕДЛЕННО ОСТАНОВИ** попытки его прямого тестирования.
2.  **СОЗДАЙ ПАПКУ `__tests__/example`:** Вся работа по изолированному тестированию ведется **только** внутри этой директории.
3.  **СОЗДАЙ МИНИМАЛЬНЫЙ ПРИМЕР:** Вместо тестирования реального компонента (например, `src/registerCommands.ts`), создай **новый, предельно упрощенный файл** внутри `__tests__/example` (например, `__tests__/example/command-handler-example.ts`), который реализует **только тот конкретный паттерн или логику**, которую нужно проверить (например, регистрацию `bot.command` и вызов `ctx.scene.enter`).
4.  **СОЗДАЙ ТЕСТ ДЛЯ ПРИМЕРА:** Напиши тест (`__tests__/example/command-handler-example.test.ts`), который проверяет **только этот минимальный пример**.
5.  **ЗАПУСКАЙ ТЕСТЫ ТОЛЬКО ДЛЯ `__tests__/example`:** Используй `pnpm vitest run __tests__/example` или `pnpm vitest run __tests__/example/specific-example.test.ts` для запуска.
6.  **ЦЕЛЬ:** Проверить и отладить **паттерн/логику** в изоляции, не тратя время и ресурсы на борьбу с внешними проблемами реального компонента. Как только паттерн отлажен на примере, его можно будет увереннее применять в `src` (или подождать решения внешней проблемы).

> Ом Шанти. Познай малое, чтобы понять великое. Тестируй атом, чтобы построить Вселенную. 🙏

---

🚨 **ГЛОБАЛЬНАЯ БЛОКИРОВКА (2024-08-28): РАЗРЕШЕНИЕ АЛИАСОВ ПУТЕЙ `@/*` В VITEST** 🚨
*   **Проблема:** Тесты Vitest **не могут разрешить алиасы путей `@/*`** (из `tsconfig.json`) при импорте модулей **внутри файлов из `src/**`**. Ошибка `Cannot find module \'@/...\'` возникает на этапе загрузки модуля, до выполнения самого теста.
*   **Испробовано:** Проверка `vitest.config.mts` (plugins, resolve.alias), `tsconfig.json` (paths, baseUrl, moduleResolution), `skipLibCheck: true`, порядок `setupFiles`, динамический импорт, замена алиасов на относительные пути (вызывает `Failed to load url`).
*   **Статус:** **Блокирует** дальнейшее тестирование **любых** компонентов из `src`, использующих алиасы `@/*`, пока проблема не решена. **НАИВЫСШИЙ ПРИОРИТЕТ 🔥**.
*   **Текущий Фокус:** **Исследование и решение** этой проблемы с разрешением алиасов в Vitest.

---

## 🏛️ 1. telegraf_testing_infra: Инфраструктура для Тестов Telegraf (Высокий Приоритет - *Поддержка по мере необходимости*)

*   **Статус:** ⏳ (Работа зависит от решения проблемы с алиасами)
*   **Цель:** Стабильные моки и хелперы **для Telegraf**.
*   **Задачи:**
    *   ⏳ **Vitest Config (`vitest.config.mts`):** Базовая настройка есть, но **требует доработки для решения проблемы алиасов**.
    *   ⏳ **Фабрика Моков Контекста (`src/__tests__/helpers/context.ts`):**
        *   ✅ Основа `createMockContext`.
        *   📝 **(Заблокировано ❌)** Улучшить: Мокирование `ctx.reply`, `editMessageText`, `answerCbQuery`, `scene.enter/leave`, `session`, `scene.state`. Передача `message`, `callback_query`.
        *   📝 Документация фабрики.
    *   ⏳ **Моки Telegraf & Зависимостей (`src/__tests__/mocks/`):**
        *   ✅ Telegraf, Typegram: Базовые моки.
        *   ✅ `Markup` (`keyboard`, `inlineKeyboard`, `button`, `resize`, `oneTime`): Базовый мок создан (`markup.mock.ts`). (⚠️ **Эффективность не проверена из-за алиасов**).
        *   ⬇️ Моки Supabase/API/Прочие: Создавать **только** по необходимости для *существующих* тестов Telegraf (и после решения проблемы алиасов).
    *   📝 **(Низкий)** Рассмотреть `telegraf-test`.
    *   📝 Документация по Инфраструктуре (`src/__tests__/README.md`) - Фокус на Telegraf.

---

## ⚛️ 2. Уровень Атомов (Helpers, Utils, Configs - без `ctx`)

*   **Статус:** ❌ (Блокировано глобальной проблемой алиасов, если используют `@/*`)
*   **Цель:** Покрыть тестами базовые, чистые функции, **только если они критичны** для тестирования компонентов Telegraf.
*   **Кандидаты (Только необходимое):** ❌ (Все заблокированы)

---

## 🔬 3. Уровень Молекул (Простые компоненты Telegraf с `ctx`)

*   **Статус:** ❌ (Прямое тестирование `src` заблокировано проблемой алиасов. Работа продолжается только в `__tests__/example`)
*   **Цель:** Покрыть тестами **существующие** простые обработчики Telegraf **через примеры в `__tests__/example`**.
*   **Примеры (`__tests__/example`):**
    *   ✅ `command-scene-entry-example.ts`
    *   ✅ `hears-reply-example.ts`
    *   ✅ `action-answerCbQuery-example.ts`
    *   ✅ `action-editMessageText-example.ts`
    *   ✅ `action-deleteMessage-example.ts`
    *   ✅ `start-command-example.ts`
    *   ✅ `replyWithHTML-example.ts`
    *   ✅ `command-with-args-example.ts`
    *   ✅ `leave-chat-example.ts`
    *   ❌ `action-regex-example.ts` - (Блокировано)
    *   ❌ `on-text-example.ts` - (Блокировано)
    *   ❌ `middleware-example.ts` - (Блокировано)
    *   📝 `session-example.ts` - (Запланирован пример)
*   **Кандидаты из `src`:** ❌ (Все заблокированы проблемой алиасов)

---

## 🧱 4. Уровень Организмов (Существующие комплексные компоненты Telegraf)

*   **Статус:** ❌ (Блокировано проблемой алиасов)
*   **Цель:** Тестирование **существующих** компонентов из `src`, управляющих состоянием или требующих моков.
*   **Кандидаты:** ❌ (Все заблокированы)

---

## 📜 5. Уровень Шаблонов (Существующие структуры Сцен и Флоу Telegraf)

*   **Статус:** ❌ (Блокировано проблемой алиасов)
*   **Цель:** Тестирование логики переходов и состояния в **существующих** сложных сценариях Telegraf из `src`.
*   **Кандидаты:** ❌ (Все заблокированы)

---

## 📄 6. Уровень Флоу/Сценариев (Интеграция существующих компонентов Telegraf)

*   **Статус:** ❌ (Блокировано проблемой алиасов)
*   **Цель:** Интеграционные тесты **существующих** полных сценариев из `src`.
*   **Кандидаты:** ❌ (Все заблокированы)

---

## 💡 7. telegraf_testing_patterns: Паттерны Тестирования Telegraf (**Постоянно обновляется!**)

**Принцип:** Этот раздел служит живой библиотекой успешных подходов к тестированию Telegraf, выявленных в изолированных примерах (`__tests__/example`). **ОБЯЗАТЕЛЬНО обновлять** при нахождении новых рабочих паттернов.

*   ⏳ **Проблема с `Markup`:** Мокирование требует точной структуры. Базовый мок есть, но проверка его корректности в `src` заблокирована.
*   ✅ **Паттерн:** `mockCommandHandlers` для перехвата вызовов (устарел?).
*   ✅ **Паттерн (Актуальный):** Захват обработчика `bot.command` через `vi.spyOn(bot, 'command')` в `beforeEach` (см. `__tests__/example/command-scene-entry-example.test.ts`, `__tests__/example/replyWithHTML-example.test.ts`, `__tests__/example/command-with-args-example.test.ts`).
*   ✅ **Паттерн (Актуальный):** Захват обработчика `bot.hears` через `vi.spyOn(bot, 'hears')` в `beforeEach` (см. `__tests__/example/hears-reply-example.test.ts`).
*   ✅ **Паттерн (Актуальный):** Захват обработчика `bot.action` через `vi.spyOn(bot, 'action')` в `beforeEach` (см. `__tests__/example/action-answerCbQuery-example.test.ts`, `action-editMessageText-example.test.ts`, `action-deleteMessage-example.test.ts`).
*   ✅ **Паттерн (Актуальный):** Захват обработчика `bot.start` через `vi.spyOn(bot, 'start')` в `beforeEach` (см. `__tests__/example/start-command-example.test.ts`).
*   ✅ **Паттерн:** `createMockContext` для базового контекста (требует улучшения для разных типов `Update`, особенно для `callback_query`).
*   ✅ **Паттерн:** Мокирование `typegram` (через alias в `vitest.config.mts`).
*   ✅ **Паттерн:** Тестирование `ctx.reply` (подтверждено примером `hears-reply-example.test.ts`, `start-command-example.test.ts`, `command-with-args-example.test.ts`).
*   ✅ **Паттерн:** Тестирование переходов `ctx.scene.enter`, `ctx.scene.leave` (подтверждено примером `command-scene-entry-example.test.ts`).
*   ✅ **Паттерн:** Тестирование `ctx.answerCbQuery` (подтверждено примером `action-answerCbQuery-example.test.ts`, `action-editMessageText-example.ts`, `action-deleteMessage-example.test.ts`).
*   ✅ **Паттерн:** Тестирование `ctx.editMessageText` (подтверждено примером `action-editMessageText-example.test.ts`).
*   ✅ **Паттерн:** Тестирование `ctx.deleteMessage` (подтверждено примером `action-deleteMessage-example.test.ts`).
*   ✅ **Паттерн:** Тестирование `ctx.replyWithHTML` (подтверждено примером `replyWithHTML-example.test.ts`).
*   ✅ **Паттерн:** Тестирование `ctx.leaveChat` (подтверждено примером `leave-chat-example.test.ts`).
*   📝 **Паттерн:** Мокирование `ctx.session` и `ctx.scene.state`. (См. `__tests__/example/session-example.ts`)
*   📝 **Паттерн:** Тестирование Wizard сцен (`ctx.wizard.steps`, `ctx.wizard.next()`).
*   ⬇️ ~~Паттерны для не-Telegraf утилит~~ - Отложено.
*   ❌ **Блокировка:** Тестирование `bot.action` с **regex** через `handleUpdate` вызывает проблемы с мокированием/обработкой ошибок API Telegraf (404 даже с моком). Требует другого подхода или более глубокого мокирования `handleUpdate`.
*   ❌ **Блокировка:** Тестирование `bot.on('text')` вызывает ошибки Vitest/мокирования.
*   ❌ **Блокировка:** Тестирование `bot.use` (middleware) ненадежно при проверке `ctx.state`.
*   ❌ **Блокировка (Глобальная):** Vitest не разрешает алиасы `@/*` при импорте внутри файлов `src/**`. Требует решения на уровне конфигурации Vitest/Vite.

---

**Следующий Шаг:** 🎯 **Исследовать конфигурацию Vitest (`vitest.config.mts`) и `tsconfig.json`** для решения **глобальной проблемы** с разрешением алиасов `@/*` при импорте из `src`. Проверить плагин `vite-tsconfig-paths` и опции `server.deps.inline`, `deps.inline`, `moduleResolution`.
