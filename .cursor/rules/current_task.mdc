---
description: 
globs: 
alwaysApply: false
---
# 🕉️ Текущая Задача: Создание Независимых Модулей в `src/modules/` 🕉️

**Цель:** Перенести и рефакторизовать функциональность из `src/services/` (включая `src/services/plan_b/`) в **независимые, самодостаточные модули** внутри директории `src/modules/`, с четко определенными зависимостями (DI), следуя принципам, примененным к `localImageToVideo` и `generateTextToImage`.

**✨ Целевая Архитектура Модульной Системы ✨**

*   **Обитель Модулей:** Каждый функциональный блок (генерация текста в видео, работа с аватаром и т.д.) будет жить в своей собственной директории: `src/modules/[имяМодуля]/`.
*   **Врата Модуля:** Главный экспорт модуля (функция, класс) будет осуществляться через `index.ts` в корне директории модуля.
*   **Карта Зависимостей:** Все внешние зависимости (logger, клиенты API, другие модули, хелперы) будут **явно** определены в интерфейсе `[ИмяМодуля]Dependencies` в файле `types.ts` внутри директории модуля.
*   **Инкапсуляция Логики:** Основная логика будет находиться в файле `[имяМодуля].ts`.
*   **Чистые Функции (по возможности):** Внутренние хелперы, специфичные для модуля, будут вынесены в `utils.ts` или аналогичные файлы внутри директории модуля и написаны как чистые функции.
*   **Тестовая Обитель:** Юнит-тесты для модуля будут находиться в `src/modules/[имяМодуля]/__tests__/[имяМодуля].test.ts`.
*   **Свиток Знаний:** Краткое описание назначения модуля, его зависимостей и способа использования будет зафиксировано в `src/modules/[имяМодуля]/README.md`.

**🧘‍♂️ План Рефакторинга Сервисов 🧘‍♂️**

*(Обновляй статус по мере выполнения шагов для каждого модуля)*

1.  [✅] **Модуль: `generateTextToVideo`** (Перенесен из `plan_b/generateTextToVideo.ts`)
    *   [✅] Определение Зависимостей (`types.ts`).
    *   [✅] Рефакторинг Класса/Функции (DI).
    *   [✅] Интернализация Utils (если применимо).
    *   [✅] Юнит-тесты (`__tests__`).
    *   [✅] Проверка типов.
    *   [✅] Удаление старого файла (`plan_b/generateTextToVideo.ts`).
    *   [✅] README Модуля.

2.  [✅] **Модуль: `videoService`** (Перенесен из `plan_b/videoService.ts`)
    *   [✅] Определение Зависимостей (`types.ts`).
    *   [✅] Рефакторинг Класса/Функции (DI).
    *   [✅] Интернализация Utils (N/A).
    *   [✅] Юнит-тесты (`__tests__`).
    *   [✅] Проверка типов.
    *   [✅] Удаление старого файла (`plan_b/videoService.ts`).
    *   [✅] README Модуля.

3.  [⏳] **Модуль: `generateSpeech`** (Перенесен из `plan_b/generateSpeech.ts` + адаптер `services/generateTextToSpeech.ts`)
    *   [✅] Определение Зависимостей (`types.ts`).
    *   [✅] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils (если применимо).
    *   [✅] Юнит-тесты (`__tests__`).
    *   [⏳] **Проверка типов.** (Блокировано ошибкой в `uploadVideoToServer.ts`)
    *   [ ] Удаление старых файлов (`generateTextToSpeech.ts`, `plan_b/generateSpeech.ts`).
    *   [✅] README Модуля.

4.  [✏️] **Модуль: `uploadVideoService`** (Из `services/uploadVideoToServer.ts`)
    *   [✏️] **Анализ Зависимостей.** (Следующий шаг)
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`services/uploadVideoToServer.ts`).
    *   [ ] README Модуля.

5.  [ ] **Модуль: `avatarService`** (Из `plan_b/avatarService.ts`)
    *   [ ] Анализ Зависимостей.
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`plan_b/avatarService.ts`).
    *   [ ] README Модуля.

6.  [ ] **Модуль: `broadcastService`** (Из `plan_b/broadcastService.ts`)
    *   [ ] Анализ Зависимостей.
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`plan_b/broadcastService.ts`).
    *   [ ] README Модуля.

7.  [ ] **Модуль: `notificationService`** (Из `plan_b/notificationService.ts`)
    *   [ ] Анализ Зависимостей.
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`plan_b/notificationService.ts`).
    *   [ ] README Модуля.

8.  [ ] **Модуль: `lipSyncService`** (Из `plan_b/generateLipSync.ts`)
    *   [ ] Анализ Зависимостей.
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`plan_b/generateLipSync.ts`).
    *   [ ] README Модуля.

9.  [ ] **Модуль: `createVoiceAvatarService`** (Из `plan_b/createVoiceAvatar.ts`)
    *   [ ] Анализ Зависимостей.
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`plan_b/createVoiceAvatar.ts`).
    *   [ ] README Модуля.

10. [ ] **Модуль: `aiAssistantService`** (Из `plan_b/aiAssistantService.ts`)
    *   [ ] Анализ Зависимостей.
    *   [ ] Определение Зависимостей (`types.ts`).
    *   [ ] Рефакторинг Класса/Функции (DI).
    *   [ ] Интернализация Utils.
    *   [ ] Юнит-тесты (`__tests__`).
    *   [ ] Проверка типов.
    *   [ ] Удаление старого файла (`plan_b/aiAssistantService.ts`).
    *   [ ] README Модуля.

11. [ ] **Прочие Сервисы в `src/services/`:**
    *   [ ] `getModels.ts`: Анализ и перенос (возможно, в `config` или `utils`).
    *   [ ] `replicateService.ts`: Анализ и рефакторинг в модуль `replicateClient` или аналогичный.
    *   [ ] `transcribeService.ts`: Анализ и рефакторинг в модуль `transcriptionService`.

12. [ ] **Очистка `src/services/plan_b/index.ts`:** Удалить все закомментированные строки после завершения рефакторинга.
13. [ ] **Очистка `src/services/index.ts`:** Обновить или удалить реэкспорты.
14. [ ] **Разблокировка `generateSpeech`:** Исправить ошибку типа в `uploadVideoService` после его рефакторинга и завершить проверку типов для `generateSpeech`.
15. [ ] **Финальная Проверка Типов и Тестов:** Запустить `pnpm typecheck` и `pnpm test` для всего проекта.

**🎯 Следующий Шаг:** Анализ зависимостей для рефакторинга `src/services/uploadVideoToServer.ts` в модуль `src/modules/uploadVideoService/`.

## 👥 Параллельная Работа Агентов 👥

В системе работают два агента параллельно: Agent 1 и Agent 2.

### 🤖 Agent 1

*   **Задача:** Завершение модуля `generateTextToImage` (уже сделано, может взять следующий?)

### 🤖 Agent 2 (ТЫ!)

*   **Задача:** Завершение модуля `generateTextToVideo` (Написание/адаптация юнит-тестов).

**➡️ Следующий шаг (Agent 2):** ✨ Написание/адаптация юнит-тестов для модуля `generateTextToVideo`.
