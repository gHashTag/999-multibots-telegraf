---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Текущая Задача: Создание Независимых Модулей в `src/modules/` 🕉️

**Цель:** Перенести и рефакторизовать функциональность из `src/services/plan_b/` в **независимые, самодостаточные модули** внутри директории `src/modules/`, с четко определенными зависимостями (DI), следуя принципам, примененным к `localImageToVideo`.

**✨ Целевая Архитектура Модульной Системы ✨**

*   **Обитель Модулей:** Каждый функциональный блок (генерация текста в видео, работа с аватаром и т.д.) будет жить в своей собственной директории: `src/modules/[имяМодуля]/`.
*   **Врата Модуля:** Главный экспорт модуля (функция, класс) будет осуществляться через `index.ts` в корне директории модуля.
*   **Карта Зависимостей:** Все внешние зависимости (logger, клиенты API, другие модули, хелперы) будут **явно** определены в интерфейсе `[ИмяМодуля]Dependencies` внутри файла `types.ts` модуля.
*   **Нектар Внедрения (DI):** Зависимости **не будут** импортироваться напрямую внутри модуля. Вместо этого они будут **передаваться снаружи** как аргумент основной функции модуля или в конструктор класса (принцип Dependency Injection).
*   **Внутренний Мир:** Утилиты и хелперы, используемые **только** этим модулем, будут находиться внутри его директории (например, `src/modules/[имяМодуля]/utils/`).
*   **Лотос Независимости:** Модули должны быть максимально самодостаточными, не зависящими от глобального контекста (как `ctx` Telegraf) или специфических деталей других модулей, кроме как через четко определенные интерфейсы зависимостей.
*   **Храм Инстанцирования (DI Контейнер - Будущее):** Позже мы создадим центральный механизм (фабрику или контейнер), который будет отвечать за сборку модулей и внедрение в них необходимых зависимостей.

**Ветка:** `refactor/modular-system` (Слита с `recover`, конфликты разрешены)

**Статус Слияния:** ✅ Все конфликты разрешены, проверка типов (`bun run typecheck`) пройдена.

## 📝 Общий План Создания Модулей

1.  [✅] **Подготовка:** Слияние `recover`, разрешение конфликтов, проверка типов.
2.  [✏️] **Создание Модулей в `src/modules/`:** ✨ (По одному)
    *   [✏️] **Модуль: `generateTextToImage`** (`src/modules/generateTextToImage/`)
        *   [✅] Анализ Зависимостей (из `src/services/plan_b/generateTextToImage.ts`).
        *   [✅] Определение Интерфейса Зависимостей (`GenerateTextToImageDependencies` в `src/modules/generateTextToImage/types.ts`).
        *   [✅] Рефакторинг Функции (перенос и адаптация `generateTextToImage.ts` в модуль, использование DI).
        *   [✅] Интернализация Utils (перенос `determineImageSize` в `src/modules/generateTextToImage/utils/`).
        *   [✅] Конфигурация (проверка передачи `imageModelsConfig` через DI).
        *   [✅] Создание Структуры Модуля (перемещение файлов, создание `index.ts`).
        *   [✅] Обновление Вызовов (в тестах `generateTextToImage.test.ts`).
        *   [✅] Тестирование Модуля (юнит-тесты).
        *   [✅] Проверка типов (`bun run typecheck`).
        *   [✅] Удаление старого файла (`src/services/plan_b/generateTextToImage.ts`).
        *   [✅] README Модуля (`src/modules/generateTextToImage/README.md`).
    *   [✏️] **Модуль: `generateTextToVideo`** (`src/modules/generateTextToVideo/`)
        *   [✅] Анализ Зависимостей.
        *   [✅] Определение Интерфейса Зависимостей.
        *   [✅] Рефакторинг Функции.
        *   [✅] Интернализация Utils.
        *   [✅] Конфигурация.
        *   [✅] Создание Структуры Модуля.
        *   [✅] Обновление Вызовов.
        *   [✏️] Тестирование Модуля.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла.
        *   [ ] README Модуля.
    *   [ ] **Модуль: `videoService`** (`src/modules/videoService/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `generateSpeech`** (`src/modules/generateSpeech/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `createVoiceAvatar`** (`src/modules/createVoiceAvatar/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `generateImageToPrompt`** (`src/modules/generateImageToPrompt/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `generateImageToVideo`** (`src/modules/generateImageToVideo/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `generateLipSync`** (`src/modules/generateLipSync/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `aiAssistantService`** (`src/modules/aiAssistantService/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `notificationService`** (`src/modules/notificationService/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `broadcastService`** (`src/modules/broadcastService/`)
        *   ... (Аналогичные шаги)
    *   [ ] **Модуль: `avatarService`** (`src/modules/avatarService/`)
        *   ... (Аналогичные шаги)
3.  [ ] **Реализация DI Контейнера/Фабрики:** 🏭 (После создания нескольких модулей) Создать механизм для удобного инстанцирования модулей с их зависимостями.
4.  [ ] **Финальная проверка типов:** `bun run typecheck` 🕉️
5.  [ ] **Тестирование:** ✅ Запуск всех тестов (`vitest run`).
6.  [ ] **Коммит:** 💾 Фиксация модульной системы.
7.  [ ] **Обновление Документации:** ✍️ Обновить `README.md` проекта и модулей.

---

## 👥 Параллельная Работа Агентов 👥

В системе работают два агента параллельно: Agent 1 и Agent 2.

### 🤖 Agent 1

*   **Задача:** Завершение модуля `generateTextToImage` (создание README).

### 🤖 Agent 2 (ТЫ!)

*   **Задача:** Создание независимого модуля `broadcastService` в `src/modules/`.

## 📝 Задача для Agent 2: Модуль `broadcastService`

**Цель:** Перенести и рефакторизовать функциональность из оригинальной реализации `broadcastService` в **независимый, самодостаточный модуль** `src/modules/broadcastService/`, с четко определенными зависимостями (DI), следуя принципам, примененным к `generateTextToImage`.

**План:**

1.  Анализ Зависимостей (из оригинальной реализации `broadcastService`).
2.  Определение Интерфейса Зависимостей (`BroadcastServiceDependencies` в `src/modules/broadcastService/types.ts`).
3. Рефакторинг Функции (перенос и адаптация кода в модуль, использование DI).
4. Интернализация Utils (перенос любых применимых utils в `src/modules/broadcastService/utils/`).
5. Конфигурация (убедиться, что конфигурации передаются через DI).
6. Создание Структуры Модуля (перемещение файлов, создание `index.ts`).
7. Обновление Вызовов (обновить использования в тестах).
8. Модульное Тестирование (юнит-тесты).
9. Проверка типов (`bun run typecheck`).
10. Удаление старого файла (оригинальная реализация `broadcastService`).
11. Создание README Модуля (`src/modules/broadcastService/README.md`).

**➡️ Следующий шаг (Agent 2):** ✨ Анализ зависимостей оригинальной реализации `broadcastService`.

**➡️ Следующий шаг:** ✨ Определение интерфейса зависимостей (`GenerateTextToVideoDependencies` в `src/modules/generateTextToVideo/types.ts`).

**➡️ Следующий шаг:** ✨ Рефакторинг функции `generateTextToVideo` для переноса в модуль `src/modules/generateTextToVideo/` и использования DI.

**➡️ Следующий шаг:** ✨ Интернализация утилит для модуля `generateTextToVideo` (если есть).

**➡️ Следующий шаг:** ✨ Проверка конфигурации для модуля `generateTextToVideo` (передача `videoModelsConfig` через DI).

**➡️ Следующий шаг:** ✨ Создание структуры модуля `generateTextToVideo` (перемещение файлов, создание `index.ts`).

**➡️ Следующий шаг:** ✨ Обновление вызовов функции `generateTextToVideo`.

**➡️ Следующий шаг:** ✨ Написание/адаптация юнит-тестов для модуля `generateTextToVideo`.
