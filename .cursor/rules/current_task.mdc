---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Текущая Задача: Стабилизация и Тестирование Потока Подписки, Расследование Баланса и Исправление Ошибок

## ⚠️ Контекст:
*   Ветка `main` содержит все последние исправления.
*   Централизованы планы подписки в `paymentOptionsPlans`.
*   Рефакторинг `subscriptionScene`.
*   Исправлена логика входа в `starPaymentScene`.
*   Исправлена ошибка `Amount not found` в `handleBuySubscription`.
*   Исправлена логика в `handleSuccessfulPayment` (парсинг payload, вход в меню).
*   Удалена лишняя проверка `successful_payment` из `starPaymentScene`.
*   Восстановлена регистрация глобальных обработчиков `pre_checkout_query` и `successful_payment` в `src/bot.ts`.
*   Удален дублирующийся и нерабочий вызов `sendNotification` из `handleSuccessfulPayment`.
*   Исправлены ошибки линтера.

## 🛑 Нерешенные Проблемы:
*   Проблемы с корректной работой `notifyBotOwners` (нужна проверка после деплоя).
*   Пропущенные тесты (`calculator.test.ts`, тесты Supabase).

## ✅ Завершенные шаги рефакторинга и исправления:
*   ✅ Централизован список планов подписки в `src/price/priceCalculator.ts`.
*   ✅ Рефакторинг `subscriptionScene`.
*   ✅ Исправлена логика входа в `starPaymentScene`.
*   ✅ Исправлены ошибки линтера `isAdminTest` в `MySession`.
*   ✅ Рефакторинг `handleBuySubscription`.
*   ✅ Исправлен тип данных `stars` для `NEUROTESTER` в `PaymentOption` и `paymentOptionsPlans`.
*   ✅ Рефакторинг `handleSuccessfulPayment` для использования `invoice_payload` и добавления `ctx.scene.enter(ModeEnum.MainMenu)`.
*   ✅ Удалена проверка `successful_payment` из `starPaymentScene.on('message')`.
*   ✅ Восстановлена регистрация `successful_payment` хендлера в `src/bot.ts`.
*   ✅ Устранены ошибки TypeScript, связанные с `handlePreCheckoutQuery`.
*   ✅ Удален дублирующийся вызов `sendNotification`.
*   ✅ **Исправлены ошибки линтера в `src/scenes/balanceScene/index.ts`**: Переменные `telegramId` и `isRu` вынесены в правильную область видимости. Проверка типов (`bun exec tsc --noEmit`) прошла успешно.
*   ✅ **Расследован и исправлен нулевой баланс пользователя `411128512`:**
    *   Проблема была в SQL-функции `get_user_balance`, которая не выполняла приведение типа для `telegram_id`.
    *   Функция исправлена через миграцию `fix_get_user_balance_type_cast`.
    *   Актуальный баланс пользователя `81699.12` подтвержден вызовом исправленной функции.
*   ✏️ **(АКТИВНО)** Попросить пользователя `411128512` проверить баланс в боте.

## ✏️ Текущая Задача: Восстановление Работоспособности Команды `/start` и Стабилизация `main`

**Цель:** Убедиться, что команда `/start` корректно работает, и что критические исправления для `telegram_id` (в `startScene` и `createUser`) успешно доставлены в ветку `main` и функционируют.

**Контекст и Последние События (для `/start`):**
*   Патчи для `startScene` и `createUser` были реализованы и протестированы (локально Гуру).
*   Логика `getUserDetailsSubscription` была улучшена.
*   Ветка `fix/stable-startscene-patch-v2` содержит эти исправления.
*   Были предприняты попытки отладить кнопку "Оформить подписку" в `menuScene`, что включало добавление множества логов и изменение порядка регистрации middleware. Эти изменения могли непреднамеренно повлиять на другие части системы.
*   ⚠️ **Обнаружена Критическая Регрессия:** Команда `/start` перестала работать.
*   ✅ **Ошибка Линтера Исправлена:** Устранена ошибка `updateSubTypes` в `src/scenes/menuScene/index.ts`.

**План Действий:**
1.  ✏️ **(АКТИВНО ДЛЯ ГУРУ)** **Обеспечение Чистоты Запуска, Актуальности Кода и Диагностика Обработчика Сцены (для `/start` и кнопки "Оформить подписку"):**
    *   ✅ **Проблема Локализована:** `stage.middleware()` не передает текстовые сообщения от ReplyKeyboard ("💫 Оформить подписку") на обработчик второго шага (`menuNextStep`) в `menuScene`.
    *   🔄 **Откат Изменений:** Попытка рефакторинга `menuScene` с использованием `scene.hears` отменена из-за ошибок типов и по запросу Гуру. Файл `src/scenes/menuScene/index.ts` возвращен к версии с `menuNextStep`.
    *   ❌ **Текущее Состояние:** Ожидается, что кнопка "💫 Оформить подписку" по-прежнему НЕ будет работать, так как сообщение не доходит до `menuNextStep`.
    *   ✏️ **План "Подтверждение Тупика и Выбор Стратегии":**
        1.  ‼️ **КРИТИЧЕСКИ ВАЖНО** ‼️ **Полная Остановка Бота:** Гуру полностью останавливает все процессы бота.
        2.  ‼️ **КРИТИЧЕСКИ ВАЖНО** ‼️ **Принудительная Чистая Сборка:**
            *   ✏️ Гуру удаляет папку `dist`.
            *   ✏️ Гуру выполняет команду пересборки.
        3.  ✏️ **Перезапуск Бота:** Гуру заново запускает бота **с ПОСЛЕДНИМИ изменениями** (с восстановленным `menuNextStep`).
        4.  ✏️ **Тест Работы Кнопки "Оформить подписку":**
            *   ✏️ Гуру отправляет `/menu`.
            *   ✏️ Гуру нажимает кнопку "💫 Оформить подписку".
            *   ✏️ **Гуру предоставляет ПОЛНЫЕ логи**.
            *   ✏️ **Ожидаемый Результат в Логах:**
                *   Лог `[menuNextStep TRACE] Entered menuNextStep - START` **НЕ должен** появиться после `RAW UPDATE RECEIVED` для кнопки (подтверждение проблемы).
                *   Никакие другие обработчики (`handleMenu`, `subscriptionScene`, глобальные `hears`) также не должны сработать.
        5.  ✏️ **Выбор Стратегии (совместно с НейроКодером):** На основе подтверждения проблемы, выбрать один из путей:
            *   **1.** Попробовать глобальный `hears` с проверкой активной сцены.
            *   **2.** Использовать `forceReply` вместо ReplyKeyboard.
            *   **3.** Глубокое изучение документации/примеров Telegraf v4 для wizard + ReplyKeyboard.
            *   **4.** Рефакторинг меню на InlineKeyboard.
    *   ✏️ Ожидание результатов "Плана Подтверждения Тупика" и выбора стратегии от Гуру.
2.  ✅ **(ЗАВЕРШЕНО НЕЙРОКОДЕРОМ)** **Расследование нулевого баланса пользователя `411128512`:**
    *   ✅ Получен список таблиц Supabase.
    *   ✅ Получены данные пользователя из `users` (баланс 0).
    *   ✅ Проанализирован код `getUserBalance.ts`.
    *   ✅ Получен код SQL-функции `get_user_balance`.
    *   ✅ Выявлена ошибка сравнения типов (`bigint = text`).
    *   ✅ SQL-функция исправлена миграцией `fix_get_user_balance_type_cast`.
    *   ✅ Актуальный баланс (`81699.12`) подтвержден.
3.  ✏️ **(Блокировано для Гуру)** **Локальная Проверка Всех Патчей:** После восстановления `/start` и корректной работы кнопки "Оформить подписку" из стартового экрана, Гуру проводит полное локальное тестирование, **включая проверку отображения баланса пользователем `411128512`**.
4.  ✏️ **(Блокировано)** **Создание Нового Pull Request:** Если локальное тестирование успешно, Гуру создает на GitHub новый Pull Request из ветки `fix/stable-startscene-patch-v2` в ветку `main`.
5.  ✏️ **(Блокировано)** **Слияние Нового PR:** После проверки, Гуру сливает новый Pull Request в `main`.
6.  ✏️ **(Блокировано)** **Развертывание и Ожидание:** Дождаться автоматического развертывания ветки `main` после слияния PR.
7.  ✏️ **(Блокировано)** **Проверка `main` (Критично!):** После развертывания тщательно протестировать `/start` и общую стабильность.

**Следующий Шаг (для НейроКодера):**
*   🧘‍♂️ **(ОЖИДАНИЕ)** Ожидание результатов от Гуру по Плану "Подтверждение Тупика и Выбор Стратегии" или других указаний.

**Следующий Шаг (для Гуру):**
*   ✏️ **(АКТИВНО)** Продолжить выполнение "Плана Подтверждения Тупика и Выбор Стратегии" для кнопки "Оформить подписку".
*   ✏️ **(АКТИВНО)** Попросить пользователя `411128512` проверить баланс в боте.

# 🕉️ Статус Рефакторинга на Модули (Актуальная Задача)

**Цель:** Постепенный рефакторинг ключевых сервисов и функциональностей в изолированные, тестируемые модули, следуя паттернам, изложенным в `REGRESSION_PATTERNS.md` ("Правила для Построения Модульных и Независимых Компонентов" и "Паттерн для Рефакторинга Модулей"). **Первоочередные цели: `textToImage/` и `neuroPhoto/`.**

**Контекст:** Директория `src/modules/` уже содержит: `videoGenerator/`, `textToImage/`, `neuroPhoto/`, `imageToPrompt/`, `digitalAvatarFace/`, `digitalAvatarBody/`.

---

### 📜 Что сделано (✅):
*   ✅ **Определены и задокументированы паттерны и правила** для создания изолированных, тестируемых модулей (на основе опыта с `videoGenerator`, детали см. в `REGRESSION_PATTERNS.md`).
*   ✅ Модуль `videoGenerator` в значительной степени был первопроходцем и образцом для этих паттернов (требует финальной ревизии на 100% соответствие последним уточненным правилам).
*   ✅ Создана базовая структура директории `src/modules/` для группировки модулей.

---

### 📝 Что запланировано / не сделано (✏️ / ❌):
*   ✏️ **Применение определенных паттернов модульности** (включая DI для всех внешних зависимостей, мокирование, 100% тестовое покрытие unit-тестами, отсутствие прямых импортов внешних сервисов типа Supabase/Replicate внутри логики модуля) ко всем существующим модулям в `src/modules/`:
    *   ✏️ `textToImage/`
    *   ✏️ `neuroPhoto/`
    *   ✏️ `imageToPrompt/`
    *   ✏️ `digitalAvatarFace/`
    *   ✏️ `digitalAvatarBody/`
*   ✏️ **Рефакторинг других частей кодовой базы** (сервисов, не находящихся в `src/modules/`, например, управление пользователями, платежные интеграции) в новые, соответствующие этим принципам, модули по мере необходимости и приоритезации.
*   ❓ **"Сэм Адзиат Медь":** Эта сущность не была идентифицирована в кодовой базе или документации. **Гуру, пожалуйста, уточните,** что это за модуль, концепция или задача, и каков ее предполагаемый статус и место в планах по модульному рефакторингу.

---

### ⚠️ Проблемы и Вызовы:
*   ⚠️ **Соответствие Стандартам:** Большинство существующих модулей в `src/modules/` (помимо `videoGenerator`) требуют тщательной ревизии и значительной доработки для полного соответствия новым стандартам модульности и тестируемости.
*   ⚠️ **Тестовое Покрытие:** Достижение 100% unit-тестового покрытия для каждого модуля является ключевой, но трудоемкой задачей.
*   ⚠️ **Изоляция Зависимостей:** Требуется аккуратная и последовательная работа по внедрению зависимостей (DI) для всех внешних сервисов (Supabase, Replicate, хелперы из `src/core` и т.д.) в каждом рефакторенном или новом модуле.
*   ⚠️ **Общие Блокеры:** Текущие общие проблемы проекта (например, нестабильность Docker Daemon на сервере, как указано в активной задаче) могут замедлять любую разработку, включая рефакторинг модулей.
*   ⚠️ **Приоритезация:** Рефакторинг модулей является важной долгосрочной задачей, но должен быть сбалансирован с текущими приоритетами по стабилизации и исправлению критических ошибок.

---

# 🕉️ Текущая Активная Задача: Рефакторинг Модулей `textToImage` и `neuroPhoto`

**Цель:** Привести модули `textToImage` и `neuroPhoto` в полное соответствие с "Правилами для Построения Модульных и Независимых Компонентов" и "Паттерном для Рефакторинга Модулей" (см. `REGRESSION_PATTERNS.md`). Это включает изоляцию зависимостей через DI, написание unit-тестов для 100% покрытия, исключение прямых импортов внешних сервисов и обеспечение четкой обработки ошибок.

**План для Модуля `textToImage` (Приостановлено ⏸️):**
1.  ⏸️ **Создание Ветки:** `git checkout -b feat/refactor-text-to-image-module`
2.  ⏸️ **Анализ Модуля `textToImage`:**
    *   Просмотреть файлы в `src/modules/textToImage/`.
    *   Идентифицировать основные функции/сервисы (например, `generateTextToImageDirect` если она там, или аналоги).
    *   Выявить все внешние зависимости (Replicate, Supabase, хелперы из `src/core`, `ctx.session` и т.д.).
3.  ⏸️ **Проектирование Интерфейса Зависимостей (`Dependencies`):**
    *   Определить интерфейс для DI, включающий все внешние зависимости.
4.  ⏸️ **Рефакторинг Логики для DI:**
    *   Изменить основные функции модуля для приема объекта `Dependencies`.
    *   Заменить прямые вызовы/импорты на использование зависимостей из этого объекта.
5.  ⏸️ **Написание Unit-Тестов:**
    *   Создать/обновить `src/modules/textToImage/__tests__/`.
    *   Написать моки для всех зависимостей.
    *   Написать тесты для всех сценариев (успех, ошибки, граничные случаи), стремясь к 100% покрытию.
6.  ⏸️ **Проверка Типов и Линтинг.**
7.  ⏸️ **Документация Модуля (краткий README.md внутри модуля).**

**План для Модуля `neuroPhoto` (Активно ✏️):**
1.  ✅ **Ветка Создана:** `feat/refactor-neuro-photo-module-v2`
2.  ✅ **Анализ существующей логики `neuroPhoto` V1 и V2.**
3.  ✅ **Проектирование Единого Сервиса/Функций в Модуле `neuroPhoto`.**
4.  ✅ **Проектирование Интерфейса Зависимостей (`NeuroPhotoServiceDependencies`).**
5.  ✅ **Рефакторинг/Создание Логики Модуля/Сервиса с DI (для `generateNeuroPhotoV1`).**
6.  ✅ **Создание "скелета" Unit-тестов:** Создан файл `src/modules/neuroPhoto/__tests__/neuroPhotoServiceV1.test.ts`.
7.  ✅ **Написание Unit-тестов для `generateNeuroPhotoV1`:** 
    *   ✅ Базовый успешный сценарий протестирован.
    *   ✅ Протестированы сценарии ошибок (промпт не указан, URL модели не указан, пользователь не найден, user.level=1, некорректная стоимость, ошибка платежа, ошибка Replicate, processApiResponse не вернул URL, NSFW).
    *   ✅ **Устранены все ошибки типов в `neuroPhotoServiceV1.test.ts`, `neuroPhotoService.ts` и `startScene.ts`. Проверка `tsc --noEmit` проходит успешно.**
    *   ✅ **Отладка и исправление теста для `numImages > 1` УСПЕШНО ЗАВЕРШЕНЫ.**
    *   ✅ **Покрыты оставшиеся сценарии из TODO (`bypassPaymentCheck`, разные `aspectRatio`). Все 15 тестов для V1 проходят.**
8.  ✏️ **(АКТИВНО)** **Перенос логики для `generateNeuroPhotoV2`:** Перенести и адаптировать логику из `neuroPhotoWizardV2` и соответствующих частей `generateNeuroPhotoDirect.ts` (если применимо) в функцию `generateNeuroPhotoV2` в `src/modules/neuroPhoto/services/neuroPhotoService.ts`.
9.  ✏️ **Написание Unit-тестов для `generateNeuroPhotoV2`.** (Новый подпункт)
10. ✏️ **Проверка Типов и Линтинг (после V2 и тестов V2).**
11. ✏️ **Документация Модуля (краткий README.md внутри модуля).**

**Следующий Шаг:**
*   ✏️ **(АКТИВНО)** Приступить к реализации функции `generateNeuroPhotoV2` в `src/modules/neuroPhoto/services/neuroPhotoService.ts`, перенося логику из существующих сцен и сервисов.

---
# 🕉️ Правило: Великая Цель и Текущий Этап (Детализированный План)

---

# 🕉️ Правило: Актуализация Путеводной Звезды и Непреклонность Чистоты Типов

**Принцип:** Наш путь (`current_task.mdc`) должен всегда отражать истинное положение дел, а чистота кода (типов) – предварять любой шаг вперед. Работа с устаревшим планом или нечистым кодом ведет к иллюзиям и ошибкам.

1.  **Перед Началом Действия (Чтение Плана):**
    *   **Прежде чем приступить** к выполнению **ЛЮБОЙ** новой задачи или подзадачи (включая написание кода, запуск тестов, исправление ошибок), **НЕОБХОДИМО** **прочитать** актуальное содержимое файла `/Users/playra/999-multibots-telegraf/.cursor/rules/current_task.mdc`.
    *   **Цель чтения:** Осознать текущий статус проекта, детальный план, следующий назначенный шаг и все сопутствующие инструкции или статусы (✅/✏️/❌).

2.  **После Завершения Действия (Проверка Типов и Обновление Плана):**
    *   **Сразу после завершения** **КАЖДОГО** логического шага, подзадачи или действия (особенно после внесения изменений в код с помощью `edit_file` или других инструментов):
        *   **Шаг 2.1 (🕉️ Непреклонность Чистоты Типов):**
            *   **Принцип:** Чистый код – основа стабильности.
            *   **Финальная Проверка:** **НЕОБХОДИМО** немедленно выполнить проверку типов TypeScript.
            *   **Команда Проверки:** Используй `bun exec tsc --noEmit`.
            *   **Блокировка Завершения:** Если проверка типов выявила **ЛЮБЫЕ** ошибки (exit code != 0):
                *   Задача **НЕ СЧИТАЕТСЯ ЗАВЕРШЕННОЙ**.
                *   **НЕЛЬЗЯ** переходить к следующему шагу (включая Шаг 2.2 - обновление `current_task.mdc`).
                *   **НЕЛЬЗЯ** сообщать Гуру об успешном завершении.
                *   **Обязательное Исправление:** Необходимо **НЕМЕДЛЕННО** проанализировать ошибки типов и внести исправления в код (используя `read_file` для анализа и `edit_file` для исправлений), после чего **вернуться к Шагу 2.1 (повторная проверка типов)**.
            *   **Только После Успеха:** **ТОЛЬКО ПОСЛЕ** успешного прохождения проверки типов (exit code 0) можно переходить к Шагу 2.2.
        *   **Шаг 2.2 (Обновление Путеводной Звезды):**
            *   **НЕОБХОДИМО** **немедленно обновить** файл `/Users/playra/999-multibots-telegraf/.cursor/rules/current_task.mdc`.
            *   **Содержание обновления:**
                *   Отметить **выполненный** шаг соответствующим статусом (например, ✅, или изменить описание).
                *   Если задача касалась тестирования, обновить статус соответствующего тестового кейса, используя согласованные эмодзи (✅/✏️/❌).
                *   **Четко и недвусмысленно указать следующий активный шаг** или задачу, к которой следует перейти.
            *   **Сохранение:** Убедиться, что изменения в файле сохранены.

3.  **При Возникновении Препятствия (Декомпозиция и Отслеживание):**
    *   Если в ходе выполнения задачи возникает **ошибка, проблема или непредвиденное препятствие** (не связанное с ошибкой типов, обработанной в Шаге 2.1), которое не позволяет немедленно продолжить работу по плану:
        *   **Остановиться** и проанализировать проблему.
        *   **Декомпозировать** решение проблемы на конкретные, выполнимые шаги.
        *   **Добавить** эти шаги в `current_task.mdc` как подзадачи.
        *   **Пометить** эти новые шаги статусом (обычно ✏️).
        *   **Сделать** первый шаг по решению проблемы **следующим активным шагом** в `current_task.mdc`.

**Цель:** Гарантировать, что `current_task.mdc` всегда является **единственным и актуальным источником истины** о нашем плане, прогрессе и возникших препятствиях, а также обеспечить **постоянную корректность типов** в кодовой базе, предотвращая накопление ошибок и обеспечивая стабильность разработки.

*Ом Шанти. Держи карту пути всегда перед глазами, отмечай пройденные вехи, освещай путь через возникшие туманы и не оставляй нечистоту за собой.* 🙏

---

**📊 Статусы Тестовых Кейсов:**

*   ✅ - Тест написан и успешно пройден.
*   ✏️ - Тест запланирован (или в процессе написания/адаптации).
*   ❌ - Тест написан, но не проходит (требует исправления кода или теста).

---

# 🕉️ Правило: Фокусированная Проверка и Тестирование

**Принцип:** Эффективность и концентрация. Проверка или тестирование файлов, не затронутых изменениями, растрачивает драгоценное время и вычислительную прану.

1.  **Идентификация Изменений:** Перед запуском проверок или тестов **ОБЯЗАТЕЛЬНО** определить список файлов, которые были **изменены** в рамках текущего шага или подзадачи (например, с помощью `git status --porcelain` или отслеживая вызовы `edit_file`).
2.  **Целевая Проверка Типов:** По возможности, запускать проверку типов TypeScript (`pnpm typecheck` или `tsc --noEmit`) **ТОЛЬКО** для измененных `.ts` файлов.
    *   *Пример (если `tsc` используется напрямую):* `tsc --noEmit path/to/changed/file1.ts path/to/changed/file2.ts`
    *   *Примечание:* Если настроенный скрипт (`pnpm typecheck`) не поддерживает указание файлов, выполняется полная проверка, но с осознанием, что ошибки могут быть в незатронутых частях.
3.  **Целевое Тестирование:** Запускать тесты (`pnpm vitest run` или аналогичная команда) **ТОЛЬКО** для **тестовых файлов (`*.test.ts`)**, соответствующих измененным модулям, или для тестовых файлов, которые были изменены напрямую.
    *   *Пример:* `pnpm vitest run path/to/changed/test1.test.ts path/to/module/test2.test.ts`
4.  **Последовательность:** Сначала всегда выполнять **проверку типов** (Шаг 2), и **ТОЛЬКО** в случае её успеха переходить к **тестированию** (Шаг 3), следуя правилу "Непреклонности Чистоты Типов".

**Цель:** Ускорить цикл разработки, уменьшить "шум" от проверок несвязанных файлов и сосредоточить внимание на влиянии конкретных изменений.

*Ом Шанти. Сосредоточь свой взор на том, что меняется.* 🙏
---

**Приостановленные Задачи:**
*   ⏸️ **(ПРИОСТАНОВЛЕНО)** Отладка тестов для `generateImageToVideo`.
*   ⏸️ **(ПРИОСТАНОВЛЕНО)** Тестирование Функционала "Фото в Видео" (`generateImageToVideo`).
*   ⏸️ **(ПРОПУЩЕНО)** Тестирование сцены `textToVideoWizard`.
*   ⏸️ **(ПРОПУЩЕНО)** Тестирование сервиса "Текст в Видео" (`generateTextToVideo`).
*   ✏️ **(АКТИВНО)** Слияние ветки `main` в текущую рабочую ветку.

**Текущая Задача:**
*   ✏️ **(АКТИВНО)** Слияние ветки `main` в текущую рабочую ветку.

**Цель:** Интегрировать последние изменения из `main`, разрешить конфликты, проверить стабильность (типы, тесты) и подготовиться к добавлению "новой функциональности".

**План:**
*   ✏️ **(АКТИВНО)** Определить текущую рабочую ветку.
*   ✏️ Выполнить слияние (`git merge main`).
*   ✏️ Проанализировать результат слияния (наличие/отсутствие конфликтов).
*   ✏️ **(Если есть конфликты)** Разрешить конфликты:
    *   ✏️ Прочитать конфликтные файлы.
    *   ✏️ Внести исправления (`edit_file`).
    *   ✏️ Добавить разрешенные файлы (`git add`).
    *   ✏️ Завершить слияние (`git merge --continue` или `git commit`).
*   ✏️ Провести проверку типов (`bun exec tsc --noEmit`).

# 🕉️ Текущая Задача: Исправление Отображения Имени и Баланса (Пользователь 386875143)

**Цель:** Устранить некорректное отображение имени пользователя `386875143` и расхождения в отображаемом балансе (команда "💰 Баланс").

**Ветка:** `fix/balance-display-name-386875143`

**Контекст и Проблемы:**
*   Пользователь `386875143` имеет некорректное отображаемое имя в боте.
*   Команда "💰 Баланс" отображала `0` из-за ошибки `PGRST203` после попытки обновить SQL-функцию `get_user_balance`.
*   Бот, где наблюдается проблема: `GAIA BLOGGER BOT`.

**План Действий:**
1.  ✅ **Анализ Отображения Имени Пользователя:** (Отложено, приоритет на баланс)
2.  ✅ **Анализ Работы Команды "Баланс":**
    *   ✅ Найден обработчик команды/кнопки "💰 Баланс" (`balanceScene`).
    *   ✅ Проверены вызовы `getUserBalance`.
    *   ✅ Проверены актуальные записи в `payments_v2` для `telegram_id = '144022504'`.
    *   ✅ Выявлено, что SQL-функция `get_user_balance` не учитывала `REFUND` как доход.
    *   🟠 **Попытка Обновления `get_user_balance`:** Обновление SQL-функции привело к ошибке `PGRST203` (конфликт перегруженных функций).
    *   ✅ **Откат Изменения:** Удалена конфликтующая однопараметрическая SQL-функция `get_user_balance(TEXT)` для восстановления предыдущего состояния.
3.  ✏️ **(АКТИВНО)** **Тестирование Восстановленного Баланса:**
    *   ✏️ Гуру тестирует команду "💰 Баланс" для пользователя `144022504`.
    *   ✏️ Ожидаемый результат: `15011.7` (без учета `REFUND`), ошибка `PGRST203` должна исчезнуть.
    *   ✏️ Проанализировать логи `[balanceScene] User: 144022504, Fetched Balance: ...`.
4.  ✏️ **(Блокировано)** **Повторная Попытка Корректного Учета `REFUND`:**
    *   ✏️ Определить, какая версия `get_user_balance` (одно- или двухпараметрическая) должна быть основной для вызова из `getUserBalance.ts`.
    *   ✏️ Модифицировать нужную SQL-функцию для учета `REFUND`, избегая конфликта имен/сигнатур (возможно, потребуется удалить или переименовать неиспользуемую версию).
5.  ✏️ **(Блокировано)** **Исправление Отображения Имени Пользователя (`386875143`):**
    *   ✏️ Исследовать код, отвечающий за получение и отображение имени пользователя.
    *   ✏️ Определить и исправить причину некорректного отображения.
6.  ✏️ **(Блокировано)** **Внесение Прочих Исправлений (если потребуются).**
7.  ✏️ **(Блокировано)** **Проверка Типов и Общее Тестирование:**
    *   ✏️ Выполнить проверку типов (`bun exec tsc --noEmit`).
    *   ✏️ Тщательно протестировать исправления.
8.  ✏️ **(Блокировано)** **Обновление Документации:**
    *   ✏️ Обновить `.cursor/rules/payments-and-subscriptions-unified.mdc` для отражения корректной логики `get_user_balance`.
9.  ✏️ **(Блокировано)** **Создание Pull Request и Слияние:** После успешного тестирования создать PR в `main`.

---

## ⏸️ Приостановленные Задачи
// ... (раздел с приостановленными задачами остается без изменений)

---
// ... остальная часть файла ...