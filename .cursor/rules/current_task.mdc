---
description: 
globs: 
alwaysApply: false
---
# 🕉️ Текущая Задача: Создание Независимых Модулей в `src/modules/` 🕉️

**Цель:** Перенести и рефакторизовать функциональность из `src/services/` (включая `src/services/plan_b/`) в **независимые, самодостаточные модули** внутри директории `src/modules/`, с четко определенными зависимостями (DI), следуя принципам, примененным к `localImageToVideo` и `generateTextToImage`.

**✨ Целевая Архитектура Модульной Системы ✨**

*   **Обитель Модулей:** Каждый функциональный блок (генерация текста в видео, работа с аватаром и т.д.) будет жить в своей собственной директории: `src/modules/[имяМодуля]/`.
*   **Врата Модуля:** Главный экспорт модуля (функция, класс) будет осуществляться через `index.ts` в корне директории модуля.
*   **Карта Зависимостей:** Все внешние зависимости (logger, клиенты API, другие модули, хелперы) будут **явно** определены в интерфейсе `[ИмяМодуля]Dependencies` внутри файла `types.ts` модуля.
*   **Нектар Внедрения (DI):** Зависимости **не будут** импортироваться напрямую внутри модуля. Вместо этого они будут **передаваться снаружи** как аргумент основной функции модуля или в конструктор класса (принцип Dependency Injection).
*   **Внутренний Мир:** Утилиты и хелперы, используемые **только** этим модулем, будут находиться внутри его директории (например, `src/modules/[имяМодуля]/utils/`).
*   **Лотос Независимости:** Модули должны быть максимально самодостаточными, не зависящими от глобального контекста (как `ctx` Telegraf) или специфических деталей других модулей, кроме как через четко определенные интерфейсы зависимостей.
*   **Храм Инстанцирования (DI Контейнер - Будущее):** Позже мы создадим центральный механизм (фабрику или контейнер), который будет отвечать за сборку модулей и внедрение в них необходимых зависимостей.

**Ветка:** `refactor/modular-system` (Слита с `recover`, конфликты разрешены)

**Статус Слияния:** ✅ Все конфликты разрешены, проверка типов (`bun run typecheck`) пройдена.

---

## 📝 Общий План Рефакторинга `src/services` в `src/modules`

1.  [✅] **Подготовка:** Слияние `recover`, разрешение конфликтов, проверка типов.
2.  [✏️] **Создание Модулей в `src/modules/`:** ✨ (По одному, из `src/services/` и `src/services/plan_b/`)

    *   [✅] **Модуль: `generateTextToImage`** (из `src/services/plan_b/generateTextToImage.ts`)
        *   *Статус:* Завершено (включая README).
    *   [✏️] **Модуль: `generateTextToVideo`** (из `src/services/plan_b/generateTextToVideo.ts`)
        *   [✅] Анализ Зависимостей.
        *   [✅] Определение Интерфейса Зависимостей.
        *   [✅] Рефакторинг Функции.
        *   [✅] Интернализация Utils.
        *   [✅] Конфигурация.
        *   [✅] Создание Структуры Модуля.
        *   [✅] Обновление Вызовов (в тестах).
        *   [✏️] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/plan_b/generateTextToVideo.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `videoService`** (из `src/services/plan_b/videoService.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`VideoServiceDependencies`).
        *   [ ] Рефакторинг Класса/Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/videoService/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/plan_b/videoService.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateSpeech`** (из `src/services/generateTextToSpeech.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateSpeechDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateSpeech/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/generateTextToSpeech.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateVoiceAvatar`** (из `src/services/generateVoiceAvatar.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateVoiceAvatarDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateVoiceAvatar/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/generateVoiceAvatar.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateImageToPrompt`** (из `src/services/generateImageToPrompt.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateImageToPromptDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateImageToPrompt/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/generateImageToPrompt.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateImageToVideo`** (из `src/services/generateImageToVideoAdapter.ts`?)
        *   *Примечание: Проверить, является ли `generateImageToVideoAdapter.ts` основной точкой входа.*
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateImageToVideoDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateImageToVideo/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/generateImageToVideoAdapter.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateLipSync`** (из `src/services/generateLipSync.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateLipSyncDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateLipSync/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/generateLipSync.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `aiAssistantService`** (из `src/services/plan_b/aiAssistantService.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`AiAssistantServiceDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/aiAssistantService/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/plan_b/aiAssistantService.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `notificationService`** (из `src/services/plan_b/notificationService.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`NotificationServiceDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/notificationService/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/plan_b/notificationService.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `broadcastService`** (из `src/services/plan_b/broadcastService.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`BroadcastServiceDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/broadcastService/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/plan_b/broadcastService.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `avatarService`** (из `src/services/plan_b/avatarService.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`AvatarServiceDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/avatarService/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/plan_b/avatarService.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `uploadVideoToServer`** (из `src/services/uploadVideoToServer.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`UploadVideoToServerDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/uploadVideoToServer/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/uploadVideoToServer.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `createModelTraining`** (из `src/services/createModelTraining.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`CreateModelTrainingDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/createModelTraining/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/createModelTraining.ts`).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateNeuroImage`** (из `src/services/generateNeuroImageV2.ts`? или `generateNeuroImage.ts`?)
        *   *Примечание: Изучить обе версии (`V2` и старую), выбрать актуальную или объединить.*
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateNeuroImageDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateNeuroImage/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла(ов).
        *   [ ] README Модуля.
    *   [ ] **Модуль: `generateImageFromPrompt`** (из `src/services/generateImageFromPrompt.ts`)
        *   [ ] Анализ Зависимостей.
        *   [ ] Определение Интерфейса Зависимостей (`GenerateImageFromPromptDependencies`).
        *   [ ] Рефакторинг Функции (DI).
        *   [ ] Интернализация Utils.
        *   [ ] Конфигурация (DI).
        *   [ ] Создание Структуры Модуля (`src/modules/generateImageFromPrompt/`).
        *   [ ] Обновление Вызовов.
        *   [ ] Модульное Тестирование.
        *   [ ] Проверка типов.
        *   [ ] Удаление старого файла (`src/services/generateImageFromPrompt.ts`).
        *   [ ] README Модуля.

3.  [ ] **Реализация DI Контейнера/Фабрики:** 🏭 (После создания нескольких модулей) Создать механизм для удобного инстанцирования модулей с их зависимостями.
4.  [ ] **Финальная проверка типов:** `bun run typecheck` 🕉️
5.  [ ] **Тестирование:** ✅ Запуск всех тестов (`vitest run`).
6.  [ ] **Коммит:** 💾 Фиксация модульной системы.
7.  [ ] **Обновление Документации:** ✍️ Обновить `README.md` проекта и модулей.

---

## 👥 Параллельная Работа Агентов 👥

В системе работают два агента параллельно: Agent 1 и Agent 2.

### 🤖 Agent 1

*   **Задача:** Завершение модуля `generateTextToImage` (уже сделано, может взять следующий?)

### 🤖 Agent 2 (ТЫ!)

*   **Задача:** Завершение модуля `generateTextToVideo` (Написание/адаптация юнит-тестов).

**➡️ Следующий шаг (Agent 2):** ✨ Написание/адаптация юнит-тестов для модуля `generateTextToVideo`.
