================================================================================
  FACE SWAP WIZARD - QUICK INTEGRATION GUIDE
================================================================================

NEW FILE CREATED:
  src/scenes/faceSwapWizard/index.ts

5 FILES TO UPDATE:
  1. src/interfaces/paidServices.ts  (2 additions)
  2. src/interfaces/modes.ts         (1 addition)
  3. src/scenes/index.ts             (1 addition)
  4. src/registerCommands.ts         (2 additions)

================================================================================
STEP 1: src/interfaces/paidServices.ts
================================================================================

LINE ~10 (after ImageUpscaler):
  FaceSwap = 'face_swap', // Замена лица на изображении

LINE ~75 (after ImageUpscaler config):
    [PaidServiceEnum.FaceSwap]: {
      name: 'Замена лица',
      pricingType: PricingType.SIMPLE,
      baseCostUSD: 0.01,
      description: 'Замена лица на изображении с помощью ИИ',
      category: 'image',
    },

================================================================================
STEP 2: src/interfaces/modes.ts
================================================================================

LINE ~8 (after ImageUpscaler):
  FaceSwap = PaidServiceEnum.FaceSwap,

================================================================================
STEP 3: src/scenes/index.ts
================================================================================

LINE ~3 (after imageUpscalerWizard):
export * from './faceSwapWizard'

================================================================================
STEP 4: src/registerCommands.ts - IMPORT
================================================================================

LINE ~48 (in import block):
  imageUpscalerWizard,
  faceSwapWizard,  // ← ADD THIS
  improvePromptWizard,

================================================================================
STEP 5: src/registerCommands.ts - STAGE
================================================================================

LINE ~123 (in stage array):
  imageUpscalerWizard,
  faceSwapWizard,  // ← ADD THIS
  improvePromptWizard,

================================================================================
VERIFICATION
================================================================================

npm run build        # Should compile without errors
grep "faceSwapWizard" src/scenes/index.ts
grep "faceSwapWizard" src/registerCommands.ts

================================================================================
OPTIONAL: ADD TO MENU
================================================================================

File: src/menu/mainMenu.ts
Add button:
  Markup.button.callback(
    isRu ? '🎭 Замена лица' : '🎭 Face Swap',
    `mode:${ModeEnum.FaceSwap}`
  )

================================================================================
OPTIONAL: ADD COMMAND
================================================================================

File: src/registerCommands.ts
Add command handler:
  bot.command('faceswap', async (ctx) => {
    await ctx.scene.enter(ModeEnum.FaceSwap)
  })

================================================================================
COST: 1 ⭐ per face swap ($0.01 base cost + 50% markup)
================================================================================

DOCUMENTATION:
  - FACESWAP_WIZARD_DELIVERY.md          (summary)
  - docs/FACESWAP_SCENE_INTEGRATION.md   (detailed guide)
  - docs/FACESWAP_INTEGRATION.md         (service docs)

================================================================================
